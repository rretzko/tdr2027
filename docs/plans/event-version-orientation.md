# TheDirectorsRoom.com — TDR2027
# Event & Version Domain — Orientation & Build Specification

**Audience:** Claude Code (PhpStorm) and human reviewers.
**Status:** In-scope build (§0.2 "Build now") implemented and tested, including Version Invitations (§5.4), Version Pitch Files (§5.5), and Version Obligations (§5.6, not yet committed). Verified against the codebase 2026-07-09: 522/522 Feature/Unit tests passing app-wide (500 baseline + 22 new for §5.6, added across `VersionEditTest`, `Registrations/VersionObligationsTest`, and `VersionObligationObserversTest`). Reference-only items (§7, adjudication/tab room/cut-offs) remain intentionally unbuilt. See §9 — all tracked items are resolved except intentionally-deferred sub-features. **Known open issue in §5.6**: bulleted/numbered lists don't render with visible markers in the live `flux:editor` admin UI or its Preview modal, even for freshly-typed (not pasted) content — confirmed the stored HTML and server-side rendering are both correct via direct DB inspection and a Blade-render test, so the defect is isolated to the client-side editor/TipTap layer, not this codebase's PHP/Blade/CSS. Unresolved; see the note at the end of §9. **Candidate Registration workflow designed 2026-07-16** (§5.8 Version Invitation Requests, new `candidate_upload_files` table in §5.2, and the access/navigation rules in §6.2) — scoped from a source design doc plus a clarifying-question pass. ~~Not yet built.~~ §5.8 built 2026-07-16 (§9 item 14); `candidate_upload_files` built 2026-08-12 (§9 item 33). **Audition Environment built 2026-07-29** (§5.9: `version_rooms`, `room_score_categories`, `room_voice_parts`, `room_judges`, `score_categories`, `score_factors`, and a schema-only `scores` table, plus the `VersionRooms` and `VersionScoringRubric` Event Manager/Registration Manager CRUD screens, and `VersionRoleAssignmentService::canManageAuditionEnvironment()`) — scoped from a source design doc plus a clarifying-question pass. 681/681 Feature/Unit tests passing app-wide, PHPStan clean. **Co-Registration Manager delegation built 2026-08-04** (§5.3): a Registration Manager can now assign/revoke Co-Registration Manager via its own screen, `VersionCoRegistrationManagers`, paired with mutually-exclusive county-of-responsibility assignment (`co_registration_manager_counties`) — no longer assignable from the generic Configure → Roles tab. 718/718 Feature/Unit tests passing app-wide, PHPStan clean. **Registration Manager Reporting Module designed and built 2026-08-04, pagination added 2026-08-06** (§5.10): nine county-scoped reports over registered Candidates/Teachers/Schools for Registration/Co-Registration Managers (plus Event Manager/Founder oversight) — new `teacher_payments`, `candidate_payments`, `version_teacher_packets` tables and a `PaymentType` enum — scoped from a source design doc plus a clarifying-question pass. 900/900 Feature/Unit tests passing app-wide. **Web Registration Manager Module designed and built 2026-08-05** (§5.11): gives the previously name-only "Web Registration Manager" role two real, Version- and invitation-scoped capabilities — impersonate an invited teacher (extending the existing Founder-only impersonation mechanism, locked to that Version's Registrations pages and barred from the teacher's account/profile) and transfer students between teachers/schools (generalizing the existing `ReplacedTeacherStudentTransfer` into a new `TeacherStudentTransferService`, reusing the existing auto-enrollment observer cascade rather than writing new enrollment logic) — scoped from a source design doc plus a clarifying-question pass. 837/837 Feature/Unit tests passing app-wide, PHPStan clean. **Adjudication built 2026-08-07** (§7.2, §9 items 22-23): judge visibility/access gate, and the real judge scoring page (Judge Bios, Room Progress bar, candidate roster, score-entry form, Room Scoring table with tolerance checking, candidate Recordings). **Score-tally design + `AuditionResult` built 2026-08-08** (§5.2, §9 item 24): a live, on-demand candidate total (`AdjudicationService::candidateTotals()`, honoring `score_factors.multiplier`) plus room-/version-level Tab Room progress rollups (`roomProgress()`/`versionProgress()`) for while adjudication is open, and a narrower-than-legacy `AuditionResult`/`audition_results` frozen tally snapshot (no `accepted`/`acceptance_abbr` — that stays on `Candidate`, for the not-yet-built cut-off phase) intended to be written once at the not-yet-built Version-close step. 899/899 Feature/Unit tests passing app-wide, PHPStan clean. **Tab Room Module — Phase 1 (Add/Edit Scores + Adjudication Tracking) built 2026-08-10** (§7.2–§7.3, §9 item 26): scoped from a source design doc (`Tab Room Module.docx`, 13 embedded screenshots reviewed) plus a clarifying-question pass; `canManageTabRoom()`, Tab-Room-side score override auditing (`scores.overridden_by_user_id`/`overridden_at`), the destructive voice-part-change cascade, and a manager-facing lens over the same `AdjudicationService` the judge page already used. Surfaced and fixed a real scope bug in `candidatesForRoom()` along the way (see §9 item 26). 944/944 Feature/Unit tests passing app-wide immediately after Phase 1+2, PHPStan clean; every subsequent change in this pass verified against its own affected-area test run, all green. **Tab Room Module — Phase 2 (Ensemble Cut-offs, the `CutoffStrategy` engine, Ensemble Breakdown pie charts, and Ensemble History) built 2026-08-10–11** (§7.3–§7.5, §8.5, §9 items 27–28): the four `CutoffStrategy` cases (§7.5, §8.5) are real implementations for the first time — `PerVoicePartPerEnsembleStrategy`, `SequentialEnsembleFillStrategy`, `AlternatingEnsembleAssignmentStrategy`, `GradeSegmentedEnsemblesStrategy` — orchestrated by a new `EnsembleCutoffService`, plus a non-destructive "reopen and re-decide" UI model, per-Ensemble pie charts (color-validated per the house dataviz method), and a manually-entered/auto-snapshotted `EnsembleHistory` table for the two prior seasons this system never tracked. §7.6's Results/Close-Version machinery remains unbuilt, but the one directly-relevant slice — snapshotting Ensemble Cut-offs' counts at Close — is now wired into `VersionEdit`'s existing general Status field rather than waiting on that phase. PHPStan clean throughout; see §9 items 27–28 for full built/not-built detail and exact test counts per sub-feature. **Tab Room Module — Phase 3, Close Audition half (§7.3, §7.6, §9 item 29) built 2026-08-11**: a dedicated `Close Audition` sub-module — a new `versions.results_released_at` timestamp, owned exclusively by this page (not `VersionEdit`'s general Status field, so an Event Manager closing for unrelated administrative reasons never silently releases results), gates teacher-facing results visibility; Close sets `status=Closed` + stamps it (and still snapshots `EnsembleHistory`, same as the `VersionEdit` path); Reopen clears it without touching any Candidate decision. An optional "email participating teachers" checkbox batch-sends a new `AuditionResultsAvailableMail`, signed by the closing manager's name rather than the app name. The long-placeholder `Registrations\Results`/`ResultsIndex` pages (hardcoded "—" columns, filtered to `status=Registered` which is empty by the time cut-offs have run) are now real, gated on `results_released_at !== null`. **Tab Room Module — Phase 3, Reports half (§7.6, §9 item 30) built 2026-08-11**: the five reports Tab Room Module.docx specs — Audition Scores, Combined Audition Scores confidential/public, Ensemble Participation, Student Seniority — completing the Tab Room Module in full. A new `TabRoomReportService` reads `scores` directly (every row already carries its own frozen `judge_order_by`/`score_factor_order_by`/`voice_part_id` snapshot) rather than resolving Room/rubric membership, and a new `canManageTabRoomReports()` gate mirrors `canManageTabRoom()`. 995/995 Feature/Unit tests passing app-wide (up from item 29's 976/976), PHPStan clean throughout — Tab Room Module.docx is now fully built. **Teacher-facing public score reports + optional Share Results built 2026-08-12** (§7.6, §5.1, §9 item 31): a direct product-owner ask (not a source design doc) — `Registrations\Results` gained Per-School and Per-Person public (no student/school/teacher identity) score-report view-modals/PDFs reusing `TabRoomReportService`, a `share_results` Version setting that auto-shares the existing "All Ensembles" public PDF with every participating teacher once results are released, and sortable Name/Voice Part(`sort_order`)/Score/Result/Ensemble columns. Also corrected paper size app-wide (all seven `setPaper()` call sites, including the pre-existing five from item 30) from `a4` to `letter` per product-owner direction, and widened the new modals, after a live-downloaded PDF surfaced both defects. 1039/1039 Feature/Unit tests passing app-wide, PHPStan clean. **Obligations iron gate widened + `CandidateDetail` redesigned to card+modal + emergency-contact NOT NULL bug fixed, built 2026-08-12** (§5.6, §6.2, §9 item 32): rejection now blocks every Version-scoped teacher page, not just enrollment, via a redirect (`GuardsAcceptedObligations`), and re-accepting now reinstates the candidates that rejection's cascade withdrew (`CandidateService::reinstateAllForTeacherVersion()`); `CandidateDetail` reorganized into a fixed section order with a uniform card+modal pattern and gained Student (name, voice part — `candidates.voice_part_id`, not `students.voice_part_id`) and Home Address editing, plus per-contact Emergency Contact editing; `emergency_contacts.email`/`cell_phone` are now nullable and enforced conditionally per the pre-existing but previously-unused `versions.emergency_contact_cell`/`emergency_contact_email` flags. 431/431 passing in the affected suites, PHPStan clean app-wide. **Application/Recordings/Payment sections added to `CandidateDetail`, built 2026-08-12** (§5.2, §5.7, §9 item 33): the Application toggle-buttons UI moved into a card+modal showing the real merged document; a Recordings section finally built out the `candidate_upload_files`/`version_upload_files` design designed-but-unbuilt since 2026-07-16; a Payment section (teacher e-payment opt-in + manual `candidate_payments` recording, explicitly scoped via `AskUserQuestion` after confirming no real payment gateway exists in this codebase) was built as a clearly-labeled manual placeholder, not a live charge. 1079/1079 passing app-wide, PHPStan clean. **S3 signed-URL bug fixes + phone-number formatting, found and fixed 2026-08-12–13** (§9 item 34): live browser-testing surfaced that this S3 bucket's newer objects default to private, so the plain `->url()` pattern used for Recordings, Pitch Files, and (proactively) Application logos all 403'd silently — fixed with signed `temporaryUrl()`s everywhere; also fixed `preload="none"` stalling the displayed recording duration at 0:00/0:00, and un-formatted phone numbers in the Application document. **Recordings→Adjudication sync bridge + amber checklist status + `RecordingReviewService` mis-filed-recording assist, designed and built 2026-08-13** (§5.2, §6.2, §9 item 35): a user report that an approved Recording never appeared in the judge-facing `recordings` table surfaced two disconnected tables for the same concept — reconciled via a one-way sync bridge (chosen over a full rebuild via `AskUserQuestion`) that leaves the already-tested Adjudication code untouched; checklist items gained a third amber "in progress" render state; a new `RecordingReviewService` (filename-vs-slot-name + per-slot duration-outlier heuristics, `james-heinrich/getid3` for real duration extraction) flags a likely mis-filed recording for the teacher's own review without ever auto-rejecting. 119 passing across Registrations + the new service test file, PHPStan clean app-wide.

---

## 0. Orientation

### 0.1 Purpose of this document

This document orients an AI coding agent and human reviewers to the **Event** and **Version** domain of the TDR2027 rewrite. It defines the domain vocabulary, the data model, the lifecycle, and the business rules required to configure an event and register candidates. It is written to be read top-to-bottom before any code is generated.

### 0.2 Scope of THIS phase

**Build now (in scope):**

- The Event, Event Grades, Ensemble, Ensemble Grades, and Ensemble Voice Parts data model.
- The Version data model and its configuration child tables (dates, fees, timeslots, roles, adjudication config).
- The Configuration lifecycle phase (Event Manager sets up a Version).
- The Registration lifecycle phase, including the candidate eligibility → registered gate and the poka-yoke status surface for teachers.

**Describe for context, do NOT build yet (out of scope for this phase):**

- Pre-adjudication reconciliation, Adjudication (judge scoring UI), and the Tab Room.
- Score cut-offs and ensemble-assignment algorithms. These belong to the **Adjudication Wizard, Release 2**. They appear in §7 so the model is forward-compatible, but no code should be scaffolded for them now.

> **Decision needed:** Confirm the in/out-of-scope split above matches your intended first PR. The agent will treat §7.1–§7.8 as reference only.

### 0.3 Stack & conventions

- Modular Laravel monolith (TDR2027), Livewire + Flux UI, Pest for tests, deployed on Laravel Vapor.
- Uploads are all saved to S3.
- Authorization uses **version-scoped Spatie roles** — roles are granted relative to a specific Version, not globally.
- Datetime pairs use the `start_at` / `end_at` naming convention throughout.
- Datetimes are stored as UTC and displayed at `America/New_York`.
- Status and category fields are backed by **PHP enums**, not free strings.
- Open/close windows, fees, and timeslots are modeled as **rows in child tables** (`version_dates`, `version_fees`, `version_timeslots`), not as wide columns on `versions`.

### 0.4 How to read the spec tables

The **Event** table in §3 is the worked reference for column specification (Column / Type / Null / Default / Notes). Every other table should be fleshed out to the same level of detail before migrations are generated. Where a type or rule is genuinely undecided, it is flagged with a **Decision needed** callout rather than guessed.

---

## 1. Domain Vocabulary

These terms have precise meanings in TDR2027 and must be used consistently in code, UI, and conversation. In particular, the person being auditioned is referred to by different terms at different lifecycle stages — do not treat them as interchangeable model names.

| Term          | Meaning                                                                                                                                                                                                                                     |
|---------------|---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| Organization  | The sponsoring body that owns one or more Events (e.g., a state music association). Top of the hierarchy.                                                                                                                                   |
| Event         | A periodic — generally annual — series of activities sponsored by an Organization that results in the creation of one or more Ensembles. Holds the relatively stable definition of the program.                                             |
| Version       | A single periodic iteration of an Event. Holds the rules, dates, fees, and configuration needed to run that one occurrence.                                                                                                                 |
| Ensemble      | A choir produced as the output of a Version (e.g., Mixed Chorus, Treble Choir).                                                                                                                                                             |
| Voice Part    | A reference value (S1, S2, A1, A2, T1, T2, B1, B2, etc.). Defined in §4.3.                                                                                                                                                                  |
| **Student**   | A durable person identity (belongs to a school, has a teacher). Persists across Events and Versions.                                                                                                                                        |
| **Candidate** | A point-in-time enrollment of a Student into a specific Version — the snapshot that moves through the registration and adjudication lifecycle. Has a twelve-state status enum; see *Candidate states* below and the transition map in §8.3. |
| Teacher       | The teacher who registers and approves their Candidates for a Version.                                                                                                                                                                      |
| Roles         | Version-scoped responsibilities: Event Manager, Registration Manager, Co-Registration Manager, Web Registration Manager, Tab Room Manager, Rehearsal Manager.                                                                               |

**Candidate states** (status enum, in lifecycle order):

- `eligible` — version qualified.
- `pending` — at least one, but not all, required registration milestones met.
- `registered` — all required registration milestones met.
- `withdrew` — candidate withdrew themselves; post-registered, pre-adjudication.
- `teacher_withdrawn` — teacher withdrew the candidate; post-registered, pre-adjudication.
- `adjudicated` — candidate completed the adjudication process.
- `no_show` — adjudication result: candidate did not show up to be adjudicated.
- `incomplete` — adjudication result: candidate started but did not complete the audition.
- `accepted` — adjudication result from cutoff assignment: assigned to an ensemble.
- `not_accepted` — adjudication result from cutoff assignment: not assigned to an ensemble.
- `declined` — candidate refused acceptance into a Version ensemble; post-adjudication.
- `removed` — candidate accepted to an ensemble but failed to meet ensemble rehearsal requirements (Rehearsal Manager decision).

---

## 2. Domain Model Overview

Relationship summary (read "1—\*" as one-to-many, "\*—\*" as many-to-many):

- `Organization` 1—\* `Event`
- `Event` 1—\* `Version`
- `Event` 1—\* `Ensemble` (Ensemble is owned by the Event, populated by a Version)
- `Event` 1—\* `EventGrade`
- `Ensemble` 1—\* `EnsembleGrade`
- `Ensemble` \*—\* `VoicePart` (via `ensemble_voice_parts`)
- `Version` 1—\* `version_dates`, `version_fees`, `version_timeslots`, `version_upload_files`, `version_adjudication`
- `Version` 1—\* `Candidate`
- `Candidate` 1—\* `candidate_status_history`
- `Version` 1—1 `VersionObligation` (via `version_obligations`, unique `version_id`)
- `VersionObligation` 1—\* `VersionObligationResponse`; `VersionInvitation` 1—1 `VersionObligationResponse` (via `version_obligation_responses`, unique `version_invitation_id`)
- `Version` 1—\* `VersionInvitationRequest` (via `version_invitation_requests`, unique `version_id`+`teacher_id`; approval writes the corresponding `VersionInvitation` row — see §5.8)
- `Candidate` 1—\* `CandidateUploadFile` (via `candidate_upload_files`, one row per fulfilled `VersionUploadFile` slot — see §5.2)

---

## 3. Event

An Event is a periodic, generally annual, series of activities sponsored by an Organization that results in the creation of one or more Ensembles. The Event holds the comparatively stable definition of the program; year-to-year variation lives on the Version.

### 3.1 Event status

Event status is an enum. Default is `sandbox`.

| Status | Meaning |
|---|---|
| `sandbox` | The Event Manager is configuring the Event and its current Version: adjudication and scoring rules, dates, membership eligibility and invitations, candidate eligibility, ensemble definition. Not public. |
| `active` | The current Version is public to invited members; non-invited teachers may request an invitation. |
| `inactive` | The Event is withdrawn but not closed (e.g., COVID). |
| `closed` | The Event is closed. |

**Transitions.** Sandbox is the initial value while the Event/Version is configured; it can transition to any other status, but no status returns to sandbox. Active releases the Event/Version for teacher input and can transition to inactive or closed. Inactive closes teacher input indefinitely and can transition to active or closed. Closed marks the end of input by all actors and can transition back to active.

### 3.2 Event properties (worked reference table)

This is the reference format. Types below are proposed; confirm the flagged ones. `id` and timestamps follow Laravel defaults. The boolean rows below capture per-Event data-collection requirements.

| Column                      | Type             | Null | Default | Notes                                     |
|-----------------------------|------------------|------|---------|-------------------------------------------|
| `id`                        | bigIncrements    | no   | —       | PK                                        |
| `organization_id`           | foreignId        | no   | —       | FK → organizations; indexed               |
| `name`                      | string           | no   | —       |                                           |
| `short_name`                | string           | yes  | null    | Display abbreviation                      |
| `logo_url`                  | string           | yes  | null    | S3 / signed URL target                    |
| `logo_alt`                  | string           | yes  | null    | Accessibility alt text                    |
| `status`                    | enum             | no   | sandbox | EventStatus enum (§3.1)                   |
| `frequency`                 | enum             | no   | annual  | annual \| biannual \| biennial \| monthly |
| `audition_count`            | unsignedSmallInt | no   | 1       | Cap on auditions available to a Candidate |
| `ensemble_count`            | unsignedSmallInt | no   | 1       | Cap on ensembles created by the Event     |
| `created_at` / `updated_at` | timestamp        | yes  | null    | Laravel timestamps                        |
| `deleted_at`                | timestamp        | yes  | null    | Soft delete                               |

### 3.3 Event Grades

The eligible Candidate grades an Event will accept into its Ensemble(s).

| Column     | Notes       |
|------------|-------------|
| `id`       | PK          |
| `event_id` | FK → events |
| `grade`    | smallint    |

---

## 4. Ensembles

An Ensemble is a choir produced as the output of a Version. Ensembles are owned by the Event and re-populated each Version.

### 4.1 Ensemble

| Column         | Notes       |
|----------------|-------------|
| `id`           | PK          |
| `event_id`     | FK → events |
| `name`         |             |
| `short_name`   |             |
| `abbreviation` |             |
| `deleted_at`   | Soft delete |

### 4.2 Ensemble Grades

| Column        | Notes                            |
|---------------|----------------------------------|
| `id`          | PK                               |
| `ensemble_id` | FK → ensembles                   |
| `grade`       | Eligible grade for this Ensemble |

### 4.3 Voice Parts

| Column                      | Notes                                               |
|-----------------------------|-----------------------------------------------------|
| `id`                        | PK                                                  |
| `name`                      | Text description (e.g., soprano, alto, tenor, bass) |
| `abbr`                      | Text abbreviation (e.g., sop, alt, ten, bas)        |
| `sort_order`                | smallint to order the rows in score order           |
| `created_at` / `updated_at` | Laravel timestamps                                  |

### 4.4 Ensemble Voice Parts

| Column          | Notes            |
|-----------------|------------------|
| `id`            | PK               |
| `ensemble_id`   | FK → ensembles   |
| `voice_part_id` | FK → voice_parts |

The pivot carries no ordering column of its own by design. `Ensemble::voiceParts()` must always return its collection ordered by `voice_parts.sort_order` — the pivot's role is membership only, not sequencing.

---

## 5. Version

A Version is the periodic iteration of an Event. It contains the rules and configurations needed to run that occurrence in a systematic, predictable manner. Most cross-year variation lives here.

### 5.1 Version properties (worked reference table)

This is the reference format. Types below are proposed; confirm the flagged ones. `id` and timestamps follow Laravel defaults. The boolean rows below capture per-Event data-collection requirements.

| Column                         | Type                | Null | Default | Notes                                               |
|--------------------------------|---------------------|------|---------|-----------------------------------------------------|
| `id`                           | bigIncrements       | no   | —       | PK                                                  |
| `event_id`                     | foreignId           | no   | —       | FK → events; indexed                                |
| `name`                         | string              | no   | —       |                                                     |
| `short_name`                   | string              | yes  | null    | Display abbreviation                                |
| `senior_class_of`              | unsignedInteger     | no   | 2022    | Year of version's senior class                      |
| `status`                       | enum                | no   | sandbox | EventStatus enum (§3.1)                             |
| `application_type`             | enum                | no   | pdf     | eapplication, pdf                                   |
| `audition_timeslot`            | unsignedTinyInt     | yes  | 20      | for in_person auditions, sets the audition timeslot |
| `audition_type`                | enum                | no   | remote  | in_person, remote                                   |
| `birthday`                     | boolean             | no   | false   | Event requires candidate's birthday                 |
| `emergency_contact_name`       | boolean             | no   | true    | Event requires emergency contact name               |
| `emergency_contact_cell`       | boolean             | no   | true    | Event requires emergency contact cell phone         |
| `emergency_contact_email`      | boolean             | no   | false   | Event requires emergency contact email              |
| `height`                       | boolean             | no   | false   | Event requires candidate's height                   |
| `home_address`                 | boolean             | no   | false   | Event requires candidate's home address             |
| `judge_count`                  | unsignedTinyInteger | no   | 1       | Count of judges per room                            |
| `max_registrants`              | unsignedInteger     | yes  | null    | Cap on total Candidates                             |
| `max_upper_voice_registrants`  | unsignedInteger     | yes  | null    | Cap on upper-voice Candidates                       |
| `pitch_file_visibility`        | enum                | no   | both    | both, candidate, teacher                            |
| `score_order`                  | enum                | no   | asc     | asc, desc                                           |
| `share_results`                | boolean             | no   | false   | Auto-share the public (no name/school) combined score PDF with participating teachers when results are released — built 2026-08-12, see §7.6 and §9 item 31 |
| `shirt_size`                   | boolean             | no   | false   | Event requires candidate's shirt size               |
| `teacher_cell`                 | boolean             | no   | true    | Event requires teacher's cell phone                 |
| `upload_type`                  | enum                | no   | none    | audio, none, video                                  |
| `created_at` / `updated_at`    | timestamp           | yes  | null    | Laravel timestamps                                  |
| `deleted_at`                   | timestamp           | yes  | null    | Soft delete                                         |


### 5.2 Configuration child tables

Rather than many wide columns on `versions`, configuration is grouped into child tables. The originals listed below map to these tables:

**Adjudication rules** → `version_adjudication` (and related)

- Number of rooms, number of judges per room, judge types.
- **Judge types:** head judge, judge 2, judge 3, judge 4, judge monitor, monitor.
- **Scoring categories:** `version_id`, `description`, `order_by`, timestamps.
- **Scoring factors:** `version_id`, `category_id`, `factor`, `abbreviation`, `best`, `worst`, `interval_by`, `multiplier`, `tolerance`, `order_by`, timestamps.
- Note: tolerance is currently applied at the **room level**; the scoring-factor `tolerance` column is future-proofing only at this point.
- Room definitions (`score_category_id`(s), `voice_part_id`(s)), room tolerances, judge assignments.

**Milestone dates** → `version_dates` (one row per Version per date type, using `start_at` / `end_at`)

Date types: 
- admin: Timestamp that users named in version_roles can access the system.  end_at is null.
- teacher: Timestamp that teachers can access the candidate registration pages for the version. end_at is null.  Access to registration pages is also controlled by version and other date events; ex. when final_teacher_changes is met, the teacher loses the ability to change any candidate registration data.
- candidate: Timestamp that candidates can access version registration pages on StudentFolder.info. Timestamps are used for both start_at and end_at values. Certain registration pages (ex. pitch files and payment) will be controlled by other timestamps, remaining available to candidates after the access to change the registration pages has ended.
- final_teacher_changes: Timestamp after which teacher changes to candidate registration pages is disabled.  end_at date is null.
- adjudication: Timestamps to define the adjudication period.  After the start_at timestamp, named judges for the version can access the adjudication pages.  Access to the adjudication pages ends at end_at timestamp.
- tab_room: Timestamp to open access to the tab room pages for named tab room managers.  end_at is null. Access to the tab room pages ends when the version status is "closed". 
- participation_fee: Timestamp to open access to payment by accepted candidates for ensemble participation fees. start_at and end_at define the ability to execute payment via StudentFolder.info and TheDirectorsRoom.com.
- rehearsal: Timestamp to give the named version rehearsal manager(s) access to the rehearsal pages. start_at should be no later than when the version closes.  end_at should be no later than when the performance occurrs. 
- postmark_deadline: Advisory timestamp to warn teachers when any physical packets must be postmarked by.

**`version_dates` table** — current implementation:

| Column                      | Type            | Notes                                                                                                                     |
|-----------------------------|-----------------|---------------------------------------------------------------------------------------------------------------------------|
| `id`                        | bigIncrements   | PK                                                                                                                        |
| `version_id`                | foreignId       | fk                                                                                                                        |
| `date_type`                 | enum            | admin, teacher, candidate, final_teacher_changes, adjudication, tab_room, participation_fee, rehearsal, postmark_deadline |
| `start_at`                  | Timestamp       | Laravel timestamp                                                                                                         |
| `end_at`                    | Timestamp       | Laravel timestamp, nullable, default = null                                                                               |
| `created_at` / `updated_at` | timestamp       | Laravel timestamps                                                                                                        |


**Fees** → `version_fees`

**`version_fees` table** — next-phase implementation, to identify actual version fees to be charged:

| Column                      | Type            | Notes                  |
|-----------------------------|-----------------|------------------------|
| `id`                        | bigIncrements   | PK                     |
| `version_id`                | foreignId       | fk                     |
| `registration`              | unsignedInteger | in cents, default 2000 |
| `on_site_registration`      | unsignedInteger | in cents, default 0    |
| `participation`             | unsignedInteger | in cents, default 0    |
| `epayment_surcharge`        | unsignedInteger | in cents, default 0    |
| `housing`                   | unsignedInteger | in cents, default 0    |
| `created_at` / `updated_at` | timestamp       | Laravel timestamps     |


**E-payment credentials** → `epayment_credentials`

**`epayment_credentials` table** — next-phase implementation, to epayment vendor credentials. Still schema-only — no admin UI writes to it (a row has to be created directly, e.g. via tinker) — but as of 2026-08-13 (§9 item 33) its mere *presence* for a Version is read as a boolean gate: `CandidateDetail`'s Payment section (opt-in checkbox + manual `candidate_payments` recording) only renders when `Version::epaymentCredential()` resolves a row:

| Column                      | Type            | Notes                             |
|-----------------------------|-----------------|-----------------------------------|
| `id`                        | bigIncrements   | PK                                |
| `version_id`                | foreignId       | fk                                |
| `epayment_id`               | string          | typically email address           |
| `secret`                    | string          | encrypted, nullable, default null |
| `created_at` / `updated_at` | timestamp       | Laravel timestamps                |

**`version_teacher_epayment_opt_ins` table** — new 2026-08-13 (§9 item 33): a teacher's roster-wide opt-in to e-payment for one Version, not per-Candidate — toggled via a checkbox on `CandidateDetail`'s Payment card, but it applies to every Candidate that teacher manages in that Version at once.

| Column         | Type          | Notes                                            |
|----------------|---------------|---------------------------------------------------|
| `id`           | bigIncrements | PK                                                 |
| `version_id`   | foreignId     | FK → versions; cascade on delete                   |
| `teacher_id`   | foreignId     | FK → teachers; cascade on delete                   |
| `opted_in`     | boolean       | default false                                      |
| `created_at` / `updated_at` | timestamp | Laravel timestamps                    |

Unique on (`version_id`, `teacher_id`).

**`version_membership_requirements` table** — current implementation

| Column                      | Type          | Notes                  |
|-----------------------------|---------------|------------------------|
| `id`                        | bigIncrements | PK                     |
| `version_id`                | foreignId     | fk                     |
| `membership_card`           | boolean       | default false          |
| `valid_thru`                | date          | nullable, default null |
| `created_at` / `updated_at` | timestamp     | Laravel timestamps     |

**`version_counties` table** — current implementation

| Column                      | Type          | Notes                  |
|-----------------------------|---------------|------------------------|
| `id`                        | bigIncrements | PK                     |
| `version_id`                | foreignId     | fk                     |
| `county_id`                 | foreignId     |                        |
| `created_at` / `updated_at` | timestamp     | Laravel timestamps     |

**`version_ensemble_order` table** — current implementation

| Column                      | Type            | Notes              |
|-----------------------------|-----------------|--------------------|
| `id`                        | bigIncrements   | PK                 |
| `version_id`                | foreignId       | fk                 |
| `ensemble_id`               | foreignId       | fk                 |
| `order_by`                  | unsignedTinyInt | default 1          |
| `created_at` / `updated_at` | timestamp       | Laravel timestamps |

**Version timeslots (if in-person)** → `version_timeslots` (`start_at`, `end_at`, duration, break count)

**`version_timeslots` table** — next-phase implementation:

| Column                      | Type          | Notes              |
|-----------------------------|---------------|--------------------|
| `id`                        | bigIncrements | PK                 |
| `version_id`                | foreignId     | fk                 |
| `school_id`                 | foreignId     | fk                 |
| `timeslot`                  | timestamp     | Laravel timestamp  |
| `created_at` / `updated_at` | timestamp     | Laravel timestamps |

**Expected upload files (if `audition_type` is remote)** → `version_upload_files`

**`version_upload_files` table** — current implementation, the generic, ordered list of files a Candidate is expected to upload (e.g., scales, solo, quintet) when `audition_type` is `remote`:

| Column                      | Type            | Notes                                          |
|-----------------------------|-----------------|-------------------------------------------------|
| `id`                        | bigIncrements   | PK                                              |
| `version_id`                | foreignId       | FK → versions; cascade on delete                |
| `name`                      | string          | Generic file label (e.g., "scales", "solo")     |
| `order_by`                  | unsignedTinyInt | default 1                                       |
| `created_at` / `updated_at` | timestamp       | Laravel timestamps                              |

`Version::uploadFiles()` is a `HasMany` ordered by `order_by`. There is no stored `upload_file_count` column — `Version::upload_file_count` is a derived accessor (`count($this->uploadFiles)`), so the count can never drift from the list of names.

**Other Version configuration (column or small table as appropriate):**

- **Audition type:** in-person \| remote.
- **Membership requirements:** organization id, membership card, `valid_thru` date.
- **Application type:** eapplication \| pdf (enum).
- **Eligible counties:** if applicable.
- **County assignments:** if multiple co-registration managers.
- **Event ensemble order:** if multiple Ensembles share the same candidate pool (drives cascading / alternating assignment).
- **Default invitations:** auto-invite current Organization members.
- **Pitch files & visibility:** audience (teacher \| candidate \| both); visible to candidates post-candidate-close; visible to teachers post-teacher-close; visible to teachers post-version-close. Event Manager CRUD is built — see §5.5. The post-close visibility windows are not yet enforced (flagged in §5.5).
- **Score order:** ascending (golf; lower is better) or descending (bowling; higher is better).

**`candidates` table** — next-phase implementation, to identify a candidate:

| Column                      | Type                | Notes                                                                                                                                                          |
|-----------------------------|---------------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------|
| `id`                        | unsignedBigInteger  | PK (not auto-increment); set by observer: concatenation of version_id + zero-padded random 4-digit suffix (1000–9999) unique within the version, e.g. 421357  |
| `ref`                       | string              | Human-readable display id; set by observer from id, hyphen inserted after version prefix, e.g. "42-1357"; unique                                               |
| `student_id`                | foreignId           | fk                                                                                                                                                             |
| `version_id`                | foreignId           | fk                                                                                                                                                             |
| `school_id`                 | foreignId           | fk                                                                                                                                                             |
| `teacher_id`                | foreignId           | fk                                                                                                                                                             |
| `voice_part_id`             | foreignId           | fk                                                                                                                                                             |
| `status`                    | enum                | eligible, pending, registered, withdrew, teacher_withdrawn, no_show, adjudicated, incomplete, accepted, not_accepted, declined, removed                        |
| `program_name`              | string              | How the candidate wants their name listed in the program; not nullable; defaults to users.name at enrollment                                                   |
| `emergency_contact_id`      | foreignId           | fk                                                                                                                                                             |
| `created_at` / `updated_at` | timestamp           | Laravel timestamps                                                                                                                                             |

**`emergency_contacts` table** — current implementation (student-level, pre-dates this spec):

| Column                      | Type          | Notes                                                                                                                                                                |
|-----------------------------|---------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| `id`                        | bigIncrements | PK                                                                                                                                                                    |
| `student_id`                | foreignId     | FK → students                                                                                                                                                        |
| `name`                      | string        | Emergency contact name                                                                                                                                              |
| `relationship`              | enum          | `EmergencyContactRelationship`: mother, father, step_mother, step_father, grandmother, grandfather, guardian, sibling, aunt, uncle, guardian_mother, guardian_father, foster_mother, foster_father, other |
| `email`                     | string        | Nullable (made nullable 2026-08-12, §9 item 32) — conditionally required per `versions.emergency_contact_email` (§5.1); enforced in `CandidateDetail::saveEmergencyContact()`, not at the column level |
| `cell_phone`                | string(20)    | Nullable (made nullable 2026-08-12, §9 item 32) — normalized on set via `PhoneNormalizer`; conditionally required per `versions.emergency_contact_cell` (§5.1), same enforcement point as `email` |
| `home_phone`                | string(20)    | Nullable; normalized on set via `PhoneNormalizer`; never required by any Version flag                                                                              |
| `work_phone`                | string(20)    | Nullable; normalized on set via `PhoneNormalizer`                                                                                                                   |
| `best_phone`                | enum          | `BestPhone`: cell, home, work — default `cell`                                                                                                                      |
| `created_at` / `updated_at` | timestamp     | Laravel timestamps                                                                                                                                                  |
| `deleted_at`                | timestamp     | Soft delete                                                                                                                                                          |


**`candidate_status_history` table** — current implementation, immutable audit log of every candidate status transition:

| Column         | Type          | Notes                                              |
|----------------|---------------|----------------------------------------------------|
| `id`           | bigIncrements | PK                                                 |
| `candidate_id` | foreignId     | FK → candidates                                    |
| `from_status`  | enum          | nullable — null on initial enrollment              |
| `to_status`    | enum          | the new status                                     |
| `user_id`      | foreignId     | FK → users; who triggered the change               |
| `notes`        | text          | nullable — admin override reason, reconciliation   |
| `created_at`   | timestamp     | when the transition occurred; no `updated_at`      |

**`audition_results` table** — built 2026-08-08, narrower than originally sketched (see §9 item 24): a frozen, one-row-per-candidate tally snapshot only, written once at Version close. `accepted`/`accepted_ensemble_id` are deliberately **not** columns here — that decision belongs on `Candidate` (status + a future `accepted_ensemble_id`) once cut-offs/ensemble assignment (§7.4–§7.5) are built, not duplicated as a second in/out flag on the tally table:

| Column | Type | Notes |
|---|---|---|
| `id` | bigIncrements | PK |
| `candidate_id` | unsignedBigInteger | fk, unique — one row per candidate |
| `version_id` | foreignId | fk |
| `voice_part_id` | foreignId | fk |
| `school_id` | foreignId | fk |
| `voice_part_order_by` | unsignedSmallInt | |
| `score_count` | unsignedSmallInt | |
| `total` | unsignedMediumInt | |
| `created_at` / `updated_at` | timestamp | Laravel timestamps |

While adjudication is in progress (and through any Tab Room override), the comparable total is computed live via `AdjudicationService::candidateTotals()` — an aggregate query in the same style as `candidateStatuses()`/`candidateTolerances()`, not a stored running counter. This table is only ever written once, at the not-yet-built Version-close step (§7.6).

**`candidate_upload_files` table** — designed 2026-07-16, **built 2026-08-12** (§9 item 33); fulfills a `version_upload_files` (§5.2) slot with an actual Candidate-uploaded recording, per the Registration workflow (§6.2):

| Column                       | Type          | Null | Default | Notes                                                                                   |
|-------------------------------|---------------|------|---------|--------------------------------------------------------------------------------------------|
| `id`                           | bigIncrements | no   | —       | PK                                                                                           |
| `candidate_id`                 | foreignId     | no   | —       | FK → candidates; cascade on delete                                                           |
| `version_upload_file_id`       | foreignId     | no   | —       | FK → version_upload_files; cascade on delete — which expected slot ("scales", "solo", …) this fulfills |
| `uploaded_by_user_id`          | foreignId     | yes  | null    | FK → users, null on delete; added 2026-08-13 (§9 item 35) so an approved upload can be synced into `recordings`, whose own `uploaded_by` is NOT NULL |
| `url`                          | string        | no   | —       | S3 object key, `candidateUploads/{candidate_id}/...`, same disk convention as pitch files — served to the browser via a **signed** `temporaryUrl()`, not a plain `->url()` (§9 item 34: this bucket's new objects default to private, and a plain URL 403s silently) |
| `original_filename`            | string        | yes  | null    | Added 2026-08-13 (§9 item 35); the client-submitted filename, shown to the teacher and fed to the filename mis-filed-recording heuristic (`RecordingReviewService`) |
| `duration_seconds`             | unsignedInteger | yes | null   | Added 2026-08-13 (§9 item 35); extracted via `getID3` at upload time, feeds the per-slot duration-outlier heuristic and its own future baseline |
| `flagged_at` / `flag_reason`   | timestamp / text | yes | null | Added 2026-08-13 (§9 item 35); set when either `RecordingReviewService` heuristic fires — non-blocking, informational only, never auto-rejects |
| `status`                       | string        | no   | pending | `CandidateUploadStatus` (§8.12): `pending \| approved` — only two states; a rejection deletes the row (see below), it is never a stored third state |
| `uploaded_at`                  | timestamp     | no   | —       | Set when the Candidate/teacher uploads the file                                              |
| `decided_at`                   | timestamp     | yes  | null    | Set when the teacher approves it                                                             |
| `decided_by_user_id`           | foreignId     | yes  | null    | FK → users; null on delete; the (co-)teacher who approved it                                 |
| `created_at` / `updated_at`    | timestamp     | yes  | null    | Laravel timestamps                                                                            |

Unique on (`candidate_id`, `version_upload_file_id`) — one active upload per Candidate per slot. **Reject deletes, it does not transition to a "rejected" status**: per the source design doc, "the recording is removed from the candidate's record and another recording can be uploaded" — this matches the existing delete-on-remove precedent from Version Pitch Files (§5.5), including deleting the underlying S3 object. Replacing an approved file (teacher re-opens it for re-upload) also reverts `status` to `pending` and clears `decided_at`/`decided_by_user_id`, consistent with `version_pitch_files`' replace-deletes-previous-S3-object behavior. **Synced into `recordings` on approval** (§9 item 35) — see below.

**`recordings` table** — built 2026-08-07 (§9 item 23) for judge-facing Adjudication, originally populated only via CSV import of legacy data with no in-app writer of its own:

| Column                       | Type          | Null | Default | Notes                                                                                   |
|-------------------------------|---------------|------|---------|--------------------------------------------------------------------------------------------|
| `id`                           | bigIncrements | no   | —       | PK                                                                                           |
| `version_id`                   | foreignId     | no   | —       | FK → versions; cascade on delete                                                             |
| `candidate_id`                 | unsignedBigInteger | no | —    | fk (explicit `foreign()`, not `constrained()` — `candidates.id` is a custom PK, not `bigIncrements`) |
| `file_type`                    | string        | no   | —       | Doubles as the score category name (e.g. "scales", "solo") — `AdjudicationService::recordingsForCandidate()` matches it to `score_categories.description` case-insensitively |
| `uploaded_by`                  | foreignId     | no   | —       | FK → users; cascade on delete                                                                |
| `approved_at` / `approved_by`  | timestamp / foreignId | yes | null | Both must be set for `recordingsForCandidate()` to surface the row to a judge             |
| `url`                          | string        | no   | —       | S3 object key                                                                                 |
| `created_at` / `updated_at`    | timestamp     | yes  | null    | Laravel timestamps                                                                            |

**Reconciled with `candidate_upload_files` 2026-08-13 (§9 item 35).** These were two independent, disconnected tables for the same real-world thing — a candidate-submitted audition recording — until a user report ("I approved a recording but don't see it in the `recordings` table") surfaced the gap: `candidate_upload_files` (the registration-time upload/approve/reject gate, §9 item 33) had no path into `recordings` (the only table Adjudication ever reads from), so a recording approved through the new Recordings section was genuinely invisible to a judge. Resolved as a one-way **sync bridge**, not a merge — keeps `recordings`'s already-tested Adjudication code path completely untouched, at the cost of two tables that must stay in step:
- **On approve:** `CandidateDetail::approveRecording()` upserts a `recordings` row keyed on `(version_id, candidate_id, file_type)`, where `file_type` is the `version_upload_files` slot's own `name` — `uploaded_by`/`approved_at`/`approved_by`/`url` copied across.
- **On reject** (a pending upload, or removing a previously-approved one — both go through the same `rejectRecording()`): the synced `recordings` row is deleted too.
- **On replace** (re-uploading over an already-approved file): the stale synced `recordings` row is deleted before the slot reverts to `pending`, so a judge can never score against superseded audio.
- Approving the same upload twice is idempotent (`updateOrCreate`, never a duplicate row).

**Known limitation, not fixed:** `version_upload_files.name` and `score_categories.description` are two independently-configured, same-Version lists that happen to usually align by name (e.g. both have a "Scales" entry) — nothing enforces that they actually do. If an admin names a slot something a Room's rubric categories don't recognize, the sync still writes the `recordings` row, but `recordingsForCandidate()`'s own case-insensitive match against `score_categories.description` simply won't surface it to a judge. This was a deliberate tradeoff, not an oversight — see the product-owner discussion below.

**Registration-gate mapping (§6.2).** "Upload of required audio/video files" is satisfied when the Candidate has a `candidate_upload_files` row (any status) for every `version_upload_files` slot on that Version. "Teacher approval of required audio/video files" is satisfied when every one of those rows has `status = approved`. Both milestone items only apply when the Version's `audition_type` is `remote` (§5.1). **Amber "in progress" rendering, and media-type-aware labels (built 2026-08-13, §9 item 35):** both checklist labels now read "Upload of required audio files" / "video files" (not the generic "audio/video files") when `versions.upload_type` is `Audio`/`Video`. Each item also renders amber, not just green/red, when it's part-satisfied — "some but not all of the expected slots have an upload" for the Upload item, "some but not all of the files actually uploaded so far have been approved" for the Approval item (the denominator is uploads that exist, not the full slot count) — via an optional `partial` closure on the general `HasCandidateChecklist` item shape, consulted only when `check` returns false. Every other checklist item (Program name, Emergency contact, Birthday, etc.) is unaffected — they simply don't define `partial` and keep the original green/red-only rendering.

**Voice-part invariant.** `candidates.voice_part_id` is the single source of truth for a Candidate's voice part — no upload, application, or pitch-file record duplicates or overrides it. This mirrors the rule already in place for §5.7's Candidate Application (`CandidateApplicationData` resolves `voicePartName` from the live Candidate record, never from a stored copy).

### 5.3 Version roles (version-scoped)

Roles are granted relative to the Version via Spatie. The defined roles are:

- Event Manager
- Registration Manager
- Co-Registration Manager
- Web Registration Manager
- Tab Room Manager
- Rehearsal Manager

**Co-Registration Manager delegation (built 2026-08-04).** Assigning Co-Registration Manager is deliberately split out of the generic Configure → Roles tab (Event Manager only) into its own screen, `VersionCoRegistrationManagers` (route `events.versions.co-registration-managers`), reachable by Founder, Event-wide Event Manager, **or a Registration Manager held specifically on that Version** — gated by `VersionRoleAssignmentService::canManageCoRegistrationManagers()`. This lets a Registration Manager build out their own support team without exposing the rest of Configuration (dates, fees, obligations, other roles). Deliberately narrower than `canManageAuditionEnvironment()` (§5.9): a Co-Registration Manager cannot delegate the role further. Every Co-Registration Manager assignment is paired with a set of county responsibilities (`co_registration_manager_counties`: `version_id`, `user_id`, `county_id`), restricted to a subset of the Version's own `version_counties` and mutually exclusive across managers on the same Version (enforced by a unique constraint on `(version_id, county_id)`). `VersionRoleAssignmentService::assignCoRegistrationManager()`/`revokeCoRegistrationManager()` write the role and county rows atomically; the generic `assignRole()`/`revokeRole()` path now rejects `Co-Registration Manager` outright (400) so the role can never be granted without a county assignment.

### 5.4 Version Invitations (Event Manager controls who may participate)

The Event Manager builds and maintains the invitation list for a Version: the set of teachers permitted to enroll Candidates. Confirmed 2026-07-07.

**Eligibility pool (computed, not stored).** A teacher is eligible for a Version if they have at least one active + verified school (`Teacher::hasActiveSchool()`) **and** satisfy at least one of:

- **County leg:** any of the teacher's active + verified schools has a `county_id` present in that Version's `version_counties`. If the Version has **zero** `version_counties` rows, the county leg is unrestricted — it passes for every active+verified teacher (the Version isn't geographically scoped).
- **Membership leg:** the teacher has **any** `Membership` row (regardless of `membership_expires_at`) for the Version's Event's root organization (`Organization::membershipOrganization()`). Expired memberships still satisfy this leg; `membership_expires_at` is displayed for the Event Manager's own judgment, not enforced as a hard gate here.

This mirrors the existing `EligibilityService` pattern (computed pool, not a persisted table) — implement as a new `VersionInvitationEligibilityService`, not by pre-seeding rows.

**`version_invitations` table** — new, this phase:

| Column                | Type          | Null | Default | Notes                                                                                     |
|------------------------|---------------|------|---------|---------------------------------------------------------------------------------------------|
| `id`                   | bigIncrements | no   | —       | PK                                                                                           |
| `version_id`           | foreignId     | no   | —       | FK → versions; cascade on delete                                                             |
| `teacher_id`           | foreignId     | no   | —       | FK → teachers; cascade on delete                                                             |
| `status`               | enum          | no   | invited | `VersionInvitationStatus` (§8.8) — `invited \| obligated \| participating`. No `eligible` case: absence of a row **is** "eligible" (see below). |
| `invited_at`           | timestamp     | no   | —       | Set when the row is created                                                                  |
| `invited_by_user_id`   | foreignId     | no   | —       | FK → users; who performed the invite                                                         |
| `created_at`/`updated_at` | timestamp  | yes  | null    | Laravel timestamps                                                                            |

Unique on (`version_id`, `teacher_id`).

**Status derivation for display.** A teacher's displayed status is: no `version_invitations` row → `eligible`; row exists → the row's `status` column (`invited` by default at creation). `obligated` and `participating` are reserved enum cases — this phase does not set them. They will be written by the not-yet-built registration workflow (a teacher reaches `obligated` by agreeing to Version obligations during registration; `participating` follows from there). Do not scaffold that trigger now.

**Invite / uninvite mechanics.**

- Checking a teacher's checkbox and saving creates a `version_invitations` row (`status = invited`, `invited_at = now()`, `invited_by_user_id = auth()->id()`).
- Unchecking an already-invited teacher deletes their row, reverting them to computed `eligible`.
- **Invite All** creates rows for every currently-listed teacher with no existing row.
- **Remove All** deletes rows for every currently-listed teacher whose status is `invited`.
- **Guard:** attempting to uninvite (individually or via Remove All) a teacher whose row status is `obligated` or `participating` must be blocked with an explanation, not silently skipped or hidden — consistent with this project's guarded-action-visibility convention. This guard has no live trigger yet (those statuses aren't reachable this phase) but must be implemented now so the future registration workflow doesn't have to retrofit it.
- No email/notification is sent on invite in this phase — status is recorded only.

**Roster display.** One row per teacher (not per school). Columns: teacher name, email, school name (county), `membership_expires_at`, status.

- School/county shown: among the teacher's active + verified schools, prefer the first (alphabetical by school name) whose county is in the Version's `version_counties` list; if the teacher qualifies only via the membership leg (no county match), fall back to their first active+verified school alphabetically.
- `membership_expires_at` shown: the latest (`max`) value across the teacher's `Membership` rows for the Version's root organization; blank if the teacher has no membership record (i.e., they qualified via the county leg only).

**Authorization.** Gate the invitation-management screen the same way as `VersionEdit`: `VersionRoleAssignmentService::canManageEvent($user, $version->event)` (Founder or Event Manager on any Version of the Event).

**Not built this phase (flag, don't scaffold):** search/filter/pagination on the roster (revisit if a real organization's teacher roster proves too large for a single page), email notification on invite, and any UI for the `obligated`/`participating` transitions themselves.

### 5.5 Version Pitch Files (Event Manager maintains the audio/reference library)

The Event Manager maintains an ordered, per-voice-part library of pitch files (audio/video/PDF reference material — e.g., scales, solo excerpts, quintet mixes) for a Version. Confirmed 2026-07-09.

**`version_pitch_files` table** — current implementation:

| Column                      | Type              | Null | Default | Notes                                                          |
|------------------------------|-------------------|------|---------|-----------------------------------------------------------------|
| `id`                          | bigIncrements     | no   | —       | PK                                                               |
| `version_id`                  | foreignId         | no   | —       | FK → versions; cascade on delete                                 |
| `voice_part_id`               | foreignId         | no   | —       | FK → voice_parts; cascade on delete                              |
| `name`                        | string            | no   | —       | File label (e.g., "scales", "solo")                              |
| `description`                 | string            | yes  | null    |                                                                   |
| `url`                         | string            | no   | —       | S3 object key (`pitchFiles/{version_id}/...`)                    |
| `order_by`                    | unsignedSmallInt  | no   | 1       | Display order; resequenced on reorder/save-order                 |
| `created_at` / `updated_at`   | timestamp         | yes  | null    | Laravel timestamps                                                |

`Version::pitchFiles()` is a `HasMany` ordered by `order_by`. Uploaded files accept `mp3, wav, m4a, pdf, mp4, mov` (max 50 MB) and are stored on the `s3` disk under `pitchFiles/{version_id}/`; replacing a file on edit deletes the previous S3 object, and removing a row deletes its S3 object too.

**Voice part scoping.** `voice_part_id` must be one of the Version's `availableVoiceParts()` (the union of voice parts across the Event's Ensembles) — a pitch file cannot be assigned to a voice part the Event doesn't use. `order_by` is assigned per-Version (not per-voice-part) as `max(order_by) + 1` on create.

**Roster UI.** `VersionPitchFiles` Livewire component (`/events/versions/{version}/pitch-files`, route `events.versions.pitch-files`) provides: search (name/description), filter by voice part and by file-name type, sortable columns, drag-and-drop reorder (native HTML5 drag-and-drop per [[project_livewire_wire_sort]] — Livewire's `wire:sort` is broken on Flux table rows), and a bulk "save order" fallback via numeric inputs. Add/edit is a modal form; both create and edit go through the same `save()` action.

**Authorization.** Gated identically to `VersionEdit` and `VersionInvitations`: `VersionRoleAssignmentService::canManageEvent($user, $version->event)` (Founder or Event Manager on any Version of the Event) on mount and on every mutating action (`save`, `remove`, `saveOrder`, `reorderPitchFiles`).

**Not built this phase (flag, don't scaffold):** enforcement of `pitch_file_visibility` (§5.1) and the post-close visibility windows described in the bullet above — the column and enum (`PitchFileVisibility`: `both | candidate | teacher`) exist and are configurable on `VersionEdit`, but nothing yet reads them to gate what a teacher or candidate can see. Candidate/teacher-facing display of pitch files (StudentFolder.info side) is not built — this phase is Event Manager CRUD only.

### 5.6 Version Obligations (Event Manager authors, teacher accepts/rejects)

The Event Manager authors a per-Version "Teacher Obligations" document (rich text, previously hand-built per organization and emailed to a developer) and publishes it; an invited teacher views it and accepts or rejects, which feeds the `VersionInvitationStatus::Obligated` case reserved in §8.8. Built 2026-07-09, as the pilot for a broader "Event Manager self-serve content" strategy (Candidate Application and Estimate Form/Invoice are the next two candidates for the same pattern, not yet built).

**`version_obligations` table** — current implementation:

| Column                       | Type          | Null | Default | Notes                                                               |
|-------------------------------|---------------|------|---------|----------------------------------------------------------------------|
| `id`                           | bigIncrements | no   | —       | PK                                                                    |
| `version_id`                   | foreignId     | no   | —       | FK → versions; cascade on delete; **unique** (one row per Version)   |
| `title`                        | string        | yes  | null    | Optional display title                                               |
| `body`                         | longText      | no   | —       | Sanitized HTML (see below); rendered via `{!! !!}`                   |
| `status`                       | string        | no   | draft   | `VersionObligationStatus`: `draft \| published`                      |
| `published_at`                 | timestamp     | yes  | null    | Set when published                                                    |
| `published_by_user_id`         | foreignId     | yes  | null    | FK → users; null on delete                                            |
| `created_at` / `updated_at`    | timestamp     | yes  | null    | Laravel timestamps                                                    |

**`version_obligation_responses` table** — current implementation:

| Column                         | Type          | Null | Default | Notes                                                                 |
|----------------------------------|---------------|------|---------|-------------------------------------------------------------------------|
| `id`                              | bigIncrements | no   | —       | PK                                                                       |
| `version_invitation_id`           | foreignId     | no   | —       | FK → version_invitations; cascade on delete; **unique** (one response per invitation, toggled in place) |
| `version_obligation_id`           | foreignId     | no   | —       | FK → version_obligations; cascade on delete                             |
| `decision`                        | string        | no   | —       | `ObligationDecision`: `accepted \| rejected`                            |
| `decided_at`                      | timestamp     | no   | —       | Updated on every toggle                                                 |
| `obligation_snapshot`             | longText      | no   | —       | Frozen copy of the merged `body` at decision time — the audit record of exactly what the teacher agreed to, independent of later edits |
| `created_at` / `updated_at`       | timestamp     | yes  | null    | Laravel timestamps                                                       |

**Authoring & sanitization.** The Event Manager edits `title`/`body` via a `flux:editor` (TipTap-based) on the `VersionEdit` "Obligations" tab, toolbar restricted to `heading bold italic underline | bullet ordered blockquote | link`. `body` is passed through `mews/purifier`'s `obligations` HTMLPurifier profile (`config/purifier.php`) on every save via `VersionObligationObserver::saving()` — allowlist `p,h1,h2,h3,strong,b,em,i,u,ul,ol,li,blockquote,a[href]`, no `img`/`style`/`class`/`div`/`span`, unsafe URI schemes (e.g. `javascript:`) stripped. This runs regardless of entry point (Livewire, tinker, future tooling), not just the one form.

**Merge fields.** `body` may contain literal tokens `{{versionShortName}}` / `{{versionName}}`, resolved by plain `str_replace` (not Blade evaluation — the Event Manager's content must never be passed through a Blade compile step) at render time on both the admin preview intent and the teacher-facing page, and frozen into `obligation_snapshot` at accept/reject time.

**Publish workflow.** `status` starts `draft`. **Save** persists `title`/`body` without touching `status`. **Publish** persists and sets `status = published`, `published_at = now()`, `published_by_user_id = auth()->id()` in one action. **Unpublish** reverts to `draft` (`wire:confirm`-gated) — existing teacher responses are untouched (their `obligation_snapshot` remains the audit record regardless of later publish state changes). Teachers only ever see a `published` obligation; `draft` is invisible to them.

**Teacher accept/reject.** `App\Livewire\Registrations\VersionObligations` (`/registrations/{version}/obligations`, route `registrations.obligations`) shows the published obligation (merge-resolved) to an invited teacher and lets them Accept or Reject via `updateOrCreate` keyed on the unique `version_invitation_id` — toggling updates the same row rather than accumulating history. `VersionObligationResponseObserver::saved()` flips the parent `VersionInvitation.status` to `VersionInvitationStatus::Obligated` (§8.8, previously reserved and unwritten) when the current decision is `accepted`.

**Rejection is an iron gate (resolved 2026-07-10, was an open question above; widened 2026-08-12, §9 item 32).** On a `rejected` decision, the observer sets `VersionInvitation.status = VersionInvitationStatus::Rejected` (new case) and calls `CandidateService::withdrawAllForTeacherVersion()`, which bulk-withdraws (`CandidateStatus::TeacherWithdrawn`) every candidate that teacher has enrolled for that Version still in an active state (`Eligible`/`Pending`/`Registered`) — full audit trail via the existing `CandidateObserver` history hook. `EligibilityService::isBlockedByRejectedObligations()` remains the service-level defense-in-depth check.

Page-level enforcement was widened 2026-08-12 from "block enrollment, show a callout" to a real access gate: `App\Concerns\GuardsAcceptedObligations` (a new shared trait, applied to both `VersionDashboard::mount()` and `CandidateDetail::mount()`) redirects to `registrations.obligations` whenever the teacher's obligation response for that Version isn't `accepted` — covering both "never responded" and "rejected" identically, since a rejection is treated exactly like no Obligation record existing at all. No other Version-scoped teacher page is reachable until they accept (or re-accept). The persistent red callout that used to live on `VersionDashboard` is gone — there's nothing left to show a callout on, since the teacher never lands there while blocked.

**This is now fully reversible.** Re-accepting flips the invitation back to `Obligated` (unconditional on decision, existing code path) **and** `CandidateService::reinstateAllForTeacherVersion()` (new 2026-08-12) resets every candidate that rejection's cascade withdrew back to `Eligible`, then immediately re-runs `CandidateService::recalculateStatus()` on each — one that was fully checklisted before the rejection lands straight back on `Registered` rather than sitting at `Eligible` until the teacher happens to touch it. Manual re-enrollment is no longer needed. Known limitation, not fixed: nothing distinguishes a candidate the rejection cascade withdrew from one the teacher separately withdrew by hand for an unrelated reason (both share the one `TeacherWithdrawn` status) — re-accepting reinstates all of them.

**Two new teacher-facing affordances (2026-08-12).** An "Obligations" badge (green Accepted / amber Awaiting / red Rejected) was added to each Version card on `Registrations/Index` (`/registrations`) for any Version with a published obligation, linking straight to `registrations.obligations` — the reachable-after-acceptance re-access this section previously lacked. And once a teacher's decision is `accepted`, `VersionObligations`'s own page now shows a "Review Eligible Students" success callout linking to `registrations.version` — closing the "what do I do next?" gap that used to leave a teacher on the obligations page with no forward link.

**Guard activation.** `VersionInvitations::uninvite()` (§5.4) already contained a guard — `in_array($rawStatus, [Obligated, Participating])` blocks removing a teacher who has agreed to obligations — written before this feature existed. This phase is what makes that guard live for the first time.

**Authorization.** Authoring is gated identically to the rest of `VersionEdit` (`VersionRoleAssignmentService::canManageEvent`), checked once at `mount()` per the existing `VersionEdit` convention (not re-checked per action). The teacher-facing page scopes by the authenticated teacher's own `VersionInvitation` row (404 if not invited); no separate role check.

**Not built this phase (flag, don't scaffold):** PDF rendition of the obligations (a disabled placeholder button exists on the teacher page; `barryvdh/laravel-dompdf`'s first real usage ended up being §5.7's Candidate Application, not this feature); an "insert merge field" toolbar button (Event Manager must type the token literally).

---

### 5.7 Candidate Application (Event Manager authors, teacher certifies signatures, real PDF)

The Event Manager authors the per-Version audition/participation application document — the actual form students, parents, and teachers sign — and a teacher certifies signatures per candidate. Built 2026-07-10 as the second "Event Manager self-serve content" feature (see §5.6's intro), extending the pattern to three authored bodies instead of one, per-*candidate* (not per-teacher) state, a much larger merge-token catalog, and this codebase's first real `barryvdh/laravel-dompdf` usage. Scoped from three real legacy templates (`docs/sample_applications/event_id-{1,9,25}_application_pdf.blade.php`).

Two modes already existed in the schema before this feature (`App\Enums\ApplicationType`, `versions.application_type`, defaulting to `Pdf`):
- **`Pdf`** — a printed legal document. Physical ink signatures required from Candidate, Parent, Teacher, and Principal. The Teacher additionally checks **one** box in-app: "I certify these signatures are present, complete, and have integrity."
- **`EApplication`** — digital signatures required from Candidate and Parent, PDF offered only as an optional convenience copy. Because there is no Student-facing portal yet (StudentFolder.info) and no Parent/Guardian account type at all, the **Teacher** checks two boxes on their behalf — "Candidate signed" / "Parent signed" — and certification is simply both being checked (no separate third action). Explicit stopgap: once a Student portal exists, students/parents will do this themselves.

**`version_applications` table** — one row per Version:

| Column                                | Type          | Null | Default | Notes                                                                 |
|----------------------------------------|---------------|------|---------|--------------------------------------------------------------------------|
| `id`                                    | bigIncrements | no   | —       | PK                                                                        |
| `version_id`                            | foreignId     | no   | —       | FK → versions; cascade on delete; **unique** (one row per Version)       |
| `student_endorsement_body`              | longText      | no   | —       | Sanitized HTML, same profile as Obligations                              |
| `parent_endorsement_body`                | longText      | no   | —       | Sanitized HTML                                                            |
| `teacher_principal_endorsement_body`     | longText      | yes  | null    | Sanitized HTML; only used/required in `Pdf` mode — event 25's real `EApplication` template has no such section at all |
| `status`                                | string        | no   | draft   | `VersionApplicationStatus`: `draft \| published`                         |
| `published_at`                          | timestamp     | yes  | null    | Set when published                                                       |
| `published_by_user_id`                  | foreignId     | yes  | null    | FK → users; null on delete                                                |
| `created_at` / `updated_at`             | timestamp     | yes  | null    | Laravel timestamps                                                       |

No `title` column, unlike Obligations — this document has no freeform admin-authored heading; the header is always auto-rendered from live Version/Event/Organization data.

**New `candidates` columns** (not a separate child table — matches the existing precedent of `program_name`/`emergency_contact_id` living directly on `candidates` as per-candidate registration state):

| Column                                | Type       | Null | Notes                                                                 |
|-----------------------------------------|------------|------|--------------------------------------------------------------------------|
| `application_certified_at`               | timestamp  | yes  | `Pdf`-mode teacher attestation                                            |
| `application_certified_by_user_id`        | foreignId  | yes  | FK → users; null on delete; audit of who certified                       |
| `application_candidate_signed_at`         | timestamp  | yes  | `EApplication`-mode                                                       |
| `application_parent_signed_at`            | timestamp  | yes  | `EApplication`-mode                                                       |

No stored "certified" boolean for `EApplication` mode — `Candidate::is_application_certified` is a computed accessor (`Pdf` → `application_certified_at !== null`; `EApplication` → both signed timestamps non-null), never a persisted redundant flag.

**Authoring & sanitization.** A new "Application" tab on `VersionEdit` (inserted after Requirements, before Obligations) offers three `flux:editor` fields — Student Endorsement and Parent/Guardian Endorsement always, Teacher/Principal Endorsement only when `application_type = Pdf`. All three share the exact same restricted `obligations` HTMLPurifier profile as §5.6 (the toolbar/allowlist is identical, so the profile is deliberately shared rather than duplicated) via `VersionApplicationObserver::saving()`, each body gated on its own `isDirty()` check. The header, candidate-summary table, and fee amounts are **never authored** — always rendered live from real Version/Candidate/Fee/Organization data via `App\Support\CandidateApplicationData`.

**Merge-token resolver.** `App\Support\CandidateApplicationData` is a readonly DTO with two named constructors: `fromCandidate()` (real per-candidate PDF generation — name/grade/voice part/school/teacher/emergency-contact/fees all resolved from that candidate's actual data) and `placeholder()` (admin-authoring Preview — real Version-level data plus fixed sample values for anything candidate-personal, e.g. `'Jane A. Sample'`). `toTokenMap()` feeds `VersionApplication::mergeTokens()`, a generalization of `VersionObligation::mergeTokens()`'s plain `str_replace` approach (still never Blade evaluation) across the full token catalog (`candidateFullName`, `voicePartName`, `grade`, `schoolName`, `schoolShortName`, `teacherFullName`, phone numbers, `emergencyContactName`, fee amounts, `versionShortName`/`versionName`).

**Publish workflow.** Identical shape to Obligations: `saveApplication()`/`publishApplication()`/`unpublishApplication()` on `VersionEdit`. `Pdf`-mode Save/Publish additionally requires the Teacher/Principal body (validation fails otherwise); switching a Version to `EApplication` mode nulls out any previously-authored Teacher/Principal text defensively on next save, so stale text can't linger. Draft is invisible on candidate records, same "draft is invisible" rule as Obligations.

**Teacher certification.** `App\Livewire\Registrations\CandidateDetail` gained instant toggle actions (`toggleApplicationCertified` for `Pdf` mode; `toggleApplicationCandidateSigned`/`toggleApplicationParentSigned` for `EApplication` mode, both independent), each flowing through the existing `CandidateService::recalculateStatus()` path exactly like `saveProgramName`/`saveEmergencyContact` already do. `App\Concerns\HasCandidateChecklist` gained matching checklist item(s), gated on the Application being Published (mirrors §5.6's "unpublished document produces no uncompletable item" rule) — "Signatures certified" for `Pdf`; "Candidate signed" + "Parent signed" for `EApplication`. **"View Application" modal, built 2026-08-13 (§9 item 33):** the toggle buttons moved off the page into an "Application" card + modal, matching the card+modal pattern the whole page adopted (§9 item 32) — the modal renders the actual merged document (the same shared `document.blade.php` partial the PDF and admin Preview already used, now also included live inside the modal via real `CandidateApplicationData::fromCandidate()` data) alongside the sign-off checkboxes, so a teacher can read what they're certifying rather than toggling blind. The card itself shows read-only status badges (Certified/Not Certified, or Candidate/Parent Signed independently).

**PDF generation — first real `barryvdh/laravel-dompdf` usage.** A shared, mode-agnostic Blade partial (`resources/views/candidate-application/document.blade.php`) is included by both `resources/views/pdf/candidate-application.blade.php` (the dompdf entry view) and the admin Preview modal, so the two can never drift. `App\Http\Controllers\CandidateApplicationPdfController` (single-action invokable, route `registrations.candidate.application-pdf`, `GET /registrations/{version}/{candidate}/application.pdf`) guards candidate/version match (404), requesting-teacher ownership (403), and a Published Application (404), then resolves real per-candidate merge tokens and streams the PDF download. In `EApplication` mode the download is available as soon as the Application is Published, independent of either signature toggle — a convenience copy, not gated on completion.

**Authorization.** Same as Obligations — authoring gated by `VersionRoleAssignmentService::canManageEvent` at `VersionEdit::mount()`; teacher-facing actions scoped to the authenticated teacher's own candidates (existing `CandidateDetail`/`VersionDashboard` ownership checks, unchanged).

~~**Not built this phase (flag, don't scaffold):** ... verification that `organizations.logo_file_url` / `events.logo_url` are genuinely pre-resolved URLs rather than storage-relative keys (untestable today — all seeded logo columns are blank; first real data will confirm or require a one-line `Storage::disk(...)->url()` wrap) ...~~ **Resolved 2026-08-13, and not the way anticipated (§9 item 34):** `organizations.logo_file_url` is a storage-relative S3 key, as guessed — but the one-line `Storage::disk(...)->url()` wrap alone isn't enough, because this bucket's newer objects default to private and a plain public URL 403s silently. `CandidateApplicationData::resolveLogoUrl()` now uses a signed `temporaryUrl()`, found and fixed via the same live-browser-testing pass that caught the identical bug on Recordings/Pitch Files playback (§9 item 34). **Also fixed the same day (§9 item 34):** `teacherCellPhone`/`studentCellPhone`/`emergencyContactPhone` were being passed through as raw normalized digits (e.g. `2015551234`) instead of formatted — now run through `PhoneNormalizer::format()`, the same helper the Reports module already used, rendering `(201) 555-1234`. A real adjacent bug surfaced by a new test in the same pass: `$emergencyContact->name`/`->preferred_phone` were accessed without a null check, which would fatal-error for any Candidate with no emergency contact on file — fixed. Still not built: real Student/Parent-facing digital signing (StudentFolder.info) — replaces today's teacher-does-it-all stopgap once that portal exists; a student-home-phone merge token (legacy had one, current schema has no home-phone concept for a Student/User — intentionally omitted rather than faked).

---

### 5.8 Version Invitation Requests (teacher-initiated, Event Manager approves/denies)

Designed 2026-07-16; built 2026-07-16. Scoped from a source design doc plus a clarifying-question pass with the product owner. This is the teacher-initiated counterpart to §5.4: instead of (or in addition to) the Event Manager building the invitation list top-down, a teacher who is eligible for a Version but has not been invited can *request* an invitation.

**Relationship to `EventInvitationRequest`.** The codebase already has an unrelated, Event-scoped `EventInvitationRequest` (`event_invitation_requests`, `EventInvitationStatus`: `Pending | Approved | Denied`), created today only during `TeacherOnboardingWizard::requestEventInvitations()`. That model represents "let me into this Event/Organization" during onboarding and is **not reused** for this feature — `version_invitation_requests` is a distinct, Version-scoped table, because eligibility, open/close windows, and the resulting `VersionInvitation` row are all Version-scoped concepts, not Event-scoped ones. The two tables intentionally coexist, mirroring the existing precedent of `VersionInvitation` being kept separate from `EventInvitationRequest` (§9 item 8).

**`version_invitation_requests` table** — current implementation:

| Column                | Type          | Null | Default | Notes                                                                                     |
|------------------------|---------------|------|---------|-----------------------------------------------------------------------------------------------|
| `id`                    | bigIncrements | no   | —       | PK                                                                                              |
| `version_id`            | foreignId     | no   | —       | FK → versions; cascade on delete                                                                |
| `teacher_id`            | foreignId     | no   | —       | FK → teachers; cascade on delete                                                                |
| `status`                | string        | no   | pending | `VersionInvitationRequestStatus` (§8.11): `pending \| approved \| denied`                       |
| `requested_at`          | timestamp     | no   | —       | Set when the row is created, and reset on re-request (see below)                                |
| `decided_at`            | timestamp     | yes  | null    | Set when the Event Manager approves or denies                                                    |
| `decided_by_user_id`    | foreignId     | yes  | null    | FK → users; null on delete; who approved/denied — see the email-attribution note below           |
| `created_at`/`updated_at` | timestamp   | yes  | null    | Laravel timestamps                                                                                |

Unique on (`version_id`, `teacher_id`) — one row per teacher per Version, toggled in place (same "update, don't accumulate" pattern as `version_obligation_responses`, §5.6), not a history table.

**Who can request.** A teacher may request an invitation for a Version only if: they are in `VersionInvitationEligibilityService`'s eligible pool (§5.4 — the same OR logic: active+verified school, and either a county match or any org membership, unrestricted if the Version has zero `version_counties` rows); they do **not** already have a `version_invitations` row (already invited — go straight to Registration); and they either have no `version_invitation_requests` row, or their existing row is `status = denied` (a prior denial does not permanently lock them out — re-requesting is allowed and re-uses the same row, resetting `status = pending`, `requested_at = now()`, `decided_at = null`, `decided_by_user_id = null`). A `pending` request cannot be re-submitted (button becomes "Request Sent" / disabled, not hidden — per this project's guarded-action-visibility convention, §5.4).

**Request mechanics.** On request: `updateOrCreate(['version_id', 'teacher_id'], ['status' => Pending, 'requested_at' => now()])`. An email is sent to the Version's Event Manager(s) (everyone holding the Event Manager role on any Version of the Event, per `VersionRoleAssignmentService`) containing the teacher's bona fides — name, school name, county, email, phone number(s), organization membership number, `membership_expires_at` — the same fields already surfaced on the §5.4 invitation roster.

**Approve/deny via signed one-click email links.** Each Event Manager's copy of the notification email (`VersionInvitationRequestSubmittedMail`, `resources/views/mail/version-invitation-request-submitted.blade.php`) contains personalized "Approve" and "Deny" buttons, built as Laravel **signed routes** (`URL::temporarySignedRoute`, `signed` middleware) — no login required to act, since the recipient may not have an active session. Each recipient's link is generated individually in `App\Livewire\Registrations\RequestInvitation::notifyEventManagers()` and encodes both the request id and *that* Event Manager's own user id (route `version-invitation-requests.approve` / `.deny`, params `{versionInvitationRequest}/{user}`), so `decided_by_user_id` is attributable even without `auth()`, rather than a single shared link for all recipients. Because multiple Event Managers may receive the same notification, `App\Http\Controllers\VersionInvitationRequestController` checks the row's current status *before* acting and renders `version-invitation-requests.already-decided` ("Already handled by {name} on {date}") instead of erroring or double-applying the decision — first-click-wins is a real, handled case, not an edge case dismissed as unreachable.

- **Approve:** `VersionInvitationRequestController::approve()` calls `VersionInvitationRequestService::approve()`, which sets `version_invitation_requests.status = approved`, `decided_at = now()`, `decided_by_user_id = <the clicking EM>`, and creates the actual `version_invitations` row via the same shape as an Event-Manager-initiated invite (§5.4): `status = Invited`, `invited_at = now()`, `invited_by_user_id = <the clicking EM>`. `VersionInvitationRequestApprovedMail` is then sent to the teacher confirming the invitation.
- **Deny:** `VersionInvitationRequestController::deny()` calls `VersionInvitationRequestService::deny()`, setting `status = denied`, `decided_at`, `decided_by_user_id`. No `version_invitations` row is created or touched. **No server-sent email is generated** — per the source design doc, the `version-invitation-requests.denied` landing page offers a `mailto:` link (prefilled to the teacher's email, with a subject/body starter matching the source doc's wording) that the Event Manager can open on their own device to compose a custom reason. The reason is **not** captured or persisted server-side; `version_invitation_requests` has no `denial_reason` column by design.

**Resolved: signed-route expiry window is 7 days** (`now()->addDays(7)` in `RequestInvitation::notifyEventManagers()`), matching the existing precedent set by `StudentClaimMail`'s approve/deny links (`app/Livewire/Students/Index.php`) rather than the `school-email.verify` links' 3/30-day range — this was genuinely undecided at design time and picked at implementation for consistency with the closest existing analog, not re-confirmed with the product owner.

**Authorization.** The request action itself is teacher-self-service (`RequestInvitation::mount()` uses the authenticated teacher; redirects to Registration if already invited, `403`s if outside the eligible pool). The Approve/Deny signed routes bypass normal auth (by design — see above) but `VersionInvitationRequestController::authorizeDecidingUser()` still validates that the encoded `{user}` actually holds the Event Manager role for that Version's Event (`VersionRoleAssignmentService::canManageEvent`) before acting, in case a role was revoked between email-send and click — a tampered signature is rejected by the `signed` middleware itself (403) before the controller runs.

**Not built this phase (flag, don't scaffold):** any in-app "pending requests" admin screen — this phase is email-driven only, no dashboard of open requests; a re-request cooldown or rate limit (a teacher could theoretically request, get denied, and immediately re-request — no guard against this yet, left to the Event Manager's own judgment); the `registrations/{version}` (Registration) page itself doesn't yet filter its version listing by invitation/eligibility status (§6.2's access rules describe the intended nav behavior, but `Registrations/Index.php` and the top-level Sidebar in `app.blade.php` haven't been rewired to consult `VersionInvitationEligibilityService`/`version_invitations` yet — flagged as a follow-up, not done as part of this pass to avoid touching already-tested, unrelated code without sign-off).

**Test coverage.** `tests/Feature/Services/VersionInvitationRequestServiceTest.php` (12 tests: `canRequest`/`request`/`approve`/`deny`, including the denied-row-resets-to-pending and already-decided-throws cases), `tests/Feature/Livewire/Registrations/RequestInvitationTest.php` (6 tests: 403 outside the pool, redirect when already invited, request creates a row and emails each Event Manager, disabled "Request Sent" state, re-request after denial), and `tests/Feature/VersionInvitationRequestControllerTest.php` (6 tests: approve creates the invitation and emails the teacher, second Event Manager sees "Already handled" with no double-invite/double-email, deny leaves no invitation and offers the `mailto:` link, 403 for a non-Event-Manager `{user}`, tampered signature rejected).

---

### 5.9 Audition Environment (Room, Judge assignment, Scoring rubric — Version configuration)

Designed and built 2026-07-29. Scoped from a source design doc (`Audition_environment.docx`) plus a clarifying-question pass with the product owner. Configures the rooms, judge assignments, and scoring rubric a Version uses during adjudication — the setup half of the not-yet-built Adjudication Wizard (§7.2). Only Configuration-phase CRUD and the `scores` table schema are in scope this pass; live judge scoring UI, Tab Room, and cutoff/ensemble-assignment logic remain §7 reference-only.

**Room applies identically regardless of `audition_type`.** Resolved 2026-07-29 (an open question during design): the only difference between an in-person and a remote audition is whether the Candidate is physically present — a "blind" in-person audition (judges facing away) is functionally indistinguishable from a remote one to the judge. Room, Score Category/Factor assignment, and Judge assignment are therefore a single shared model for both modes; `audition_type` (§5.1) does not branch this domain at all.

**`version_scorings` dropped.** The source doc described `version_scorings` (flat `file_type`/`segment` columns) as "an alternative to using score_categories and score_factors." Resolved: not built. It offered no technical or performance benefit at this data volume — a rubric is a few dozen admin-edited rows, not a high-read table — and would have duplicated the category label across every factor row with no FK integrity, trading away the Event/Version override model for a join-avoidance that doesn't matter here. Denormalization is reserved for the genuinely high-cardinality, high-read `scores` table below, matching the existing `audition_results`/`candidate_status_history` precedent.

**`score_categories` table** — new, this phase:

| Column | Type | Null | Default | Notes |
|---|---|---|---|---|
| `id` | bigIncrements | no | — | PK |
| `event_id` | foreignId | no | — | FK → events; cascade on delete |
| `version_id` | foreignId | yes | null | FK → versions; cascade on delete; nullable — see resolution rule below |
| `description` | string | no | — | |
| `order_by` | unsignedTinyInt | no | 1 | Display/report order |
| `created_at` / `updated_at` | timestamp | yes | null | Laravel timestamps |

**`score_factors` table** — new, this phase:

| Column | Type | Null | Default | Notes |
|---|---|---|---|---|
| `id` | bigIncrements | no | — | PK |
| `event_id` | foreignId | no | — | FK → events; cascade on delete |
| `version_id` | foreignId | yes | null | FK → versions; cascade on delete; nullable — see resolution rule below |
| `score_category_id` | foreignId | no | — | FK → score_categories; cascade on delete |
| `description` | string | no | — | |
| `abbreviation` | string | no | — | Used in tight-spacing tables/reports |
| `best` | unsignedTinyInt | no | — | Best possible score for this factor |
| `worst` | unsignedTinyInt | no | — | Worst possible score for this factor |
| `interval_by` | unsignedTinyInt | no | 1 | Non-consecutive scoring step (e.g. 3 → 3,6,9,12) |
| `multiplier` | unsignedTinyInt | no | 1 | |
| `tolerance` | unsignedTinyInt | yes | null | Future-proofing only — tolerance is currently enforced at the Room level (§5.2 precedent), not per-factor |
| `order_by` | unsignedTinyInt | no | 1 | Display/report order |
| `created_at` / `updated_at` | timestamp | yes | null | Laravel timestamps |

**Event/Version resolution rule.** A Version with zero `score_categories` rows of its own inherits the Event's full rubric (`version_id IS NULL` rows); a Version with one or more of its own rows uses only its own set — all-or-nothing, not a per-row merge. Same "zero rows means unrestricted/fallback" convention already used for `version_counties` (§5.4) and `event_grades` (§6.2). Implement as `Version::availableScoreCategories()` / `availableScoreFactors()`. **Not built this phase (flag):** the authoring UI for creating a Version-specific override; the data model supports it, but only the Event-level rubric screen ships this pass.

**`version_rooms` table** — new, this phase:

| Column | Type | Null | Default | Notes |
|---|---|---|---|---|
| `id` | bigIncrements | no | — | PK |
| `version_id` | foreignId | no | — | FK → versions; cascade on delete |
| `name` | string | no | — | e.g. "Soprano I" |
| `tolerance` | unsignedTinyInt | yes | null | Acceptable point spread between judge totals; null = not applied, zero = scores must be identical |
| `order_by` | unsignedTinyInt | no | 1 | Display order |
| `created_at` / `updated_at` | timestamp | yes | null | Laravel timestamps |

**`room_score_categories` pivot** — `room_id` (FK → version_rooms, cascade), `score_category_id` (FK → score_categories, cascade). A Room's scoring rubric is the union of its assigned categories' factors — no per-factor cherry-picking.

**`room_voice_parts` pivot** — `room_id` (FK → version_rooms, cascade), `voice_part_id` (FK → voice_parts, cascade). `VersionRoom::voiceParts()` ordered by `voice_parts.sort_order`, same rule as `Ensemble::voiceParts()` (§4.4).

**`room_judges` table** — new, this phase. Renamed from the source doc's "Judge" — this row is an assignment (person + role + room + version), not a person entity; the person is the existing `User` model:

| Column | Type | Null | Default | Notes |
|---|---|---|---|---|
| `id` | bigIncrements | no | — | PK |
| `version_id` | foreignId | no | — | FK → versions; cascade on delete; denormalized alongside `room_id` for direct query without a join, matching the `audition_results` precedent (§5.2) |
| `room_id` | foreignId | no | — | FK → version_rooms; cascade on delete |
| `user_id` | foreignId | no | — | FK → users; cascade on delete — any User, not scoped to Teacher, since some judges are retired teachers or external judges who may never hold a Teacher role |
| `judge_type` | enum | no | — | `JudgeType` (§8.13) |
| `status` | enum | no | assigned | `JudgeStatus` (§8.14); default `Assigned` at creation — day-of transitions belong to the not-yet-built Adjudication phase |
| `created_at` / `updated_at` | timestamp | yes | null | Laravel timestamps |

**Judge type is a single flexible set, not gated by `audition_type`.** Resolved 2026-07-29 (an open question during design): since Room applies identically to in-person and remote auditions, there's no structural reason to enforce two separate judge-type sets by modality. The source doc's two label sets (in-person: head judge/judge2/judge3/judge4/judge monitor/monitor; remote: lead judge/judge1/judge2/judge3) more likely reflect different sponsoring organizations' own vocabulary (§7.8 already flags NJMEA/MACDA/CJMEA as having different rules) than a real in-person/remote rule. `JudgeType` merges both vocabularies into one enum with no runtime validation against `audition_type`; if organization-specific label needs emerge later, this can move to Event-configurable data (like `score_categories`) instead of a hardcoded enum.

**`scores` table** — new, this phase, schema/model only (no entry UI — live scoring remains §7.2 reference-only). Denormalized to avoid highly-joined queries at report time, matching the existing `audition_results`/`candidate_status_history` denormalization precedent:

| Column | Type | Null | Default | Notes |
|---|---|---|---|---|
| `id` | bigIncrements | no | — | PK |
| `version_id` | foreignId | no | — | FK → versions |
| `candidate_id` | foreignId | no | — | FK → candidates |
| `student_id` | foreignId | no | — | FK → students; frozen at scoring time — same freeze-at-a-point-in-time pattern as `obligation_snapshot` (§5.6), not a live-state duplicate |
| `school_id` | foreignId | no | — | FK → schools; frozen at scoring time |
| `score_category_id` | foreignId | no | — | FK → score_categories |
| `score_category_order_by` | unsignedTinyInt | no | — | Snapshot of `order_by` at scoring time, so later rubric reordering doesn't reshuffle historical reports |
| `score_factor_id` | foreignId | no | — | FK → score_factors |
| `score_factor_order_by` | unsignedTinyInt | no | — | Snapshot, same rationale |
| `judge_id` | foreignId | no | — | FK → room_judges |
| `judge_order_by` | unsignedTinyInt | no | — | Snapshot, same rationale |
| `voice_part_id` | foreignId | no | — | FK → voice_parts; frozen at scoring time — `candidates.voice_part_id` remains the live source of truth (§5.2 invariant); this is the historical record of what part the Candidate was actually judged in |
| `voice_part_order_by` | unsignedSmallInt | no | — | Snapshot, same rationale |
| `score` | unsignedTinyInt | no | — | The recorded score value |
| `created_at` / `updated_at` | timestamp | yes | null | Laravel timestamps |

**Implementation.** `VersionRooms` Livewire component (`/events/versions/{version}/rooms`, route `events.versions.rooms`), reachable from a new "Rooms" button on the Event show page alongside Invitations/Pitch Files. Add/edit is a single modal: room name/tolerance, score-category checkboxes (scoped to `availableScoreCategories()`), voice-part checkboxes (scoped to `availableVoiceParts()`), and an inline judges editor (add/remove rows of email + `JudgeType`, resolved to an existing `User` by email at save time — any User, not scoped to Teacher, per §5.9's "retired teachers/external judges" note). Saving a room fully syncs its `room_score_categories`/`room_voice_parts`/`room_judges` rows to whatever was submitted (removed rows are deleted, not left orphaned) rather than only ever adding. Drag-and-drop reorder is native HTML5, same as `VersionPitchFiles` (`wire:sort` is broken on Flux table rows in this codebase). Gated identically to `VersionEdit`/`VersionPitchFiles`/`VersionInvitations`: `VersionRoleAssignmentService::canManageEvent`. Test coverage: `ScoreCategoryResolutionTest.php` (7 tests: Event/Version rubric inheritance and the all-or-nothing override), `VersionRoomsTest.php` (16 tests: authorization, CRUD, category/voice-part sync, judge-row create/update-in-place/remove, reorder), `ScoreTest.php` (2 tests: every `Score` relation resolves, cascade-delete from `Candidate`), plus 2 new `EnumsTest.php` cases. 27 new tests; 651/651 passing app-wide, PHPStan clean.

**Audition-environment access control (designed and built 2026-07-29).** Resolved: access to every audition-environment configuration screen (Rooms, and the Scoring Rubric screen below) is Event Manager or Registration Manager/Co-Registration Manager — not the broader `canManageEvent` gate `VersionRooms` shipped with initially (that gate is Founder + Event Manager only). New `VersionRoleAssignmentService::canManageAuditionEnvironment(User $user, Version $version): bool`: Founder or Event-wide Event Manager (existing `canManageEvent`), **or** Registration Manager/Co-Registration Manager held specifically on that Version — per-Version, not Event-wide, matching how every role except Event Manager already works in this service (Event Manager is the one deliberate exception, since it's the role that bootstraps a brand-new Version before any roles exist on it — see the service's own class-level docblock). `VersionRooms` is retrofitted to this check in place of `canManageEvent`.

**`VersionScoringRubric` screen (designed and built 2026-07-29).** New Livewire component, route `events.versions.scoring-rubric` (`/events/versions/{version}/scoring-rubric`), gated by `canManageAuditionEnvironment`. Two-level nested CRUD (categories containing factors) over `score_categories`/`score_factors`, which were schema-only with no authoring UI until now. Inherit-vs-customize mode driven by `$version->scoreCategories()->exists()`:

- **Inheriting** (no Version-owned rows): edits the Event's default rows (`version_id IS NULL`) directly — every Version inheriting the default sees the change. A "Customize for this Version" action forks: duplicates every Event-default category and its factors into new Version-owned rows (same field values), after which the screen edits those instead.
- **Customized** (Version has its own rows): edits only the Version-owned rows. A "Revert to Event default" action (`wire:confirm`-gated) deletes the Version-owned category rows; factor rows cascade-delete via the existing FK.

Ordering is numeric `order_by` inputs + "Save Order" per level (categories, and factors within each category) — not full drag-and-drop; two nested DnD levels was judged more complexity than this pass needs, flagged in case that's wrong. Nav: a "Scoring Rubric" button (fuchsia) added next to Rooms (teal) on the Event show page, both gated by the new per-Version `versionAuditionAccess` map computed in `Events\Show::render()` — distinct from the `canManageEvent`-gated Invitations/Pitch Files buttons alongside them.

**Test coverage.** `VersionRoleAssignmentServiceTest.php` gained 6 tests for `canManageAuditionEnvironment` (Registration Manager and Co-Registration Manager pass per-Version, sibling-Version and unrelated-role cases fail, Event Manager sibling-version fallback and Founder still pass). `VersionRoomsTest.php` gained 3 tests confirming the retrofit (Registration Manager and Co-Registration Manager now pass, an unrelated role like Tab Room Manager still 403s). `VersionScoringRubricTest.php` (new file, 21 tests): authorization for all five role cases, category/factor CRUD including the cross-Version `activeCategory()`/`activeFactor()` ownership guard (a category from a different Event 404s via `ModelNotFoundException`, asserted with `toThrow` rather than `assertStatus` — a mid-action `findOrFail` doesn't convert to an HTTP response inside `Livewire::test()->call()` the way a mount-time 403/404 does), fork-on-customize (verifies the Event default is untouched and the Version-owned copy edits independently afterward), revert-to-default (cascade delete, mode flips back), and the 409 guards on calling customize/revert out of turn. 30 new tests; 681/681 passing app-wide, PHPStan clean.

**Not built this phase (flag, don't scaffold):** live judge-facing scoring entry UI, Tab Room, `AdjudicationStatus`, `CutoffStrategy` — unchanged from §7; `JudgeStatus` day-of transition UI beyond the `Assigned` default.

---

### 5.10 Registration Manager Reporting Module (Registration/Co-Registration Manager reports & status updates)

Designed and built 2026-08-04; four of the five roster-style reports (Participating Candidates, Participating Schools, Participating Teachers, Payment Roster — not Obligated Teachers) gained pagination on 2026-08-06. Scoped from a source design doc (`Registration Manager Reporting Module.docx`, copied verbatim to `docs/registration-manager-reporting-module.md`) plus a clarifying-question pass with the product owner. Gives the Registration Manager — and, per the resolved access decision below, the Co-Registration Manager, Event Manager, and Founder — nine reports over registered Candidates, their Teachers, and Schools, plus the two workflow actions the source doc bundles into these reports: packet-received tracking with a batch confirmation email, and manual payment recording. The source doc's opening sentence ("The Registration Manager must have access to update the registration status of schools, teachers, students, and candidates") is fully satisfied by these report screens themselves, not a separate status-editing surface: packet-received is a school/teacher-level status toggle inside Participating Schools, and Candidate status changes (`withdrew`/`teacher_withdrawn`) happen via the Edit/Remove actions inside Participating Candidates.

**Foundational scoping rule.** Every report is scoped to `*.version_id` (the Version being managed) and to the acting user's county access — `VersionRoleAssignmentService::reportCountyIds(User, Version): ?array` returns `null` (unrestricted) for Founder, Event-wide Event Manager, or a Registration Manager held on that Version; it returns the specific `county_id` list from `co_registration_manager_counties` (§5.3) for a Co-Registration Manager. Every report's base query joins to `schools.county_id` and applies this scope. `VersionRoleAssignmentService::canManageReports()` mirrors `canManageAuditionEnvironment()` (§5.9) exactly: Founder, Event-wide Event Manager, or Registration Manager/Co-Registration Manager held specifically on that Version.

**Access & navigation (resolved — reports gate matches the audition-environment gate, not a narrower one).** A Registration Manager (or Event Manager/Founder) sees all counties with an optional county filter on each report; a Co-Registration Manager is hard-scoped to only their assigned counties with no filter control needed. New "Reports" button on the Event `Show` page, alongside Invitations/Pitch Files/Rooms/Scoring Rubric/Co-Registration Managers, opening a `Reports/Index` hub Livewire page (route `events.versions.reports`) that links to each of the nine reports below.

**New enum — `PaymentType`:** `Check | PurchaseOrder | Cash | Other | Electronic`. Shared by both new payment tables below. **`Electronic` is reserved, not written this phase** on `teacher_payments` — the Participating Schools payment modal only ever writes `Check | PurchaseOrder | Cash | Other`; a teacher-initiated e-payment needs the not-yet-built `epayment_credentials` (§5.2) integration. `candidate_payments` rows are conceptually always `Electronic` (a Candidate/Student has no other payment channel); ~~this table is schema/report-only this phase — no writer exists yet, blocked on the same missing gateway integration~~ — **it gained its first writer 2026-08-13 (§9 item 33), and it's a deliberately manual one, not a real gateway integration**: `CandidateDetail`'s Payment section lets a teacher record a payment by hand (amount, date, reference, comments), clearly labeled in the UI as a manual ledger entry, not a live charge. A genuine candidate-facing e-payment flow — the thing `payment_type = Electronic` was actually meant to represent — is still blocked on `epayment_credentials` and remains unbuilt.

**`teacher_payments` table** — new, this phase:

| Column | Type | Null | Default | Notes |
|---|---|---|---|---|
| `id` | bigIncrements | no | — | PK |
| `version_id` | foreignId | no | — | FK → versions; cascade on delete |
| `school_id` | foreignId | no | — | FK → schools; cascade on delete — a payment is recorded against a specific school/teacher pairing, opened from that row on Participating Schools |
| `teacher_id` | foreignId | no | — | FK → teachers; cascade on delete |
| `payment_type` | enum | no | — | `PaymentType`; modal only ever writes `Check\|PurchaseOrder\|Cash\|Other` — see above |
| `amount` | unsignedInteger | no | — | Cents, matching the `version_fees` convention |
| `reference_number` | string | yes | null | Check or transaction number |
| `comments` | text | yes | null | |
| `recorded_by_user_id` | foreignId | yes | null | FK → users; null on delete — who entered the payment via the modal |
| `created_at` / `updated_at` | timestamp | yes | null | Laravel timestamps |

**`candidate_payments` table** — new this phase; manual writer added 2026-08-13, see above (§9 item 33):

| Column | Type | Null | Default | Notes |
|---|---|---|---|---|
| `id` | bigIncrements | no | — | PK |
| `version_id` | foreignId | no | — | FK → versions; cascade on delete — denormalized alongside `candidate_id`, matching the `room_judges`/`scores` precedent (§5.9) |
| `candidate_id` | foreignId | no | — | FK → candidates; cascade on delete |
| `payment_type` | enum | no | Electronic | `PaymentType` |
| `amount` | unsignedInteger | no | — | Cents |
| `reference_number` | string | yes | null | Transaction number |
| `comments` | text | yes | null | |
| `paid_at` | timestamp | no | — | |
| `created_at` / `updated_at` | timestamp | yes | null | Laravel timestamps |

**Payment Roster is a UNION, not a shared table** (resolved via clarifying question — two tables rather than one polymorphic-payer table, keeping each table's FK integrity simple and matching the source doc's literal "recorded in separate tables" instruction). The report query unions both tables, populating "Candidate name" only for `candidate_payments` rows; "Teacher name"/"School name" resolve for both (a `candidate_payments` row reaches them via `candidate.teacher`/`candidate.school`).

**`version_teacher_packets` table** — new, this phase, backs the Participating Schools "packet received" checkbox and its batch confirmation email:

| Column | Type | Null | Default | Notes |
|---|---|---|---|---|
| `id` | bigIncrements | no | — | PK |
| `version_id` | foreignId | no | — | FK → versions; cascade on delete |
| `school_id` | foreignId | no | — | FK → schools; cascade on delete |
| `teacher_id` | foreignId | no | — | FK → teachers; cascade on delete |
| `received_at` | timestamp | yes | null | Set when the checkbox is checked; null = not yet received |
| `received_by_user_id` | foreignId | yes | null | FK → users; null on delete |
| `confirmation_sent_at` | timestamp | yes | null | Set when the batch "Send Confirmations" action includes this row |
| `confirmation_sent_by_user_id` | foreignId | yes | null | FK → users; null on delete — who clicked "Send Confirmations"; added per product-owner request so the batch send is attributable like every other action in this module |
| `created_at` / `updated_at` | timestamp | yes | null | Laravel timestamps |

Unique on (`version_id`, `school_id`, `teacher_id`). The row is `updateOrCreate`'d the moment the checkbox is toggled (same "update in place, don't accumulate" pattern as `version_obligation_responses`/`version_invitation_requests`). Unchecking clears `received_at`/`received_by_user_id` but does **not** clear `confirmation_sent_at` — a confirmation already sent stays sent; re-checking after an uncheck does not re-trigger a second email unless "Send Confirmations" is clicked again with that row newly eligible (`received_at` set, `confirmation_sent_at` null).

**Batch confirmation email (resolved — manual trigger, not a scheduled job).** A "Send Confirmations" button on the Participating Schools report sends `PacketReceivedMail` to every teacher (`users.email`) whose `version_teacher_packets` row currently has `received_at` set and `confirmation_sent_at` null, in one batch action, then stamps `confirmation_sent_at = now()`, `confirmation_sent_by_user_id = auth()->id()` on all of them. No scheduled job — the Registration Manager controls when the batch goes out, avoiding a cron job to build/maintain for what's fundamentally a manual, occasional action.

**Fees due (resolved).** Participating Schools' "Total amount of registration fees due" = `count(registered Candidates for that school in this Version) × version_fees.registration`, evaluated live against the Version's *current* fee value — not frozen/snapshotted per candidate. Balance due = due − (`sum(teacher_payments.amount)` + `sum(candidate_payments.amount)`) for that school/teacher's candidates, badge-rendered as payment-due (positive) / over-payment (negative) / no-balance-due (zero), per the source doc.

**The nine reports.** Each is its own Livewire component under `App\Livewire\Events\Reports`, route prefix `events.versions.reports.*`. The five roster-style reports (Obligated Teachers, Participating Teachers, Participating Schools, Payment Roster, Participating Candidates) follow the established `VersionPitchFiles`/`VersionDashboard` convention: load an eager-loaded collection once, filter/sort in PHP via `#[Url]`-bound search/filter properties and a toggleable `sortBy()`. The two count reports (Participation by County, Candidate Counts) are DB aggregate queries instead — grouped/left-joined counts, not collection filtering, since "include counties/rows with no counts" (below) requires a base list independent of the fact rows.

1. **Obligated Teachers** (`ObligatedTeachers`) — teachers with an `accepted` `version_obligation_responses` row (via `version_invitations.teacher_id`, `ObligationDecision::Accepted` per `VersionObligationResponse::isAccepted()`). Columns: teacher name, phone(s) (`users.cell_phone` plus any `phones` rows), email, `decided_at` (obligations accepted date), school name, `membership_expires_at` (reuse `VersionInvitationEligibilityService`'s max-across-memberships pattern, §5.4). Search: first/last/school name. Sort: last name, school name. Export: PDF.
2. **Participating Teachers** (`ParticipatingTeachers`) — distinct teachers with ≥1 `Candidate` at `status = Registered` for the Version. Columns: teacher name, phone(s), email, school name, registered-candidate count. Search/sort/export same shape as above.
3. **Participating Schools** (`ParticipatingSchools`) — distinct (school, teacher) pairs with ≥1 registered Candidate. Columns: school, teacher, phone(s), email, packet-received checkbox (`version_teacher_packets`), registered count, fees due/paid/balance (badge), a "Payment" button opening the payment modal (`payment_type`/`amount`/`reference_number`/`comments` → `teacher_payments`). Search/sort as above. Filter: packets outstanding, balance-due state. Export: PDF, filter-dependent. Also hosts the "Send Confirmations" batch action described above.
4. **Payment Roster** (`PaymentRoster`) — union of `teacher_payments` and `candidate_payments` for the Version. Columns per source doc. Search: school/teacher/candidate name. Sort: same three. Filter: school name, payment type. Export: PDF, filter-dependent.
5. **Participating Candidates** (`ParticipatingCandidates`) — Candidates at `status = Registered`. Columns: school, teacher, phone(s), email, candidate name, grade (`Student::grade` accessor, §6.2), voice part, Edit button. Edit modal: candidate name (first/middle/last — `users.first_name`/`middle_name`/`last_name`, via `candidate.student.user`), voice part (from `Version::availableVoiceParts()`), home phone (**a `phones` row, `type = PhoneType::Home`, keyed to `candidate.student.user_id`** — reuses the existing but lightly-used `Phone`/`PhoneType` convention rather than adding a new column, per the product owner's correction during scoping), cell phone (`users.cell_phone`), Emergency Contact (select from `candidate.student.emergencyContacts()`, assigns `candidate.emergency_contact_id`). Remove button transitions `CandidateStatus` to `Withdrew` or `TeacherWithdrawn` (Registration-Manager-initiated, recorded via the existing `CandidateObserver` status-history hook). Search/sort: school/teacher/candidate name. Filter: school, grade, voice part. Export: PDF, filter-dependent.
6. **Participation by County** (`ParticipationByCounty`) — one row per county in the acting user's scope (all counties, or the Co-Registration Manager's assigned subset), **including counties with zero candidates** — left-joined counts, not an inner join. Columns: county name, obligated-teacher count, participating-teacher count, candidate count, county Registration/Co-Registration Manager name (from `co_registration_manager_counties`, falling back to the Version's Registration Manager(s) for a county with no Co-Registration Manager assigned). No search/sort/filter. Export: PDF and CSV.
7. **Candidate Counts** (`CandidateCounts`) — one row per (school, teacher); columns are school, teacher, phone(s), email, one count column per Version voice part (`Version::availableVoiceParts()`), and a total that must equal the per-voice-part sum. Header: Version-wide summary count of registered Candidates per voice part. Search: school/teacher name. Sort: school, teacher, total. No filters. Export: PDF and CSV.
8. **Adjudication Backup** (`AdjudicationBackup`) — **placeholder shell (resolved via clarifying question).** Real nav entry, room list, and Paper/CSV/Checklist buttons exist and are clickable, but since no room↔Candidate assignment exists yet (confirmed gap — `version_rooms` links to voice parts and judges, never a specific Candidate) and no in-person audition is scheduled this season, the generated PDF/CSV content is a clearly-labeled placeholder (sample rows, "No in-person audition scheduled for the current season") rather than wired to real per-candidate data — matching the source doc's own instruction to populate placeholders "for the current time."
9. **Registration Cards** (`RegistrationCards`) — same placeholder treatment: a real "Print Registration Cards" link and the Candidate id/school/voice-part filter controls exist, but the PDF is placeholder content until room↔Candidate assignment is designed (§7 territory).

**PDF/CSV export mechanics.** PDF follows the existing `barryvdh/laravel-dompdf` precedent (`CandidateApplicationPdfController`, `VersionRoomRosterPdfController`, §5.7/§5.9) — a new single-action controller per report, re-applying the same search/filter query-string state the Livewire component used, so "export, filter dependent" actually reflects what's on screen rather than the whole table. CSV is **new to this codebase** (confirmed: no prior `fputcsv`/`text/csv` usage anywhere in `app/`) — implemented as a `StreamedResponse` + `fputcsv`, first used by Participation by County and Candidate Counts, the only two reports requiring it.

**Not built this phase (flag, don't scaffold):** ~~the `candidate_payments` writer~~ — a manual one exists now (§9 item 33; still not a real candidate-facing e-payment flow, still blocked on `epayment_credentials`, §5.2); the `teacher_payments.payment_type = Electronic` writer (same blocker, teacher side); real room↔Candidate assignment (blocks true Adjudication Backup/Registration Cards content — §7 territory); a re-send/undo path for an already-sent batch confirmation email.

---

### 5.11 Web Registration Manager Module (impersonate a teacher, transfer students between teachers)

Designed and built 2026-08-05. Scoped from a source design doc (`Web Registration Manager Module.docx`, copied verbatim to `docs/web-registration-manager-module.md`, including a description of its one embedded legacy-UI screenshot) plus a clarifying-question pass with the product owner. Gives the "Web Registration Manager" role — defined in `VersionRoleAssignmentService::VERSION_SCOPED_ROLES` since the roles list was first drafted, but until now a name with no gated capability of its own (it only counted toward the generic "holds *some* version-scoped role" checks) — two real functions, both scoped to a single Version and to teachers invited to that Version: impersonating a teacher, and transferring students (and their current-cycle Candidate records) from one teacher to another.

**Role assignment — unchanged.** Resolved: Web Registration Manager continues to be granted exactly as it already is today — the generic Configure → Roles tab, Event Manager/Founder only, via the existing `assignRole()`/`revokeRole()` (it's already a member of `GENERAL_ROLES_TAB_ROLES`). No dedicated delegation screen (unlike Co-Registration Manager, §5.3) — the product owner confirmed this role doesn't need Registration-Manager-level self-service assignment.

**New authorization gate.** `VersionRoleAssignmentService::canManageWebRegistration(User $user, Version $version): bool` — Founder or Event-wide Event Manager (`canManageEvent`), **or** "Web Registration Manager" held specifically on this Version. Deliberately excludes Registration Manager/Co-Registration Manager (unlike `canManageAuditionEnvironment`/`canManageReports`) — the source doc names only this one role, and the product owner didn't extend it. Same per-version `withVersion(...)->unsetRelation('roles')->hasRole(...)` shape as every other per-version gate in this service.

**Teacher pool for both functions.** "Invited" (per the source doc's own wording, for both bullets) means holding a `version_invitations` row for this Version, **any status** — the same loose reading already used elsewhere (e.g. §6.2's "Open for Registration" bucket, §5.8's request-eligibility checks). New `VersionInvitationEligibilityService::invitedTeachers(Version): Collection<int, Teacher>`, reused by both the Impersonate picker and the Transfer From/To pickers.

**Nav.** A new "Web Registration" button on the Event `Show` page, alongside Invitations/Pitch Files/Rooms/Scoring Rubric/Co-Registration Managers/Reports, gated by `canManageWebRegistration`, opening `App\Livewire\Events\WebRegistration` (route `events.versions.web-registration`, `/events/versions/{version}/web-registration`) — a single screen with two tabs, "Impersonate Teacher" and "Transfer Students", matching the source doc's own framing of these as one module's two functions rather than two separate screens.

**Impersonate Teacher tab.** Extends the existing Founder-only impersonation mechanism (`app/Livewire/Founder/Impersonate.php`, `App\Http\Controllers\StopImpersonatingController`, session key `impersonator_id`) rather than building a parallel one:

- Teacher search/list scoped to `invitedTeachers($version)`, same name-search pattern as `Founder\Impersonate::teachers()`.
- `impersonate(int $userId)` validates the target holds a `version_invitations` row for this Version (403 otherwise — defense-in-depth beyond the picker already being filtered), then sets three session keys: the existing `impersonator_id` (unchanged meaning — the real acting user's id, so `StopImpersonatingController` can log them back in generically, exactly as it already does), plus two new keys, `impersonation_scope = 'web_registration_manager'` and `impersonation_version_id = $version->id`. Redirects straight into that Version's Registrations dashboard (`registrations.dashboard`-equivalent route) as the impersonated teacher — not the generic `dashboard` Founder's flow redirects to, since the whole point is version-scoped registration work, not general account browsing.
- **Enforcement — new middleware, `App\Http\Middleware\RestrictWebRegistrationImpersonation`**, registered in the `web` group (a cheap two-key session check on every request, so it can't be bypassed by navigating around Livewire). While `impersonation_scope === 'web_registration_manager'` is set: (1) any `registrations.*` route whose bound `{version}` doesn't match `impersonation_version_id` is blocked (enforces "Limited by version_id access" for the whole session, not just the entry point); (2) the teacher account/profile routes are blocked outright (enforces the source doc's explicit "should NOT have access to Teacher profile"). Founder-initiated impersonation (`impersonation_scope` absent) is completely untouched by this middleware — existing full-access behavior for Founder is preserved.
- **`StopImpersonatingController` changes.** Branches on `impersonation_scope` before pulling it from the session: if `web_registration_manager`, also pull `impersonation_version_id`, log the original user back in exactly as today, but redirect to `events.versions.web-registration` for that version instead of the hardcoded `founder.impersonate`. The Founder path (`impersonation_scope` absent) redirects exactly as it does today — unchanged.

**Transfer Students tab.** New `App\Services\TeacherStudentTransferService::transfer(Version $version, User $actingUser, School $fromSchool, Teacher $fromTeacher, School $toSchool, Teacher $toTeacher, array $studentIds): int`, generalizing the existing `App\Support\ReplacedTeacherStudentTransfer` (which only ever handles a same-school teacher replacement, triggered automatically at teacher-verification time) to the source doc's three real scenarios — same-school teacher replacement, cross-school student transfer, and grade-band promotion (middle → high school) — all driven by an explicit admin action instead of an automatic trigger:

- Authorization: `canManageWebRegistration`; both `fromTeacher` and `toTeacher` must be in `invitedTeachers($version)`.
- Roster query (both panels): active `student_teacher` rows for the selected School+Teacher pairing, restricted to **current** students — `school_student.class_of >= school.senior_year` — the same test `ReplacedTeacherStudentTransfer` already uses. Left panel is checkbox-selectable; right panel ("Current Students") is read-only context, matching the legacy screenshot.
- For each selected student, inside one `DB::transaction`:
  1. Move every matching active `student_teacher` row from (`fromTeacher`,`fromSchool`) to (`toTeacher`,`toSchool`) — preserving `subject`/`role` — via Eloquent `save()` (never a raw bulk `update()`), so `StudentTeacherObserver` fires normally. If the destination already has a row satisfying the table's unique constraint, delete the source row instead of moving it (same conflict rule `ReplacedTeacherStudentTransfer` already uses).
  2. If `toSchool->id !== fromSchool->id`: deactivate the `school_student` row at `fromSchool`, `updateOrCreate` an active one at `toSchool` with the same `class_of` (grade-level continuity is preserved across the move; `Student::grade` recomputing differently against the new school's own `senior_year` is expected and correct for a promotion).
  3. Update `Candidate` rows at `fromSchool`/`fromTeacher` for this student to `toSchool`/`toTeacher` — a direct generalization of `ReplacedTeacherStudentTransfer`'s existing `Candidate::where(...)->update(...)` call, now also moving `school_id`. **Resolved by inference (flagged in case it's wrong):** this is **not** scoped to only the Version currently being managed — it follows the existing precedent of moving every open Candidate record for that student at that school/teacher, since a real-world teacher/school change is a durable fact that should follow the student everywhere, not just in the one Version the Web Registration Manager happened to have open.
  4. Step 1's `StudentTeacherObserver::created()`/`updated()` → `AutoEnrollmentService::enrollNewStudentIntoInvitedActiveVersions()` cascade is what implements the source doc's second bullet ("add candidate records for any open event versions for which the teacher is invited and the student is eligible") — entirely via existing infrastructure, no new enrollment logic needed here.
- Livewire side: cascading School→Teacher selects on both panels (Teacher options constrained to `invitedTeachers($version)`; School options derived from that teacher's own active+verified schools), a "Transfer N Students" button reflecting the live checkbox count (matching the screenshot), and a `Flux::toast` confirmation per this project's CRUD-modal-toast convention.

**Ordering and same-teacher guard, added after live testing 2026-08-05.** The product owner's original design has an ordering dependency this pass initially missed: "Transfer To" is disabled (School and Teacher selects both) until "Transfer From" is fully specified — a school, a teacher, and at least one checked student. Unchecking every student (or changing the From teacher/school) drops "Transfer To" back to its disabled/cleared state rather than leaving a stale selection behind. Separately, **the From teacher can never appear as a To choice when the same school is picked on both sides** — selecting the identical teacher on both sides is a no-op transfer that was found to actually corrupt the affected student's roster state in live testing (the same `student_teacher` row being simultaneously the "source" and "destination" of the move). The To teacher list excludes the From teacher whenever `toSchoolId === fromSchoolId`; if a later From-side change causes an already-picked To teacher to collide, it's cleared automatically. `transferStudents()` also gained a `different:fromTeacherId` validation rule on `toTeacherId` as a server-side backstop, since `wire:model`'s client-side `disabled` attribute is a UX nicety, not an authorization boundary. Each Teacher `<select>` also picked up a `wire:key` (scoped to the selected School id) and each `<option>` its own `wire:key` — both sides had a live-tested Livewire/Alpine `<select>`-morphing glitch where the browser displayed the first `<option>` as though selected while the real bound value was still null, only resolving after the manager toggled between two real options; keying forces Livewire to fully replace the element instead of patching it in place. **Still live-testing as of this pass:** a school with exactly two invited teachers still sometimes requires toggling between them once before the roster displays correctly — narrowed down to the same `<select>`-morphing class of issue but not yet fully root-caused; a school with exactly one teacher (now auto-selected on both From and To) is unaffected since there's no dropdown interaction to glitch.

**Built:** `VersionRoleAssignmentService::canManageWebRegistration()`; `VersionInvitationEligibilityService::invitedTeachers()`; `App\Services\TeacherStudentTransferService` (`currentStudents()`, `transfer()`); the `WebRegistration` Livewire component + view (route `events.versions.web-registration`, two tabs) with a "Web Registration" button added to the Event `Show` page; the two new session keys and `App\Http\Middleware\RestrictWebRegistrationImpersonation` (registered globally in `bootstrap/app.php`); `StopImpersonatingController` branching on `impersonation_scope`; the impersonation banner in `app.blade.php` now reads "Return to Web Registration" for this flow instead of always "Return to Founder account"; auto-select-the-only-teacher on both From and To; the From/To ordering gate and same-teacher exclusion described above. 33 new tests across `VersionRoleAssignmentServiceTest.php` (+3), `TeacherStudentTransferServiceTest.php` (new file, 6), `WebRegistrationTest.php` (new file, 19), `WebRegistrationImpersonationTest.php` (new file, 5); 848/848 passing app-wide, PHPStan clean.

**Not built this phase (flag, don't scaffold):** any audit trail of the transfer itself beyond the incidental `updated_at` timestamps on the moved pivot/Candidate rows (no dedicated "transfer log" table — `candidate_status_history` only records *status* transitions, not `teacher_id`/`school_id` changes) — worth a follow-up product question if a durable transfer history turns out to matter; email/notification to either teacher when a transfer or impersonation occurs; extending either capability to Registration Manager/Co-Registration Manager (source doc names only Web Registration Manager).

---

## 6. Lifecycle — In Scope

The Version moves through the following workflow. Phases 6.1–6.2 are the build target for this phase; later phases are in §7 (reference only).

### 6.1 Configuration

The Event Manager sets up the Version's rules and configurations (adjudication rules, milestone dates, fees, audition type, membership requirements, application type, timeslots, county data, ensemble order, default invitations, pitch-file visibility, and role assignments — all per §5), and builds the Version's invitation list, controlling which eligible teachers may enroll Candidates (§5.4).

### 6.2 Registration

Teachers register their eligible Candidates for an audition. Teachers control:

- Their Candidates' access to self-registration material on StudentFolder.info.
- Their Candidates' ability to submit e-payments for fees.
- Creation/upload of registration materials — or, for self-registering candidates, approval of those materials.

Only Candidates who have met all registration requirements and been approved by the teacher move into adjudication.

**Access & navigation (designed 2026-07-16; wired 2026-07-16; revised to three buckets 2026-07-16).** The Sidebar's top-level "Registrations" nav item (`resources/views/components/layouts/app.blade.php`) is visible to a teacher only when `VersionInvitationEligibilityService::hasAnyRegistrationAccess()` is true — an existing `version_invitations` row, an existing active candidate, or at least one currently-open (`version_dates` type `teacher`, §5.2) Version they're eligible for per §5.4's OR logic (county match or org membership, unrestricted if the Version has no `version_counties` rows). Once inside, `App\Livewire\Registrations\Index` (`Registrations/Index.php::buildSections()`) sorts every candidate Version into **at most one of three mutually exclusive buckets** (a Version satisfying none is omitted entirely rather than shown with a dead-end link):

- **"Open for Registration"** — the window is open AND the teacher has an existing `version_invitations` row (any status, including `Rejected`). Computed eligibility is **not** re-checked here: an invited teacher whose eligibility has since lapsed (e.g. a `version_counties` reconfiguration) still sees "Manage," since the invitation itself — not the pool computation — is what grants standing. This intentionally reversed an earlier version of this rule that *did* re-check eligibility for invited teachers and dropped them from the list; that turned out to be the wrong call and was corrected the same day. As of 2026-08-12 (§9 item 32, §5.6) clicking "Manage" with a `Rejected` (or never-answered) decision no longer shows a dashboard callout — it redirects straight to `registrations.obligations` via `GuardsAcceptedObligations`; the card itself also carries an "Obligations" badge for any Version with a published obligation, a direct link back to that form regardless of decision.
- **"Invitation Available"** — the window is open, the teacher has **no** invitation row, but is eligible per §5.4's `isEligible()`. This is the self-service discovery surface for §5.8's Request Invitation flow, deliberately kept as its own visually-distinct section (separate heading, amber "Not Invited" badge) rather than folded into "Open for Registration" — an early version showed these rows inside "Open for Registration" itself, which read as if the teacher could already act on them; product feedback (screenshot-driven) called that out explicitly.
- **"Active Candidates"** — the window is not open AND the teacher has at least one Candidate there still in a registration state (`eligible`/`pending`/`registered`, §8.3). Eligibility and invitation status are never considered once the window has closed — an existing Candidate is itself proof of prior access.

**Not wired**: the dashboard's "Events" card (`resources/views/dashboard.blade.php`) still just lists active Events with no per-Event/Version registration affordance — the source design doc mentioned "the Events card" alongside the Sidebar, but that card has no per-item action links today for anything to hook into; flagged as a smaller follow-up, not done in this pass.

**Candidate access scope.** A teacher (or any other active+verified teacher at the same school, per the `TeacherRole::Coteacher` distinction on `school_teacher` — access follows the school relationship, not a per-candidate assignment) can view and manage every Candidate whose `school_id` matches one of their own active+verified `school_teacher` schools, for Versions where that school is participating — not only Candidates where they are the specific `teacher_id` of record. `candidates.teacher_id` still identifies the primary teacher for a Candidate (e.g., for default correspondence), but it is not the access gate.

**Audition uploads (`audition_type = remote`).** Each Candidate must have an uploaded recording for every `version_upload_files` slot (§5.2) before the "Upload of required audio/video files" milestone below is satisfied; the teacher (or co-teacher, per the access rule above) reviews and approves or rejects each one. Rejecting deletes the file (and its S3 object) outright and re-opens that slot for another upload — see `candidate_upload_files` in §5.2 for the schema and the delete-on-reject rationale.

**Automatic enrollment (built 2026-07-16) — the manual "Enroll a Student" form is gone.** Candidates are no longer manually enrolled by a teacher picking a student from a dropdown on `VersionDashboard` — that form and its backing `enroll()` action were removed entirely. Instead, `App\Services\AutoEnrollmentService` proactively enrolls every eligible student the moment they become eligible, via two model-Observer triggers:

- **`App\Observers\VersionInvitationObserver::created()`** — fires when a teacher is newly invited (either path: an Event Manager's direct invite, §5.4, or an approved self-service request, §5.8). Enrolls every one of that teacher's currently-eligible students into the Version. Scoped to Versions currently `Active` — an invitation issued while the Version is still `Sandbox` does not populate real Candidate data before an Event Manager has actually opened it (an inferred consistency choice, not explicitly requested, since the source instruction didn't specify this trigger needed the Active gate — flagged in case it's wrong).
- **`App\Observers\StudentTeacherObserver`** (`created`/`updated`) — fires when a student is newly added to a teacher's roster, or an existing `student_teacher` row is reactivated (`is_active` flips to `true`). Enrolls that one student into every Version that is currently `Active` **and** for which the teacher already holds an invitation (any status). **Known gap**: this does not cover the indirect `is_active` cascade in `SchoolStudentObserver::saved()` (a student's school reactivating cascades to `student_teacher.is_active` via a raw query-builder `update()`, which never fires Eloquent model events) — direct roster-add/reactivation call sites are covered, that indirect path is not.

**"Eligible" now includes grade matching, everywhere `EligibilityService::eligibleStudents()` is used** (previously grade was explicitly *not* checked there, per the source comment "grade filtering is intentionally skipped here"). A student's grade (`Student::grade`, computed from `school_student.class_of` and the school's `senior_year`) must be one of the Event's `event_grades` rows — unrestricted if the Event has none configured (same "zero rows means unrestricted" convention as `version_counties`, §5.4). Grade is a computed attribute, not a stored column, so it's filtered in PHP after the query rather than in SQL.

**Voice part on auto-enrolled Candidates.** `candidates.voice_part_id` is NOT NULL. Resolution order at enrollment time, computed by `AutoEnrollmentService`: the student's own `voice_part_id` if it's one of the Version's `availableVoiceParts()` (the Event's ensemble-linked voice parts); otherwise the first available voice part (ordered by `sort_order`); if the Event has no ensemble voice parts configured at all yet, the enrollment is skipped entirely rather than guessing. **A teacher can change it after the fact as of 2026-08-12** (§9 item 32): `CandidateDetail`'s Student edit modal writes directly to `candidates.voice_part_id`, constrained to the same `availableVoiceParts()` pool, deliberately never touching `students.voice_part_id` — the Student's own general-profile voice part and a given Candidate's actual audition voice part are allowed to diverge.

**`CandidateDetail` page redesign — card + modal throughout (2026-08-12, §9 item 32).** `App\Livewire\Registrations\CandidateDetail` (`/registrations/{version}/{candidate}`) was reorganized into a fixed section order — Student, Home Address (only when `versions.home_address` is true), Emergency Contacts, Program Name, then Candidate Application — each rendered as a read-only display card with an Edit/Add button opening a `flux:modal` form, rather than the prior mix of always-open inline forms and one modal. This applies to every section, including Program Name (previously the only inline form on the page): the underlying data here is set once during onboarding/registration and rarely touched again, so an always-visible edit form on every page load was judged more clutter than convenience — and keeping one section inline while the rest went modal would have read as inconsistent. Student editing covers first/last name (writes to the Student's `User`), voice part (see above), birthday, and height. Home Address editing covers all five `home_addresses` columns. Emergency Contacts gained per-contact Edit (previously add-only) — the same modal serves both Add and Edit, switching its heading/button label and branching `saveEmergencyContact()` between `create()` and `update()` on an `editingEmergencyContactId` tracking property.

**UX requirement (poka-yoke).** This phase benefits greatly from a poka-yoke approach: the teacher must always be able to see what is done, what is missing, and the upcoming milestone dates. Checklists and strong visual status cues are critical. The interface must make it trivial to answer:

> *"Why isn't this student in 'registered' status?"*

**Candidate gate — `eligible` → `pending` → `registered`.** Depending on Event/Version configuration, all applicable milestone items below must be satisfied to promote a Candidate from `eligible` to `registered`:

- Emergency contact (name / email / phone)
- Candidate home address
- Candidate cell phone
- Candidate email address
- eApplication approval
- Teacher application approval
- Upload of required audio/video files
- Teacher approval of required audio/video files

If a candidate has one or more milestone items completed but not the full set required by the Event/Version configuration, the Candidate status is `pending`.

Which items are required is driven by the data-collection requirement flags on the Event (§3.2). The gate logic is the responsibility of the `EligibilityService`; enrollment and promotion run through `CandidateService::enroll()`, and every status change is recorded in `candidate_status_history`.

**Teacher close-out.** Teachers close their individual registration phase by printing an estimate/invoice that includes pertinent candidate information, total paid, amount due, and required membership materials.

### 6.3 Email notifications

- Teachers receive a weekly registration-progress update (opt-out-able).
- Event Manager(s) and (Co-)Registration Manager(s) receive a weekly Version registration-progress update (opt-out-able).

---

## 7. Lifecycle — Reference Only (Not This Phase)

The following phases are documented so the data model stays forward-compatible. **Do not scaffold UI or algorithms for these now.**

### 7.1 Pre-adjudication

Registration Manager(s) review the physical materials submitted by teachers against TheDirectorsRoom.com records: signature completeness (candidate, parent, teacher, principal), payment reconciliation (with check payments recorded). Managers send a form email confirming receipt and a custom email listing items to reconcile.

### 7.2 Adjudication

Judges navigate to a Judge page listing the Candidate ids to adjudicate and the adjudication form. A judge sees other judges' scores only after entering their own. Scores outside room tolerance are immediately highlighted but not prohibited. Individual and room status (pending, in-progress, completed, out-of-tolerance, error) is always visible.

**Built 2026-08-07 — see §9 items 22–23.** The access gate (active Version + RoomJudge assignment on that Version + inside its `date_type=adjudication` window + not a currently-enrolled student) and the live scoring page itself (Judge Bios, Room Performance Indicator Bar, Candidate roster, score-entry form, Room Scoring table with tolerance checking, and a real candidate-recordings section) are built, with two deliberate departures from the paragraph above: candidates are identified only by `candidates.id` (auditions are anonymous, not by name), and a judge's own scores are never gated behind having entered scores first — every judge sees the Room Scoring table for a Candidate as soon as any judge has scored them, since judges' timing is independent of each other and only room tolerance needs cross-judge data. **Tab Room (§7.3) and score cut-offs (§7.4–§7.5) are now built too — see below.** Teacher-recusal score averaging remains unbuilt/out of scope.

### 7.3 Tab Room

The Tab Room Manager sees overall adjudication progress in graphic form per Version, per Room, per Voice Part, and per-Candidate detail, and can override judge scores. The Event Manager closes the Version and releases results via the Tab Room.

**Add/Edit Scores + Adjudication Tracking built 2026-08-10 — see §9 item 26.** A Tab Room Manager can search a Candidate by id or last name and create/update any judge's scores on their behalf (audited via `scores.overridden_by_user_id`/`overridden_at`, distinguishing a Tab-Room override from the judge's own entry), including the destructive voice-part-change cascade the source doc calls out. Adjudication Tracking is an almost entirely read-only lens over the same `AdjudicationService` the judge page uses — overall/per-Room progress bars, a per-Candidate badge grid with a judge-by-judge hover breakdown, a 10s polling refresh, and a Red/Amber/Green summary per Room. **Ensemble Cut-offs built 2026-08-10–11 — see §9 items 27–28** (below, and §7.4–§7.5). **Close Audition built 2026-08-11 — see §9 item 29** (below, and §7.6): a dedicated Tab Room Manager page — reachable via `canManageTabRoom()` specifically, unlike `VersionEdit`'s general Status field which only an Event Manager can reach — closes the Version, stamps `results_released_at`, and optionally emails participating teachers. **Reports sub-module built 2026-08-11 — see §9 item 30** (below, and §7.6): five reports gated by a new `canManageTabRoomReports()`, all Tab Room Module.docx now has completely built.

### 7.4 Score cut-offs

On adjudication completion, the Event Manager builds Ensemble membership by setting cut-off scores per voice part. Candidates at or better than the cut-off are selected; those below are not. Event/Version configuration determines whether scores sort ascending or descending (i.e., whether lower or higher is better).

**Built 2026-08-10–11 — see §9 items 27–28.** `EnsembleCutoffService` drives this: `rankedCandidates()` (per-Voice-Part, best-to-worst, including already-decided Candidates so a cutoff can be re-applied against a different score without first undoing the prior decision), `transitionNonCompletedCandidates()` (no-show/incomplete), and the accept/reject + `AuditionResult`-write pair the four `CutoffStrategy` implementations (§7.5) call back into. One material design correction made mid-build: assignment is **by distinct score value, not by candidate row** — every Candidate tied at the same score moves together to the same Ensemble (confirmed against a real "three candidates tied at 100, four tied at 101" case), which only mattered for `AlternatingEnsembleAssignment`'s rank-position rotation; the other three strategies are pure threshold checks and already grouped ties correctly. The Ensemble Cut-offs page shows every Voice Part as side-by-side columns in one shared-scroll grid (each score is itself the "apply cutoff" button) rather than one full-width section per Voice Part, after the first pass proved to require "unreasonable scrolling" in real use.

### 7.5 Ensemble assignment — align to the CutoffStrategy enum

**Built 2026-08-10–11 — see §9 item 27.** All four cases below are real implementations now (`app/Services/CutoffStrategies/*`), not reference-only. The agent implemented against these enum cases rather than introducing new names, per the original instruction. Mapping:

| Original spec name | CutoffStrategy case | Behavior |
|---|---|---|
| Single ensemble | `PerVoicePartPerEnsemble` | Same-or-better than the cutoff → assigned; otherwise unassigned. The single-ensemble base case. |
| Multiple ensembles, cascading cutoffs | `SequentialEnsembleFill` | Ensembles have a priority order; multiple cutoffs per voice part. Fill ensemble 1 to its cutoff, then ensemble 2, etc., until `ensemble_count` is met. |
| Multiple ensembles, alternating cutoff | `AlternatingEnsembleAssignment` | One shared candidate pool; score routes a candidate between ensembles (tiers of the same population). Priority order, one cutoff per voice part; starting from the best score, assign candidates to ensembles in alternating order per score until each cutoff is met. |
| Multiple ensembles defined by grade | `GradeSegmentedEnsembles` | Candidates are partitioned by grade before any score comparison (e.g., a 7th-grade soprano competes only for the junior ensemble, an 11th-grade soprano only for the senior ensemble; they never compete). Ensembles may use different voicing taxonomies; each applies its own per-voice-part cutoff. Disjoint, independent ensembles selected in parallel — not tiers of one pool. |

Implementation note: every `CutoffStrategy` case only ever **accepts** — none of the four reject. `EnsembleCutoffService::finalizeVoicePart()` is the single place rejection happens, once every relevant Ensemble's cutoff has been applied; this is what lets `SequentialEnsembleFill`/`GradeSegmentedEnsembles` apply one Ensemble's cutoff per manager click without a mid-sequence Candidate being prematurely rejected before the next Ensemble's turn. `AlternatingEnsembleAssignment`'s rotation is deterministic by score-group order alone (§7.4's tie-grouping fix), independent of which Ensemble's `apply()` call runs first. `applyEnsembleCutoff()` additionally excludes Candidates already accepted into a *different* Ensemble from the pool it hands the Strategy, so re-applying one Ensemble's cutoff after a reopen never poaches another Ensemble's already-decided members.

### 7.6 Results

When the Event Manager closes the Version via the Tab Room:

- A Version-wide confidential results summary is created (confidential).
- A Version-wide detailed results summary is created (detailed; Event Manager only).
- Teachers see their Candidates' results on the Event results page, including:
  - A printable summary PDF of all the teacher's registered candidates,
  - A printable per-candidate PDF for individual discussions, and
  - If configured: a printable confidential PDF, and retained access to pitch files.

**Close Audition built 2026-08-11 — see §9 item 29.** A dedicated `Close Audition` Tab Room page now exists (`App\Livewire\Events\TabRoom\CloseAudition`), reachable via `canManageTabRoom()` — so a Tab Room Manager who isn't also an Event Manager, who has no access to `VersionEdit`'s general Status field at all, can still close the audition, matching Tab Room Module.docx's description of this as a Tab Room Manager action. `versions.status` remains settable from two places (this page, and `VersionEdit`'s general Status field for an Event Manager) — both now snapshot `EnsembleHistory` (§9 item 28) on every save that results in Closed, so a reopen-correct-reclose cycle from either path can't leave a stale snapshot. A new, narrower `versions.results_released_at` timestamp — owned *exclusively* by this page, not `VersionEdit` — is the actual results-visibility gate: Close stamps it (and optionally batch-emails a new `AuditionResultsAvailableMail` to every teacher with a resolved-outcome Candidate, signed by the closing manager's name); Reopen clears it without touching any Candidate's decision, so results simply stop being teacher-visible during a correction window. The teacher-facing `Registrations\Results`/`ResultsIndex` pages — previously placeholders (hardcoded score/accepted/ensemble columns, filtered to `status=Registered`, which is empty by the time cut-offs have run) — are now real, gated on `results_released_at !== null` and widened to the four actual resolved outcomes (`accepted`/`not_accepted`/`no_show`/`incomplete`).

**Reports sub-module built 2026-08-11 — see §9 item 30.** Five Tab-Room-Manager-facing reports (`App\Livewire\Events\TabRoom\Reports\*`), a distinct set from this section's own "Version-wide confidential/detailed summaries" and teacher-facing printable PDFs above, which remain unbuilt (no product-owner-supplied spec for those two summaries or the per-candidate discussion PDF exists yet — only Tab Room Module.docx's own five-report list was scoped). Audition Scores (PDF only), Combined Audition Scores confidential/public (PDF+CSV, one shared component keyed by a `confidential` flag), Ensemble Participation (PDF+CSV), and Student Seniority (PDF+CSV, the one report needing a bespoke cross-Version join) — see §9 item 30 for the full built-artifact list.

**Teacher-facing public score reports built 2026-08-12 — see §9 item 31.** Fills in this section's "printable per-candidate PDF for individual discussions" bullet above (and, per-school rather than per-teacher-roster, its "printable summary PDF" bullet) — both reusing the *public* (Candidate #-only, no student/school/teacher identity) score grid `TabRoomReportService` already built for the Tab Room Manager's Reports sub-module (§9 item 30), rather than a new confidential/detailed summary format. Added directly to `Registrations\Results`: a Per-School report (that teacher's own Candidates at one School, one Candidate per page) and a Per-Person report (one Candidate), each with a view-modal and a landscape-letter PDF download, gated on the same standing + `results_released_at` checks the page already enforced. Also added the optional `share_results` Version setting (§5.1): when on, the Event Manager's Close (§7.6 above) auto-queues the existing "All Ensembles" public PDF job and a signed-S3-URL download icon appears for every participating teacher once it's ready — still no Version-wide confidential/detailed summary or teacher-facing pitch-file retention, which remain unbuilt as noted above.

### 7.7 Rehearsal

On Version close, the Rehearsal Manager gains access to detailed results and contact information for each accepted member of each Ensemble, as a web page and CSV file(s).

### 7.8 Alternates

Identification of alternate candidates to replace accepted ensemble members who decline or no-show post-acceptance. Needs rules from NJMEA; need to be tested with MACDA and CJMEA.

---

## 8. Enum & Status Reference

Consolidated enums. Define each as a PHP backed enum. Transitions marked "TBD" need confirmation.

### 8.1 EventStatus

`sandbox | active | inactive | closed` — default `sandbox`. Transitions per §3.1.

### 8.2 Frequency

`annual | biannual | biennial | monthly` — default `annual`.

### 8.3 Candidate status

Twelve states. Every change is written to `candidate_status_history`. Allowed transitions:

- `eligible → pending → registered` (registration milestones; see §6.2)
- `registered → withdrew | teacher_withdrawn` (post-registered, pre-adjudication)
- `registered → adjudicated | no_show | incomplete` (adjudication results)
- `adjudicated → accepted | not_accepted` (cutoff assignment; §7.4–§7.5)
- `accepted → declined | removed` (post-acceptance; candidate or Rehearsal Manager)

Full set (12): `eligible`, `pending`, `registered`, `withdrew`, `teacher_withdrawn`, `adjudicated`, `no_show`, `incomplete`, `accepted`, `not_accepted`, `declined`, `removed`.

**Implementation note (§9 items 27–28):** `no_show`/`incomplete`/`accepted`/`not_accepted` are real, written transitions now. `EnsembleCutoffService` deliberately skips the literal `adjudicated` intermediate hop — it writes `registered → accepted|not_accepted|no_show|incomplete` directly, since `adjudicated` was never anywhere else set as its own durable state (nothing reads it as a gate), and `candidate_status_history` still logs the real `registered`→final-state transition either way via `CandidateObserver`. `accepted → declined | removed` remains unbuilt (§7.7–§7.8).

### 8.4 Adjudication status (reference)

`pending | in_progress | completed | out_of_tolerance | error` — applies to both individual Candidate and room.

**Naming drift, resolved by design (§9 items 22–23, 27–28):** no `AdjudicationStatus` PHP enum was ever built — `AdjudicationService::candidateStatuses()`/`voicePartCompletion()` bucket into string literals `none|partial|completed|error` (`none`/`partial` cover this section's `pending`/`in_progress`), and out-of-tolerance is its own separate boolean map (`candidateTolerances()`), not folded into the status bucket — a Candidate can be `completed` *and* out-of-tolerance simultaneously, which the single-enum model above can't represent. Not revisited; the split has held up through both Adjudication and Tab Room.

### 8.5 CutoffStrategy — built 2026-08-10–11, see §9 item 27

`PerVoicePartPerEnsemble | SequentialEnsembleFill | AlternatingEnsembleAssignment | GradeSegmentedEnsembles` — see §7.5. Each case maps to a real class in `app/Services/CutoffStrategies/`, resolved via `CutoffStrategy::strategyClass()`; no longer reference-only.

### 8.6 AuditionType

`in_person | remote`.

### 8.7 ApplicationType

`eapplication | pdf`.

### 8.8 VersionInvitationStatus

`invited | obligated | participating` — no `eligible` case; absence of a `version_invitations` row is the eligible state. See §5.4. Default on row creation: `invited`. `participating` is still reserved for the not-yet-built registration workflow. `obligated` is no longer reserved-only — see §5.6: it is now written by `VersionObligationResponseObserver` when a teacher accepts Version Obligations.

### 8.9 VersionObligationStatus

`draft | published` — default `draft`. See §5.6.

### 8.10 ObligationDecision

`accepted | rejected`. See §5.6.

### 8.11 VersionInvitationRequestStatus

`pending | denied | approved` — default `pending`. See §5.8. Distinct from `VersionInvitationStatus` (§8.8) — a request row is deleted-in-effect (reset to `pending`) on re-request rather than accumulating, and `approved` triggers creation of the actual `VersionInvitation` row rather than being read as the invitation state itself.

### 8.12 CandidateUploadStatus

`pending | approved` — default `pending`. See §5.2 (`candidate_upload_files`). No `rejected` case by design — a rejection deletes the row instead of transitioning to a third state. Built 2026-08-12 (§9 item 33), alongside the table itself.

### 8.13 JudgeType

`HeadJudge | LeadJudge | Judge1 | Judge2 | Judge3 | Judge4 | JudgeMonitor | Monitor`. See §5.9. A single flexible set merging both label conventions from the source doc — not validated against `audition_type`.

### 8.14 JudgeStatus

`Assigned | Exempt | Completed | Delegated | LeftEarly | NoShow | Substitute` — default `Assigned`. See §5.9 (`room_judges`). Day-of transitions beyond the default belong to the not-yet-built Adjudication phase.

### 8.15 PaymentType

`Check | PurchaseOrder | Cash | Other | Electronic` — see §5.10 (`teacher_payments`, `candidate_payments`). `Electronic` is written on `candidate_payments` as of 2026-08-13 (§9 item 33), but only via a manual teacher-entered record, not a real gateway — genuinely automated `Electronic` writes on either table remain blocked on the not-yet-built `epayment_credentials` (§5.2) integration.

---

## 9. Open Decisions (consolidated)

Confirmed as of the 2026-07-09 implementation audit:

1. ~~**Uncommitted change.**~~ Resolved — the `tests/Feature/EnumsTest.php` `VersionDateType` case-order fix was committed in `6e1ab02 "Updt: Checks and missing configuration items."`
2. ~~**`voice_parts` naming drift.**~~ Resolved — spec §4.3 now matches the shipped `name` / `abbr` / `sort_order` columns.
3. ~~**`emergency_contact` naming drift.**~~ Resolved — spec §5.2 now matches the shipped `emergency_contacts` table (`cell_phone`/`home_phone`/`work_phone`/`relationship`, `EmergencyContactRelationship` enum, `BestPhone` enum).
4. ~~**`EnsembleVoicePart` pivot ordering.**~~ Resolved — confirmed by design: the pivot intentionally carries no ordering column, and `Ensemble::voiceParts()` must always order by `voice_parts.sort_order`. Documented in §4.4.
5. ~~**`version_timeslots` has no seeder or UI yet.**~~ Resolved — no seeder is planned; past timeslots are irrelevant, so this row is dropped from the open list.
6. **`epayment_credentials` is schema-only.** No seeder or UI, as the spec anticipates ("next-phase implementation"). No action needed this phase.
7. ~~**Reference-only items remain unbuilt by design**, confirming the §0.2 scope fence held during implementation: `version_adjudication` (rooms, scoring categories/factors, judge types), `audition_results`, `AdjudicationStatus` enum, `CutoffStrategy` enum. Do not scaffold until the Adjudication Wizard phase begins.~~ Superseded — this was correct as of the 2026-07-09 audit, but every item in this list except `AdjudicationStatus` (§8.4, deliberately never built as its own enum — see that section's note) is now built: `version_rooms`/`room_score_categories`/`room_voice_parts`/`room_judges` (§9 item 17), `audition_results` (§9 item 24), `CutoffStrategy` (§9 item 27). The Adjudication Wizard phase referenced here turned out to be the Tab Room Module build (§9 items 22–28), not a separately-named phase.
8. ~~**Version Invitations (§5.4) — new capability, not yet built.**~~ Resolved — built and committed in `9e015fa "Add: Invitations functionality."`: `VersionInvitationStatus` enum, `VersionInvitation` model + migration, `VersionInvitationEligibilityService`, `VersionInvitationRosterRow`, the `VersionInvitations` Livewire component + view, and feature/unit test coverage. Membership eligibility leg accepts any `Membership` row regardless of expiration. County leg is unrestricted when a Version has no `version_counties` rows. Multi-school teachers get one roster row. Modeled as a new `version_invitations` table, intentionally separate from `EventInvitationRequest`.
9. ~~**Still intentionally deferred within §5.4: any UI for the `obligated`/`participating` transitions.**~~ Partially resolved — the `obligated` transition now exists (§5.6: `VersionObligationResponseObserver` writes it on teacher acceptance). Still deferred: search/filter/pagination on the invitation roster, email notification on invite, and any UI for the `participating` transition — the latter still belongs to the not-yet-designed registration workflow.
10. ~~**Version Pitch Files (§5.5) — new capability, not yet built.**~~ Resolved — built and committed in `b70abb3 "Add: Initial pitch files functionality."` and `2a55580 "Updt: Completed pitch files functionality."`: `VersionPitchFile` model + migration (`version_pitch_files`), the `VersionPitchFiles` Livewire component + view (search/filter/sort/drag-reorder), S3-backed file storage with delete-on-replace and delete-on-remove, and feature test coverage (`VersionPitchFilesTest`, 14 tests). `voice_part_id` is validated against `Version::availableVoiceParts()`. Gated by the same `canManageEvent` check as `VersionEdit`/`VersionInvitations`. `VoicePartSeeder` gained an `'ALL'` voice part (for files that apply across parts) as part of this work; `SeedersTest` was updated from 17 to 18 expected voice parts to match.
11. **Still intentionally deferred within §5.5** (flagged, not scaffolded): enforcement of `pitch_file_visibility` (teacher/candidate/both) and the post-close visibility windows, and any candidate/teacher-facing display of pitch files on StudentFolder.info — this phase is Event Manager CRUD only.
12. ~~**Version Obligations (§5.6) — new capability, not yet built.**~~ Resolved — built 2026-07-09 (not yet committed): `VersionObligationStatus` and `ObligationDecision` enums, `VersionObligation` + `VersionObligationResponse` models/migrations, `VersionObligationObserver` (HTMLPurifier sanitization via a new `obligations` profile in `config/purifier.php`, itself a new `mews/purifier` dependency), `VersionObligationResponseObserver` (writes `VersionInvitationStatus::Obligated`), the "Obligations" tab added to `VersionEdit` (admin CRUD, save/publish/unpublish, Preview modal), and `App\Livewire\Registrations\VersionObligations` (teacher-facing accept/reject page).
13. ~~**Still intentionally deferred within §5.6: a dedicated Pest feature test file.**~~ Resolved — added `tests/Feature/Livewire/Events/VersionEditTest.php` (+9 tests: save/publish/unpublish, sanitization, Preview modal), `tests/Feature/Livewire/Registrations/VersionObligationsTest.php` (new file, 8 tests: 404-when-uninvited, draft invisibility, merge-field resolution, accept/reject, toggle behavior, frozen snapshot immunity to later edits), and `tests/Feature/VersionObligationObserversTest.php` (new file, 5 tests, matching the `SchoolStudentObserverTest` direct-model-observer pattern — including a regression test locking in a `getRawOriginal()`-lags-by-one-save timing bug caught and fixed during this work). 522/522 app-wide, up from the 500 baseline. ~~Still intentionally deferred within §5.6: PDF rendition of the obligations document, an "insert merge field" toolbar affordance, and any consequence for a `rejected` decision beyond logging it (open question: should rejection gate further participation, or stay purely informational?).~~ The rejection-consequence question was answered the very next day (2026-07-10, see §5.6's "Rejection is an iron gate" — this item's text just never got updated to reflect it): rejection blocks enrollment and, as of 2026-08-12 (§9 item 32), redirects the teacher away from every other Version-scoped page entirely. PDF rendition and the merge-field toolbar affordance remain genuinely deferred.
14. **Candidate Registration workflow (§5.8, `candidate_upload_files` in §5.2, access/navigation rules in §6.2) — designed 2026-07-16.** Scoped from a source design doc (`Registration_design.docx`) plus a clarifying-question pass with the product owner, resolving: (1) teacher-initiated invitation requests get their own Version-scoped `version_invitation_requests` table rather than reusing the Event-scoped `EventInvitationRequest` or overloading `VersionInvitationStatus`; (2) Event Manager approve/deny happens via personalized signed one-click email links, not an in-app queue; (3) Sidebar/Registration nav visibility reuses `VersionInvitationEligibilityService`'s existing OR logic rather than the source doc's stricter AND reading; (4) actual audition-recording upload/approve/reject gets a real schema (`candidate_upload_files`) now, not deferred as reference-only; (5) "co-teacher" candidate access is school-based (any active+verified teacher at the Candidate's school), not a per-candidate assignment. **§5.8 built 2026-07-16**: `version_invitation_requests` migration/model/enum, `VersionInvitationEligibilityService::isEligible()`, `VersionInvitationRequestService` (canRequest/request/approve/deny), `VersionRoleAssignmentService::eventManagersForEvent()`, the `RequestInvitation` teacher-facing Livewire page, `VersionInvitationRequestController`'s signed approve/deny routes (with a real "already handled" landing page, not a 404), and the submitted/approved mail classes — 24 new tests, PHPStan clean. **Nav filtering wired 2026-07-16**: `VersionInvitationEligibilityService::hasAnyRegistrationAccess()` gates the Sidebar's "Registrations" item, `Registrations/Index.php` filters its "Open for Registration" list by the same eligibility rules and routes each entry to Request Invitation or Manage accordingly — 10 more tests. 586/586 passing app-wide as of this pass, PHPStan clean. Signed-route expiry resolved at 7 days (matched to the closest existing precedent, `StudentClaimMail`). **Still open:** ~~`candidate_upload_files` (§5.2) is designed but not built~~ — built 2026-08-12, see §9 item 33; the dashboard's "Events" card still has no per-item registration affordance (§6.2); no in-app "pending requests" screen (email-only, by design); no re-request cooldown/rate-limit.

**Authorization gap found and fixed 2026-07-16.** `Registrations/VersionDashboard::mount()` had no invitation check at all — until this fix, any teacher with an active school could open `/registrations/{version}` directly (nav visibility is not an authorization boundary) and enroll candidates into a Version they were never invited to, since `EligibilityService::eligibleStudents()` only ever checked `isBlockedByRejectedObligations()` (a teacher who'd never been invited at all sailed through, since there was no row to be "rejected"). Fixed at two layers, mirroring the pattern already established by `Registrations/VersionObligations::mount()`'s single-gate convention:
- **Page-level (primary boundary):** `VersionDashboard::mount()` now checks for an existing `version_invitations` row (any status — including `Rejected`, so the existing obligations-rejected callout stays reachable). No row + eligible → redirect to `registrations.request-invitation` (mirrors `RequestInvitation::mount()`'s reverse redirect for an already-invited teacher, forming a symmetric pair). No row + ineligible → `403`. Since Livewire snapshots are checksum-signed and only ever issued after `mount()` completes, this is a real enforcement boundary, not just a first-load convenience check — a forged snapshot for an unauthorized Version can't be produced.
- **Service-level (defense-in-depth):** `EligibilityService` gained `isNotInvited(Version, Teacher): bool`, and `eligibleStudents()` now returns empty for a teacher with no invitation row, not just a rejected one — so the underlying data-access method is safe to call from anywhere, not only safe because the one known caller happens to gate first.

Test coverage: `VersionDashboardTest` gained 3 tests (redirect-when-eligible-uninvited, 403-when-ineligible-uninvited, and a direct `EligibilityService` defense-in-depth check) and every pre-existing test in that file was updated to set up a `version_invitations` row first, since none of them had one before. `EligibilityServiceTest` gained 2 tests (`isNotInvited` true/false, `eligibleStudents` empty with no invitation row) and its 5 pre-existing `eligibleStudents` tests were updated the same way, so each still isolates the specific condition it names rather than accidentally passing because the new gate alone made the result empty. 21 new/updated tests in these two files; full suite green, PHPStan clean.
15. **Open bug, unresolved: bulleted/numbered lists render with no visible marker inside `flux:editor`'s live admin UI (and its Preview modal), for both pasted-then-cleaned-up content and freshly toolbar-typed content.** Ruled out as a cause: this codebase's stored HTML (confirmed via direct DB read still has intact `<ul><li>` structure), the `mews/purifier` sanitization allowlist (includes `ul,ol,li`), and the Blade/Tailwind rendering path (a direct `view(...)->render()` test on the real persisted content produces correct `<ul>` + `list-disc` output). The defect is therefore isolated to the client-side `flux:editor`/TipTap layer — not yet root-caused (would need live browser DevTools inspection, which this agent cannot perform) and not yet reported to Flux support. Does not block any server-side functionality (save/publish/sanitize/accept/reject/merge-fields all verified correct regardless of how the editor visually renders lists while typing) but is a real polish gap before this ships to an actual Event Manager, given the source Teacher Obligations content is list-heavy.
16. **Automatic candidate enrollment (§6.2) — built 2026-07-16.** The manual "Enroll a Student" form on `VersionDashboard` is gone, replaced by proactive enrollment via two new Observers (`VersionInvitationObserver`, `StudentTeacherObserver`) and `App\Services\AutoEnrollmentService` — see §6.2 for the full trigger/eligibility/voice-part-resolution writeup. `EligibilityService::eligibleStudents()` gained grade matching (`event_grades`, unrestricted if unconfigured), applied uniformly rather than scoped to only the new automatic path, per explicit direction. 23 new/updated tests across `AutoEnrollmentTest.php`, `EligibilityServiceTest.php`, and `VersionDashboardTest.php` (5 removed, 1 renamed to reflect the removed UI); 608/608 passing app-wide, PHPStan clean. ~~**Known gap, flagged not fixed:** the indirect `student_teacher.is_active` cascade in `SchoolStudentObserver::saved()` uses a raw query-builder `update()` and never fires Eloquent events, so it does not trigger auto-enrollment — only direct roster-add/reactivation call sites do.~~ **Resolved same day**: `SchoolStudentObserver::saved()` now fetches the matching `student_teacher` rows and calls `->update()` on each pivot model individually rather than a single bulk query-builder statement, so `StudentTeacherObserver::updated()` fires normally when a student's school reactivation cascades their teacher-link back to active. One new test (`AutoEnrollmentTest.php`, 10 total now) confirms the cascade path triggers enrollment; the two pre-existing `SchoolStudentObserverTest.php` tests (direct cascade behavior, multi-school deactivation) still pass unchanged.
17. **Audition Environment (§5.9) — designed and built 2026-07-29.** Scoped from a source design doc (`Audition_environment.docx`) plus a clarifying-question pass with the product owner, resolving: (1) Room, Score Category/Factor, and Judge assignment apply identically to in-person and remote auditions — `audition_type` does not branch this domain, since the only difference between the two modes is whether the Candidate is physically present; (2) `version_scorings` (a flat alternative to score_categories/score_factors) is dropped — no technical/performance benefit at this data volume, and it would have traded away FK integrity and the Event/Version override model for a join-avoidance that doesn't matter on a few-dozen-row rubric table; (3) `score_categories`/`score_factors` are scoped to `event_id` with a nullable `version_id` override, all-or-nothing per Version (zero Version-owned rows = inherit the Event's set), matching the existing `version_counties`/`event_grades` "zero rows means unrestricted" convention; (4) the source doc's "Judge" table is renamed `RoomJudge`/`room_judges` — it's an assignment (person + role + room + version), not a person entity, avoiding confusion with the existing Spatie version-scoped role system (§5.3), which this is intentionally not part of; (5) `JudgeType` merges the source doc's in-person and remote label sets into one enum with no `audition_type` enforcement, since the two sets likely reflect different sponsoring organizations' own vocabulary (§7.8) rather than a real modality rule; (6) `scores` is built as schema/model only this phase (denormalized, frozen-at-scoring-time columns, matching the `audition_results`/`obligation_snapshot` precedent) — no live scoring entry UI, which remains §7.2 reference-only along with Tab Room and cutoff/ensemble-assignment logic. **Built:** `JudgeType`/`JudgeStatus` enums; `score_categories`/`score_factors` migrations/models with `Version::availableScoreCategories()`/`availableScoreFactors()` inheritance resolution; `version_rooms` + `room_score_categories`/`room_voice_parts` pivots + `VersionRoom` model; `room_judges` + `RoomJudge` model; the `VersionRooms` Event Manager CRUD screen (rooms, category/voice-part assignment, inline judge-by-email assignment, native drag-and-drop reorder); the schema-only `Score` model. 27 new tests (`ScoreCategoryResolutionTest.php`, `VersionRoomsTest.php`, `ScoreTest.php`, 2 new `EnumsTest.php` cases); 651/651 passing app-wide, PHPStan clean. **Still not built:** an authoring UI for `score_categories`/`score_factors` themselves (Event-level default rubric or Version-level override — both are direct-DB/tinker only for now); live judge-facing scoring entry UI, Tab Room, `AdjudicationStatus`, `CutoffStrategy` (unchanged §7 reference-only); `JudgeStatus` day-of transition UI beyond the `Assigned` default.
18. **Audition-environment access control + Scoring Rubric UI (§5.9) — designed and built 2026-07-29.** Follow-up to item 17, addressing its two flagged gaps: (1) `VersionRooms` shipped gated by `canManageEvent` (Founder + Event Manager only); the product owner wants Registration Manager/Co-Registration Manager included too, per-Version rather than Event-wide — new `VersionRoleAssignmentService::canManageAuditionEnvironment()`, with `VersionRooms` retrofitted to it; (2) `score_categories`/`score_factors` had no authoring UI at all — new `VersionScoringRubric` screen with an inherit-vs-customize fork/revert model (edit the Event default directly while inheriting; "Customize for this Version" duplicates it into Version-owned rows; "Revert to Event default" deletes them). See the design writeup above (§5.9, following the `scores` table spec) for full detail. Ordering deliberately uses numeric inputs, not drag-and-drop, for this nested two-level case. 30 new tests (`VersionRoleAssignmentServiceTest.php` +6, `VersionRoomsTest.php` +3, `VersionScoringRubricTest.php` new file with 21); 681/681 passing app-wide, PHPStan clean. **Not yet built:** everything already flagged as such throughout §5.9/§7 remains unbuilt — this item closes out both of item 17's gaps and adds nothing new to the deferred list.
19. **Co-Registration Manager delegation (§5.3) — designed and built 2026-08-04.** The product owner wanted Registration Managers able to assign the Co-Registration Manager role without opening the full Configure module to them. Resolved via clarifying questions with the product owner: (1) grantees are Founder/Event Manager (always) or Registration Manager specifically on that Version — not Co-Registration Manager itself, so the role cannot self-propagate; (2) the narrow permission covers both assign and revoke, symmetric with other CRUD screens in this app; (3) Co-Registration Manager assignment is **fully migrated** out of the generic Configure → Roles tab into a new dedicated screen — the two paths never coexist, so a Co-Registration Manager can never exist without a county assignment; (4) each assignment is paired with a set of "county of responsibility" rows, restricted to a subset of the Version's own `version_counties`, and **mutually exclusive** across managers on the same Version (a given county maps to at most one Co-Registration Manager), enforced by a unique constraint on `co_registration_manager_counties.(version_id, county_id)` — the primary check happens in the Livewire component's validation (`Rule::in()` against the live assignable set), with the DB constraint as the backstop, not the first line of defense. **Built:** `co_registration_manager_counties` migration/model (`CoRegistrationManagerCounty`); `Version::coRegistrationManagerCounties()`; `VersionRoleAssignmentService::canManageCoRegistrationManagers()`, `assignCoRegistrationManager()`/`revokeCoRegistrationManager()` (role + county rows written atomically in a DB transaction); `GENERAL_ROLES_TAB_ROLES` (5 roles, Co-Registration Manager excluded) now backs `assignableRoleNames()` and the generic `assignRole()`/`revokeRole()` guard, which reject `Co-Registration Manager` with a 400 if called directly; the `VersionCoRegistrationManagers` Livewire screen (route `events.versions.co-registration-managers`) with user search-select, county checkboxes, and an inherit-current-selection edit flow; a new "Co-Registration Managers" button on the Event `Show` page, gated independently of `canManageEvent`/`canManageAuditionEnvironment`. 37 new/updated tests (`VersionRoleAssignmentServiceTest.php`, `VersionCoRegistrationManagersTest.php` new file, `ShowTest.php`); 718/718 passing app-wide, PHPStan clean. **Not built:** any downstream enforcement of a Co-Registration Manager's county scope elsewhere in the app (e.g. restricting which teachers/candidates they can see by county) — this phase is the assignment mechanism only, matching how `version_counties` itself has no enforcement beyond invitation eligibility.
20. ~~**Registration Manager Reporting Module (§5.10) — designed 2026-08-04.**~~ Resolved — **designed and built 2026-08-04**, pagination added 2026-08-06 to four of the five roster-style reports (all but Obligated Teachers). Scoped from a source design doc (`Registration Manager Reporting Module.docx`, copied verbatim to `docs/registration-manager-reporting-module.md`) plus a clarifying-question pass with the product owner, resolving: (1) payments are modeled as two tables — `teacher_payments` (manual check/PO/cash/other entries, plus a reserved-not-written `Electronic` case) and `candidate_payments` (`Electronic` only, schema/report-only until a real e-payment integration exists) — unioned for the Payment Roster report, rather than one polymorphic-payer table, matching the source doc's literal "recorded in separate tables" instruction; (2) a Candidate's "home phone" reuses the existing but lightly-used `phones` table (`PhoneType::Home`, keyed to the Student's own `user_id`) rather than adding a new column — corrected mid-scoping by the product owner, who pointed out the convention (`Phone` model, `PhoneType` enum, `User::phones()`) already exists, current usage is just thin (only a student's cell number at registration); (3) packet-received tracking gets a new `version_teacher_packets` table keyed on (version, school, teacher), with a manual "Send Confirmations" button (not a scheduled job) batch-emailing every teacher whose packet is received but not yet confirmed, plus a `confirmation_sent_by_user_id` column added per product-owner request so the batch send is attributable; (4) Adjudication Backup and Registration Cards ship as full screen shells with placeholder PDF/CSV content this phase, since real room↔Candidate assignment doesn't exist yet (confirmed gap in `version_rooms`) and no in-person audition is scheduled — matching the source doc's own placeholder instruction; (5) access is Founder/Event-wide Event Manager/Registration Manager (all counties, optional filter) or Co-Registration Manager (hard-scoped to their assigned counties), mirroring `canManageAuditionEnvironment`'s shape (§5.9) rather than a narrower reports-only gate; (6) "registration fees due" is `registered-candidate-count × version_fees.registration` evaluated live, not frozen per candidate. **Built same day as scoped** (`a0257e7`), unlike §5.8's separate design-then-build passes — all nine `App\Livewire\Events\Reports\*` components, the `Reports/Index` hub, and the `teacher_payments`/`candidate_payments`/`version_teacher_packets` migrations shipped together 2026-08-04, followed by a cleanup pass and pagination on four of the five roster-style reports 2026-08-06. Confirmed net-new/greenfield during scoping: payments (no prior table of any kind existed), CSV export (no precedent anywhere in `app/`, PDF via dompdf does have one), and the county-scoping query helper (no existing method reads `co_registration_manager_counties` for report-filtering purposes yet). **Still not built:** ~~the `candidate_payments` writer~~ / a real candidate-facing e-payment flow (a manual `candidate_payments` writer was added 2026-08-13, §9 item 33 — see §5.10's own note) and the `teacher_payments.Electronic` writer (both still blocked on `epayment_credentials`), real room↔Candidate assignment (blocks true Adjudication Backup/Registration Cards content), and a re-send/undo path for an already-sent batch confirmation email — see §5.10's own "Not built this phase" note.
21. **Web Registration Manager Module (§5.11) — designed and built 2026-08-05.** Scoped from a source design doc (`Web Registration Manager Module.docx`, copied verbatim to `docs/web-registration-manager-module.md`) plus a clarifying-question pass with the product owner, resolving: (1) role assignment stays exactly as it already works today — generic Configure → Roles tab, Event Manager/Founder only — no dedicated delegation screen like Co-Registration Manager got; (2) "impersonate a teacher" extends the existing Founder-only mechanism (`Founder\Impersonate`, `StopImpersonatingController`, `impersonator_id` session key) rather than building a parallel one, adding two new session keys (`impersonation_scope`, `impersonation_version_id`) and a new global middleware (`RestrictWebRegistrationImpersonation`) that locks the impersonated session to the one Version's Registrations pages and blocks the teacher account/profile routes outright, satisfying the source doc's "should NOT have access to Teacher profile" line without touching Founder's existing unrestricted impersonation behavior; (3) "transfer students" generalizes the existing `ReplacedTeacherStudentTransfer` (same-school-only, auto-triggered at teacher-verification time) into a new explicit-action `TeacherStudentTransferService` covering all three of the source doc's scenarios (same-school replacement, cross-school transfer, grade-band promotion) — critically, it moves `student_teacher` rows via Eloquent `save()`/`delete()` rather than a raw bulk update, so the existing `StudentTeacherObserver` → `AutoEnrollmentService` cascade automatically creates Candidate rows in any other open Version the destination teacher is invited to, meaning the source doc's second bullet needs no new enrollment code at all; (4) both functions are restricted to teachers holding a `version_invitations` row for the Version being managed (any status — the same loose "invited" reading used throughout §5.4/§5.8/§6.2), not any teacher; (5) Registration Manager/Co-Registration Manager are deliberately **not** included in the new `canManageWebRegistration` gate — the source doc names only Web Registration Manager, unlike `canManageAuditionEnvironment`/`canManageReports`. **Built same day** — see §5.11 above for the full built-artifact list. 22 new tests; 837/837 passing app-wide, PHPStan clean. Flagged for a future product-owner check: whether transfers need a durable audit trail beyond incidental row timestamps (no `candidate_status_history`-style log exists for a `teacher_id`/`school_id` move today).
22. **Judge visibility (Dashboard/Events index) and the Adjudication access gate — built 2026-08-07.** A `RoomJudge` assignment (§5.9) had never been wired into any authorization/visibility check — a judge saw a misleading "not managing any events" CTA on the Dashboard, was invisible in `events.index`, and would 403 on `events.show`. Fixed by teaching `VersionRoleAssignmentService` about `RoomJudge` as a fourth, view-only visibility source alongside the six Spatie version-scoped roles: `isJudgeForEvent()`/`judgeEventIds()` feed into `canViewEvent()` and `eventIdsVisibleTo()` (so `canAccessEventsSection()`, the Dashboard Events card, and `events.index` all pick it up for free), while a separate `canAdjudicate(User, Version)` gates the new Adjudication page specifically — active Version + a `RoomJudge` row on *that* Version (not event-wide, unlike the view-only check) + `now()` inside that Version's `date_type=adjudication` `version_dates` window + not a currently-enrolled Student (new `Student::isCurrentlyEnrolled()`, reusing the `is_active`/`class_of >= senior_year` idiom already used by `TeacherStudentTransferService`). The Dashboard's Events card and `events.index` both gained an "Adjudicate" badge/link (`adjudicatableVersionFor()`) pointing at the new `events.versions.adjudicate` route; `events.index`'s "Versions" button was also narrowed to Founder/an actual version-scoped-role holder, since a judge has no reason to see it. `App\Livewire\Events\Adjudicate` + its route/view were scaffolded at this point as a confirmation-only stub (Room/JudgeType/window), with the real scoring UI deferred to item 23. Judges are confirmed **not** required to be Teachers throughout (`RoomJudge.user_id` is a plain User FK). 100+ new/updated tests across `VersionRoleAssignmentServiceTest.php`, `ShowTest.php`, `IndexTest.php`, `DashboardEventsCardTest.php`, and a new `AdjudicateTest.php`/`StudentEnrollmentTest.php`, PHPStan clean throughout (including a newly-cataloged Larastan quirk: chaining `->assertOk()` before a Testable-only assertion like `->assertSet()` in a Livewire test loses the chain's type).
23. **Real Adjudication (judge scoring) page + candidate Recordings — designed and built 2026-08-07.** Follow-up to item 22, scoped from a product-owner-supplied `Adjudication Structure.docx` plus a clarifying-question pass, replacing the item-22 stub. Mined the prior (pre-rewrite) `tdr2025` codebase's working Adjudication feature for proven design decisions — single-pass aggregate SQL for room-wide status instead of N+1 per-candidate queries, voice-part-derived Candidate↔Room membership with no manual assignment table, natural-key `updateOrCreate` for score upserts, `JudgeType`-declaration-order judge ranking — while deliberately departing from it on three points the product owner called out: (1) no default score value (legacy silently defaulted an unscored factor to its "best" value; every factor here requires an explicit selection before save, validated server-side against the exact stepped range the dropdown offers, via a shared `AdjudicationService::optionsForFactor()`); (2) real server-side submission-order gating, not legacy's CSS-only version (legacy sent every judge's scores to the browser regardless, hiding them visually via CSS until you'd submitted your own; here the Room Scoring table's data is never even fetched into the page until *this* judge has saved their own scores for that Candidate — checked against the database, not the live-bound form state, so an unsaved in-progress selection can't trip it early); (3) candidates are identified only by `candidates.id` in the UI, not by name — auditions are anonymous. Teacher-recusal score averaging (a legacy feature) was explicitly ruled out of scope. **Built:** `App\Services\AdjudicationService` (candidate roster derivation grouped by voice part then id, room rubric, per-candidate status bucketing (`none`/`partial`/`completed`/`error` — a different color mapping than legacy's own bucket-to-color assignment), tolerance checking, judge-completion tracking, judge ordering, score persistence); the real `Adjudicate.php`/`adjudicate.blade.php` (Judge Bios, Room Performance Indicator Bar, Candidate roster with voice-part grouping and a live count in its heading, score-entry form with a live Alpine-computed total honoring each factor's `multiplier`, Room Scoring table with tolerance banner). **Candidate recordings**, previously a placeholder (no upload data model existed): a new `Recording` model/migration (`version_id`, `candidate_id`, `file_type`, `uploaded_by`, `approved_at`, `approved_by`, `url`) and a CSV-driven `RecordingSeeder` (following `CandidateSeeder`'s established convention — gitignored local export, skip-with-warning on any row whose FK doesn't resolve against currently-seeded data, chunked upserts; 5,357 of 7,884 source rows resolved against this environment's seed data) registered after `CandidateSeeder` in `DatabaseSeeder`. Per the product owner, `Recording.file_type` (scales/solo/quintet) *is* the score category for this data set, not merely similar text — `recordingsForCandidate()` matches it to `score_categories.description` case-insensitively, ordered by `score_categories.order_by`, and only ever returns rows with both `approved_at` and `approved_by` set. A new unique index on `scores.(version_id, candidate_id, judge_id, score_factor_id)` backs the upsert's natural key. 20 new tests (`AdjudicationServiceTest.php` new file with 9, `AdjudicateTest.php` +5); 892/892 passing app-wide, PHPStan clean. **Not built:** Tab Room, score cut-offs/ensemble assignment (§7.4–§7.5, unchanged reference-only), teacher-recusal score averaging, and ~~any authoring UI for the Recording upload flow itself (this phase seeds/reads existing legacy recordings only — there's still no in-app candidate/teacher-facing upload screen)~~ — **built 2026-08-13 (§9 items 33 and 35)**: `CandidateDetail`'s Recordings section is that upload screen, built against the separate `candidate_upload_files`/`version_upload_files` design (§5.2, designed 2026-07-16 but not connected to this table until then) and reconciled with `recordings` via a sync-on-approve bridge, so a teacher-approved upload is now visible to a judge without ever touching this item's own CSV-seeded legacy path.
24. **Score-tally timing decision + `AuditionResult`/`audition_results` (§5.2) — designed and built 2026-08-08.** Item 23 shipped live judge scoring with no concept of a candidate's total score anywhere — the product owner asked when individual factor scores should be tallied into a total, and whether an `AuditionResult` model was the right vehicle, noting the legacy `tdr2025` system kept a running per-candidate tally (`audition_results.csv`: `score_count`/`total`, updated on every judge submission, bundled with the eventual `accepted`/`acceptance_abbr` cut-off outcome in the same row) and that Tab Room needs progress visibility at the candidate, judge, room, and event level. Resolved: (1) two different consumers need the total at two different lifecycle points, and conflating them is what made the legacy design awkward — a **live, on-demand** total while adjudication is open (must never drift after a judge revises a score or Tab Room overrides one; a stored running counter would need correct delta math that `Score::updateOrCreate()`'s natural-key upsert doesn't naturally provide) versus a **frozen** total once adjudication is done (cut-offs, results PDFs, and rehearsal exports need a stable number immune to a later `Score` edit); (2) the live total is `AdjudicationService::candidateTotals()` — a single grouped aggregate query honoring each `score_factors.multiplier` (previously computed only client-side in the Alpine preview, now available server-side), following the exact pattern `candidateStatuses()`/`candidateTolerances()` already established; (3) `AuditionResult`/`audition_results` is real and worth building, but narrower than both the legacy CSV and the doc's original §5.2 sketch — it holds only the frozen tally (`score_count`, `total`, dimension snapshot), written once at Version close (§7.6, the one already-spec'd finalization event); the legacy/doc's `accepted`/`accepted_ensemble_id` columns are deliberately excluded, since that's a separate decision made moments later by the §7.4–§7.5 cut-off algorithm and belongs on `Candidate` (which already has `accepted`/`not_accepted` status states) rather than duplicated as a second in/out flag here; (4) Tab Room's four-level progress need (candidate/judge/room/event) doesn't touch `AuditionResult` at all — it's satisfied by extending the same live-aggregate approach with two new rollups, `AdjudicationService::roomProgress()` (room-level bucket counts, rolling up `candidateStatuses()`) and `versionProgress()` (per-room rollup across a Version's rooms) — `AuditionResult` only ever answers "what was this candidate's final score," never "how's it going right now." **Built:** `audition_results` migration/model (`AuditionResult`, frozen-snapshot docblock explaining the split from the legacy design); `AdjudicationService::candidateTotals()`/`roomProgress()`/`versionProgress()`; `AuditionResultSeeder` (registered after `RecordingSeeder`), following the same gitignored-CSV/FK-skip-with-warning/chunked-upsert convention as `RecordingSeeder`, deliberately importing only the tally columns from `audition_results.csv` and dropping its `accepted`/`acceptance_abbr` columns per the split above. No writer/trigger for the Version-close freeze yet — that's real Tab Room scope (§7.3/§7.6), still not built. ~~Still not built as of this item~~ — **superseded by §9 item 27**: `AuditionResult` now gets written per-Candidate at cut-off-decision time (`EnsembleCutoffService::acceptCandidate()`/`rejectCandidate()`), not at a single Version-close freeze moment as this item originally assumed — a Candidate's snapshot is written and can be re-written as many times as their cutoff decision is revisited (§7.4's reopen-and-re-decide model), which turned out to be the more natural mapping once cut-offs were actually designed. 7 new tests (`AuditionResultTest.php` new file with 3, `AdjudicationServiceTest.php` +4); 899/899 passing app-wide, PHPStan clean.
25. **Breadcrumb 403 for Registration/Co-Registration Manager on Rooms, Scoring Rubric, and Reports (§5.9/§5.10) — found and fixed 2026-08-10.** The Version-name breadcrumb segment on `version-rooms.blade.php`, `version-scoring-rubric.blade.php`, and `reports/index.blade.php` linked unconditionally to `events.versions.edit` (`VersionEdit`, gated `canManageEvent` — Founder/Event Manager only), even though all three pages are themselves reachable by Registration Manager/Co-Registration Manager via `canManageAuditionEnvironment`/`canManageReports`. Clicking that breadcrumb as one of those roles 403'd — reported via a screenshot of an impersonated Registration Manager on the Reports hub. Fixed by rendering that segment as a plain non-clickable `<span>`, matching the pattern the Co-Registration Managers (§5.3) and Web Registration (§5.11) breadcrumbs already used. `version-invitations.blade.php`/`version-pitch-files.blade.php` keep the clickable link unchanged — both are `canManageEvent`-gated screens only Founder/Event Manager can reach in the first place, so the same failure mode doesn't apply there.
26. **Tab Room Module — Phase 1: Add/Edit Scores + Adjudication Tracking — designed and built 2026-08-10.** Scoped from a source design doc (`Tab Room Module.docx`, read via its 13 embedded screenshots plus text) plus a clarifying-question pass with the product owner, resolving: (1) sequencing as three independently-shippable phases — Add/Edit Scores + Adjudication Tracking (reusing `AdjudicationService` directly), then Ensemble Cut-offs (needing the new `CutoffStrategy` engine), then Reports + Close Audition (still unbuilt, §7.6); (2) `CutoffStrategy` is configured once per Version during setup (`VersionEdit`, alongside `score_order`/`judge_count`), not chosen ad hoc on the Ensemble Cut-offs page; (3) Add/Edit Scores writes `scores.overridden_by_user_id`/`overridden_at` when a Tab Room Manager enters/changes a score on a judge's behalf, distinguishing a Tab-Room override from the judge's own entry without changing `Score`'s core shape. **Built:** `VersionRoleAssignmentService::canManageTabRoom()`; the override-audit migration; `AdjudicationService` additions — `saveScores()` gained an optional override-stamp param, `findCandidate()` (id-or-last-name search), `roomsForCandidate()`, `changeVoicePart()` (the destructive scores-and-non-relevant-recordings cascade the source doc calls out), `judgeScoreSummaryForCandidate()`, `candidateJudgeBreakdown()` (a batched, non-N+1 version of the former for a whole Room), `versionCandidateTotal()`; `App\Livewire\Events\TabRoom\AddEditScores` (search → room/judge selection → score-entry form, reusing the Adjudicate page's form markup rather than extracting a shared component, since Adjudicate's own tests/markup stayed untouched); `App\Livewire\Events\TabRoom\AdjudicationTracking` (overall/per-Room progress bars, a per-Candidate badge grid with a judge-by-judge hover breakdown, a Red/Amber/Green dot per Room, an out-of-tolerance trailing asterisk on a Room's name, a 10s `wire:poll` refresh — the page is meant to be left open); a `TabRoom\Index` hub page; routes/nav wiring on the Event `Show` page. **Two real bugs found via live browser testing and fixed the same day:** (a) `AdjudicationService::candidatesForRoom()` was scoped to `status=Registered` only — harmless until Ensemble Cut-offs (item 27) started transitioning Candidates straight to `accepted`/`not_accepted`/`no_show`/`incomplete`, at which point a Candidate silently vanished from their Room's roster and progress the instant a cutoff was applied for their Voice Part; widened to a new `CandidateStatus::roomTrackingStates()` set (Registered + all four resolved outcomes). (b) `Flux::` toast/modal dispatches across the app (including three pre-existing ones — `Show.php`'s add-version/edit-ensemble modals, `Index.php`'s edit-event modal) were dispatching a `close-modal` event; Flux's actual Alpine component (`vendor/livewire/flux-pro/dist/flux.js`) listens for `modal-close` — all four call sites fixed. **Also this pass:** `events.versions.tab-room.tracking` (and, once built, `.cutoffs`) registered as Fast Pass trackable pages. Verified passing at every step; 944/944 Feature/Unit tests app-wide immediately after this item + item 27 shipped together, PHPStan clean.
27. **Tab Room Module — Phase 2: Ensemble Cut-offs + the `CutoffStrategy` engine — designed and built 2026-08-10–11.** The four `CutoffStrategy` cases (§7.5, §8.5) had been reference-only since the 2026-07-09 audit (§9 item 7); this item makes them real. **Built:** migrations for `candidates.accepted_ensemble_id` and `versions.cutoff_strategy`; the `CutoffStrategy` enum (`strategyClass()`, `needsPerEnsembleCutoff()`); `App\Services\CutoffStrategies\CutoffStrategyContract` + four implementations (`PerVoicePartPerEnsembleStrategy`, `SequentialEnsembleFillStrategy`, `AlternatingEnsembleAssignmentStrategy`, `GradeSegmentedEnsemblesStrategy` — see §7.5's mapping table for behavior); `App\Services\EnsembleCutoffService` (ranking, no-show/incomplete transition, the accept/reject + `AuditionResult`-write pair every Strategy calls back into, and the orchestration split between `applyCutoff()`/`applyEnsembleCutoff()`/`finalizeVoicePart()` described in §7.5's implementation note); the `EnsembleCutoffs` Livewire page; a `cutoff_strategy` field on `VersionEdit`. **Three design corrections made mid-build, each caught by the product owner reviewing real output against the source doc, not found independently:** (1) "reopen" was first implemented as destructive — reverting a decided Candidate to `Registered` and deleting their `AuditionResult`. Corrected: reopening is now a pure display toggle; `rankedCandidates()` was widened to include already-decided Candidates (annotated with their current status for color-coding) so a *different* score can be applied in place, and rejection moved from "whoever `finalizeVoicePart()` finds still Registered" to a direct `meetsCutoff()` check against the live pool — letting a Candidate move `accepted → not_accepted` and back without ever passing through `Registered`. (2) `AlternatingEnsembleAssignment` rotated by candidate row, not by distinct score — three Candidates tied at the same score could land in three different Ensembles. Corrected to rotate by score-group: every tied Candidate now moves together (verified against a real "three tied at 100, four tied at 101" case from production data). (3) The first Ensemble Cut-offs UI stacked one full-width, independently-scrolling section per Voice Part — "unreasonable scrolling" per the product owner, who described the prior (pre-rewrite) system's layout from memory. Rebuilt as a single shared-scroll grid, every Voice Part as a side-by-side column, each score itself the "apply cutoff" button. Verified passing at every step (144 assertions across the `EnsembleCutoffServiceTest`/`EnsembleCutoffsTest` files alone by the end of this item); PHPStan clean; see item 26 for the combined 944/944 app-wide figure.
28. **Tab Room Module — Ensemble Breakdown (pie charts) and Ensemble History — designed and built 2026-08-11.** Two "frills" the product owner requested once Ensemble Cut-offs was working: a per-Ensemble visual breakdown by Voice Part, and a way to see the two prior seasons' counts alongside it. **Pie charts:** the dataviz skill's own guidance flags pie as a non-default form for part-to-whole data and flags an 8-slice categorical palette (a Voice Part can be SI/SII/AI/AII/TI/TII/BI/BII) as failing full colorblind-safety checks under an "all-pairs" comparison — surfaced to the product owner via `AskUserQuestion` (segmented-bar alternative vs. pie as literally asked); **pie was chosen explicitly**, accepting the tradeoff. Mitigated per the skill's own guidance for that case: the 8-color categorical palette (validated via `validate_palette.js` on the *adjacent* pairlist, which a pie's fixed circular slice order satisfies) is shared with the Voice Part legend; slices are direct-labeled with `voice_parts.abbr` using a computed (not assumed) white-or-ink text color by fill luminance, skipped below an 8%-share threshold (still reachable via hover/focus and the existing Accepted Candidates table, so nothing is gated behind a label); a follow-up request corrected slice coloring for *decided* rows in the Score Cut-offs grid from a generic accepted/rejected color to each row's actual destination Ensemble's own color, reusing the same palette mapping as the summary table so an Ensemble reads as one consistent color across the whole page. **Ensemble History:** before building anything, checked empirically whether "the previous two years' counts" could be computed live from sibling Versions of the same Event — it can't: the three prior-year NJ All-State Versions in this database have Candidates sitting at `eligible`/`pending`/`registered` with zero `audition_results` rows, meaning nothing about their actual audition outcome was ever migrated into this schema's per-Candidate granularity. Recommended (and the product owner agreed) a separate `ensemble_history` table, manually entered, independent of any Version row. **Built:** `ensemble_history` migration/model (`EnsembleHistory`, unique on ensemble+voice_part+season_year); `App\Services\EnsembleHistoryService` (`priorSeasonYears()`, `historyGrid()`, `saveHistoryRow()`, `recordCurrentSeason()` — the last snapshotting `EnsembleCutoffService::acceptedCounts()` into `EnsembleHistory` keyed by `senior_class_of`); a manual-entry grid per prior season year on the Ensemble Cut-offs page, with a live client-side Total column (`x-data` `@entangle('historyInputs')`, the same pattern `Adjudicate.blade.php`'s score-total already established, not a new one). **Close-hook, per explicit product-owner redirect:** `recordCurrentSeason()` was first exposed as its own "Save to History" button on the Ensemble Cut-offs page — the product owner judged this "as likely to be missed as hit" and asked for it to run as part of Close Version processing instead, so a reopen-correct-reclose cycle is never missed. Since Tab Room Module.docx's own Close Audition description is "close the audition and version" (one action) and the only place `versions.status` is set anywhere in the app today is `VersionEdit`'s general Status field (§7.6's own dedicated Close Audition button remains unbuilt), the hook was wired there: saving Status as Closed now always calls `recordCurrentSeason()`, on every save that results in Closed, not just the first transition into it. Dummy history data (2024, 2025) populated for the two live NJ All-State Ensembles at the product owner's request, for review purposes. `events.versions.tab-room.cutoffs` registered as a Fast Pass trackable page. Verified passing at every step via each affected area's own test run (all green, PHPStan clean throughout); the product owner is independently running the complete app-wide suite as of this doc update to reconfirm the final state.
29. **Tab Room Module — Phase 3, Close Audition half — designed and built 2026-08-11.** Follow-up to items 26–28, scoping the remainder of §7.6 (Reports is the other half, still unbuilt — see this item's "Not built" note). Resolved via the pattern already established by `VersionEdit::mount()`'s gate check: `VersionEdit` is `canManageEvent`-only (Founder/Event Manager), so a Tab Room Manager who isn't also an Event Manager has zero access to it — meaning `results_released_at` (the new results-visibility gate) needed a home a Tab Room Manager could actually reach, and needed to be independent of `versions.status` so an Event Manager closing a Version for unrelated administrative reasons via the general form never silently releases results as a side effect. **Built:** `versions.results_released_at` (nullable timestamp) migration + `Version` cast; `App\Livewire\Events\TabRoom\CloseAudition` (`canManageTabRoom()`-gated; `close()` sets `status=Closed` + stamps `results_released_at` + calls `EnsembleHistoryService::recordCurrentSeason()`, same snapshot `VersionEdit`'s Status field already triggers, since Close is now a second independent path to Closed; `reopen()` reverts `status=Active` and nulls `results_released_at` only, leaving every Candidate's cutoff decision untouched — reopening is a visibility toggle, not an undo); `App\Mail\AuditionResultsAvailableMail` + markdown view, batch-sent (`Mail::to($email)->send(...)` per recipient, matching `PacketReceivedMail`'s existing batch-send convention) to every distinct teacher email with a resolved-outcome Candidate (`accepted`/`not_accepted`/`no_show`/`incomplete` — not `ParticipatingTeachers::baseRows()`, which filters to `status=Registered` and is empty by the time a Version reaches Close); a `Candidate::auditionResult()` relation that had never been added despite `AuditionResult` existing since item 24. **Follow-up request the same day:** the mail's signature line, originally `config('app.name')` (matching every other mailable in this codebase), was changed to name the closing manager specifically — `{{ $closedBy->name }}, Event Manager {{ $version->name }}` — a one-off departure from the shared convention, confirmed intentional rather than applying it app-wide. `users.name` is a live, `UserObserver`-synced column (rebuilt from `first_name`/`middle_name`/`last_name`/etc. via `NameFormatter::buildDisplayName()` on every save), not the dead Laravel-scaffold default it can look like — safe to read directly. **Also fixed:** `Registrations\Results`/`ResultsIndex`, both discovered mid-build as explicit placeholders (hardcoded "—" score/accepted/ensemble columns, candidate queries filtered to `status=Registered`) predating Ensemble Cut-offs — widened to the same four resolved-outcome statuses and gated on `results_released_at !== null` instead of `status='closed'`, so a Version closed for other reasons without releasing results stays invisible to teachers. Route, Tab Room hub card, and Fast Pass tracking registered for `events.versions.tab-room.close`. 15 new tests (`CloseAuditionTest.php`, `ResultsTest.php`, `ResultsIndexTest.php`, `AuditionResultsAvailableMailTest.php`); 976/976 passing app-wide, PHPStan clean. **Not built:** the Reports sub-module (Audition Scores by voice part, Combined Audition Scores confidential/public, Ensemble Participation, Student Seniority — five reports per Tab Room Module.docx), which is the next piece of work.
30. **Tab Room Module — Phase 3, Reports half — designed and built 2026-08-11.** Completes items 26–29's Tab Room Module in full (every sub-module in Tab Room Module.docx is now built). Followed the existing Registration Manager Reporting Module's own filter→preview→export convention (Livewire page + `GET .../export.{format}` invokable controller, `Pdf::loadView(...)`/`CsvExport::stream(...)`) rather than inventing a new one, with two departures the source doc's own report list forced: (1) a required Ensemble/Voice Part filter drives what's shown (every existing report in the Registration Manager module is search-only, no report there requires choosing something before it shows data); (2) `TabRoomReportService::auditionScoreRows()` reads `scores` directly by `(version_id, voice_part_id)` rather than resolving Room/rubric membership like `AdjudicationService`'s live-scoring queries must — every `Score` row already carries its own frozen `judge_order_by`/`score_factor_order_by`/`voice_part_id` snapshot (Score's own docblock), so a report scoped to one Voice Part needs no Room lookup at all, meaningfully simpler than the live adjudication pages' Room-scoped queries. **Built:** `VersionRoleAssignmentService::canManageTabRoomReports()` (same gate as `canManageTabRoom()`, kept as its own named method per the `canManageReports()`/`canManageAuditionEnvironment()` precedent); `App\Services\TabRoomReportService` (`auditionScoreRows()`, `combinedScoreRows()`, `ensembleParticipationRows()`, `studentSeniorityRows()` — the last doing the one genuinely new cross-Version join, via `$version->event->versions()`, keyed by `senior_class_of`); five `App\Livewire\Events\TabRoom\Reports\*` pages plus a `TabRoom\Reports\Index` hub, all under a new `events.versions.tab-room.reports.*` route group; four export controllers under `App\Http\Controllers\Reports\TabRoom\*` (Audition Scores is PDF-only per the source doc; the other three are PDF+CSV) originally shipped on `Pdf::setPaper('a4', 'landscape')` — see item 31, corrected to `'letter'` the next day — the first landscape PDF usage in this codebase, since a per-judge-per-factor score matrix is wide. Reports 2/3 (Combined Audition Scores confidential/public) share one abstract Livewire base class and one export controller keyed by a `confidential` bool — the row query is identical either way, only column projection differs — rather than duplicating either. **One real bug caught by a test, not by PHPStan:** `ensembleParticipationRows()`'s batched-totals code originally built the candidate_id→total map via `Collection::flatMap()` across per-Voice-Part groups; `flatMap()` (like `merge()`) renumbers/collapses integer keys, silently discarding the very mapping being built — every row's total came back 0. PHPStan had nothing to say about it (both sides type-checked as plain arrays); a genuine `TabRoomReportServiceTest` assertion failure (`expected 90, got 0`) caught it, fixed by switching to a `reduce()` with the `+` (array-union) operator, which preserves keys. **Also fixed as a byproduct:** two Larastan false-negatives-turned-real-catches in the new service — `$candidate->status->label()` (the enum-cast-via-`casts()`-method quirk already cataloged in this project's PHPStan-quirks memory) needed `CandidateStatus::from($candidate->getRawOriginal('status'))->label()` instead, and two `?->` nullsafe accesses on `Candidate::voicePart` were flagged `neverNull` correctly this time (unlike the cataloged false-positive case) — `candidates.voice_part_id` is a non-nullable FK, so the relation is genuinely never null; switched to plain `->`. 24 new tests (`TabRoomReportServiceTest.php` — 4; five `TabRoom/Reports/*Test.php` component files — 14; a `TabRoomExportControllersTest.php` HTTP smoke test hitting every export controller's actual PDF/CSV output — 5, new territory this codebase's other report exports have never had direct coverage for; and one `canManageTabRoomReports` case added to `VersionRoleAssignmentServiceTest.php`); PHPStan clean.
31. **Teacher-facing public score reports + optional Share Results — designed and built 2026-08-12.** Direct product-owner ask (not a source design doc) plus a clarifying-question pass, resolving: (1) the Per-School report is scoped strictly to `school_id` **and** `teacher_id` — a co-teacher at the same School gets their own separate report, not a merged one, since the literal ask named both; (2) the Per-Person report is a single row/page for that one Candidate, not that Candidate plus their Voice Part peers; (3) both required reports reuse the exact *public* (Candidate #-only, no student/school/teacher identity) score-grid shape `TabRoomReportService` already built for the Tab Room Manager's Reports sub-module (§9 item 30) — one consistent report format across all three PDFs this item adds, even though the requesting teacher already knows whose Candidates these are; (4) `share_results`'s auto-generated PDF is the existing "All Ensembles" public export (`GenerateCombinedScoresPdfJob`/`CombinedScoresPdfExport`), queued at Close rather than re-rendered per teacher-download-click, since that job's own docblock already established DomPDF can't render it synchronously at real data volume. **Built:** `TabRoomReportService::candidateScoreRow()` (single-row/single-Voice-Part table shape, thin wrapper over the already-cached `auditionScoreRows()`); `Registrations\Results` gained per-School and per-Candidate view-modal + PDF-download icons (new `resources/views/livewire/registrations/partials/candidate-score-report.blade.php` partial, shared between both modals since they're new code authored together — unlike the Tab Room screens' own deliberate no-shared-partials convention, which exists to avoid touching already-tested code); `SchoolScoreReportPdfController`/`CandidateScoreReportPdfController` (flat `App\Http\Controllers` namespace + ownership-check style, matching `CandidateApplicationPdfController` rather than the Tab Room Reports' `VersionRoleAssignmentService`-gated style, since this is Registrations-side); `versions.share_results` (migration/model/`VersionEdit` General-tab checkbox with the exact specified helper text, saved via `saveGeneral()` alongside `pitch_file_visibility`); `CloseAudition::close()` queues the public All-Ensembles export when `share_results` is on (same dedupe-by-generation check `requestAllEnsemblesPdf()` uses, minus its "already-fresh → re-email it" branch); `SharedScoresPdfController` redirects a standing teacher to a 5-minute signed S3 URL once that export is `Completed`. Also made sortable (Name, Voice Part, Score, Result, Ensemble) at the same time, per a same-day follow-up request — Voice Part sorts by `voice_parts.sort_order`, not `name`/`abbr`, since sort_order is the score-order/rubric-relevant ranking. **Two real defects caught from a live-tested PDF, not by PHPStan or the test suite:** (a) both new PDF controllers were missing `->setPaper(...)` entirely (defaulting to portrait), which silently clipped the wide per-judge-per-factor score matrix — confirmed by reading the actual downloaded PDF (Swan category's Judge 3, plus the Total/Result columns, were entirely absent); fixed to landscape, and while in there, discovered every one of this codebase's *existing* five Tab Room export call sites (§9 item 30, `GenerateCombinedScoresPdfJob` included) had shipped on `setPaper('a4', 'landscape')` — corrected all seven call sites app-wide to `'letter'` per explicit product-owner direction, not just the two new ones. (b) the Results-page score-report modals were sized far too narrow (`md:w-[42rem]`/`md:w-[52rem]`, form-modal widths copied from elsewhere in the app) for a wide data grid; widened to `w-full max-w-7xl`, still wrapped in the existing horizontal-scroll container for anything wider still. One authoring mistake caught by a genuine Blade compile error (not by review): the new partial's opening comment was closed with Twig-style `#}}` instead of Blade's `--}}`, which compiled into a raw `echo` of the entire comment body — caught immediately by `ResultsTest.php` failing with a PHP syntax error naming the exact file. New/updated tests: `TabRoomReportServiceTest.php` (+2, `candidateScoreRow()`), `RegistrationsScoreReportPdfControllersTest.php` (new file, 6), `SharedScoresPdfControllerTest.php` (new file, 4), `CloseAuditionTest.php` (+3, share_results queue/skip/dedupe), `VersionEditTest.php` (+1), `ResultsTest.php` (+2, sort behavior including the `sort_order`-not-`abbr` regression case). 1039/1039 passing app-wide, PHPStan clean (two cataloged Larastan quirks hit again in the new sort comparator — `CandidateStatus::from($candidate->getRawOriginal('status'))` instead of `$candidate->status` for the enum-cast-via-`casts()` issue, and plain `->` instead of `?->` on the non-nullable `Candidate::voicePart` FK — both per the precedents already logged in item 30). **Not built:** a Version-wide confidential/detailed results summary and teacher-facing pitch-file retention (§7.6's own two remaining un-scoped bullets, unchanged by this item).
32. **Obligations iron gate widened + reinstatement on re-accept; `CandidateDetail` (§6.2) redesigned to card+modal with Student/Home Address editing; emergency-contact NOT NULL bug fixed — built 2026-08-12.** A run of direct product-owner requests, not a source design doc, resolved as they came in. **Iron gate, widened:** §5.6's rejection consequence (built 2026-07-10) only ever blocked new enrollment and showed a callout on `VersionDashboard` — every *other* Version-scoped teacher page (the dashboard itself, `CandidateDetail`) stayed reachable regardless of decision. Per explicit direction ("their status on accessing any other pages of the version are no different than if they had no Obligation record"), a new shared trait, `App\Concerns\GuardsAcceptedObligations`, now redirects to `registrations.obligations` from both `VersionDashboard::mount()` and `CandidateDetail::mount()` whenever the teacher's decision isn't `accepted` — no response and `rejected` are treated identically. The old callout was removed from `version-dashboard.blade.php` since the page is never reached while blocked. **Reinstatement on re-accept:** previously documented as a known one-way limitation (§5.6: "does not resurrect candidates withdrawn during the rejection window"). `CandidateService::reinstateAllForTeacherVersion()` (new) is now called from `VersionObligations::accept()`: every `TeacherWithdrawn` candidate for that teacher+Version is reset to `Eligible` and immediately re-run through `recalculateStatus()`, so one that was fully checklisted before the rejection lands straight back on `Registered`. New limitation, not fixed: nothing distinguishes a candidate the rejection cascade withdrew from one the teacher withdrew by hand for an unrelated reason — re-accepting reinstates both alike, since both share the one `TeacherWithdrawn` status with no reason code. **Two navigation gaps closed:** `Registrations\Index` (`/registrations`) gained a per-Version "Obligations" badge (green/amber/red by decision) linking to `registrations.obligations`, for any Version with a published obligation in the "Open for Registration" and "Active Candidates" buckets — previously a teacher who'd already accepted had no way back to that page except editing the URL by hand; `VersionObligations`'s own page gained a "Review Eligible Students" success callout, shown once `accepted`, linking to `registrations.version` — closing the "what do I do next?" dead end the page left a teacher in. `registrations.version` was also registered as a Fast Pass trackable page (new migration inserting the `trackable_pages` row directly, following the seeded-migration precedent that table's own creation migration set — no seeder file exists for it), per the same convention noted in items 26/28. **`CandidateDetail` redesign:** reorganized into a fixed section order (Student, Home Address when required, Emergency Contacts, Program Name, Candidate Application) and a uniform card+modal pattern throughout, including converting Program Name — previously the page's one always-open inline form — to match, after weighing the tradeoff directly with the product owner (`AskUserQuestion`: inline forms vs. card+modal for mostly-static, rarely-re-edited data; card+modal chosen for consistency with the rest of the app and with Emergency Contacts' own existing modal). New editable fields: Student name (first/last, via the Student's `User`) and voice part — the latter a genuinely new capability, writing `candidates.voice_part_id` (never `students.voice_part_id`, per explicit direction to keep a Candidate's actual audition voice part independent of the Student's general-profile one) constrained to `Version::availableVoiceParts()`; Home Address (all five columns); and per-contact Emergency Contact editing (previously add-only) via a shared Add/Edit modal branching `saveEmergencyContact()` between `create()`/`update()` on a new `editingEmergencyContactId` property. **Emergency-contact NOT NULL bug, found and fixed:** `emergency_contacts.email`/`cell_phone` were `NOT NULL` at the schema level, but `versions.emergency_contact_cell`/`emergency_contact_email` (§5.1 — already-existing, already-admin-configurable per-Version flags) make each one only *conditionally* required, and `saveEmergencyContact()`'s own validation already marked both `nullable` — a teacher leaving either blank on a Version that didn't require it would crash on save. Root cause: `emergency_contact_cell`/`emergency_contact_email` were scaffolded (column, cast, `VersionEdit` checkbox) but never actually read anywhere downstream — only the blanket `emergency_contact_name` flag gated the section at all. Fixed at three points: a new migration makes both columns nullable; `saveEmergencyContact()`'s validation now marks `ec_cell_phone`/`ec_email` `required` only when the matching flag is true; `HasCandidateChecklist`'s "Emergency contact" item now requires a qualifying contact to actually carry whichever sub-fields the Version demands, not just that some contact exists. No `version_config_emergency_contact` table exists anywhere in this codebase — a user message describing one was corrected in-session; the three flags have lived as plain `versions` columns since the original §5.6-era migration. 64 new/updated tests across `VersionDashboardTest.php`, `CandidateDetailTest.php`, `IndexTest.php` (Registrations), and `VersionObligationsTest.php`; 431/431 passing in the Registrations+Events suites as of this item, PHPStan clean app-wide throughout. **Not built:** any reason/source code distinguishing a rejection-cascade withdrawal from a manual one (flagged above); anything beyond the `registrations.version` Fast Pass registration for the badge/callout additions (no new tracked routes needed, since both new affordances link to already-tracked or already-reachable pages).
33. **Application/Recordings/Payment sections added to `CandidateDetail` — designed and built 2026-08-12.** Direct product-owner request naming three new sections for the candidate manage page, each independently gated on Version configuration. **Application:** the existing §5.7 toggle-buttons UI moved into a card+modal (the modal shows the real merged document via the same shared `document.blade.php` partial the PDF/admin-Preview already used — see §5.7's own updated "Teacher certification" note). **Recordings:** builds out the `candidate_upload_files`/`version_upload_files` design that had been sitting designed-but-unbuilt since 2026-07-16 (§9 item 14) — upload (S3, mime-restricted by `versions.upload_type`), inline audio/video playback, approve, and reject (delete-and-reopen-the-slot, matching Version Pitch Files' precedent); only rendered for `audition_type = remote`. Two new checklist items ("Upload of required audio/video files" / "Teacher approval of...") implement the registration-gate mapping already sketched in §6.2. **Payment — scoped via a clarifying-question pass, not assumed:** `CandidatePayment`'s own docblock already stated *"schema/report-only... no writer exists yet, as there is no candidate-facing e-payment integration built"* — no payment gateway SDK exists anywhere in `composer.json`, confirmed before building anything. Presented three options (real gateway integration / placeholder UI / drop the e-payment framing entirely) via `AskUserQuestion`; **placeholder UI chosen**: a new `version_teacher_epayment_opt_ins` table (teacher+Version-scoped opt-in checkbox, not per-Candidate) plus a manual `candidate_payments` recording form, both gated on `Version::epaymentCredential()` resolving a row, both UI-labeled and toast-labeled as a manual ledger entry, never a live charge. 48 CandidateDetail tests passing by end of item, 1079/1079 app-wide, PHPStan clean.
34. **S3 signed-URL bug fixes across Recordings, Pitch Files, and Application logos; phone-number formatting fix — found and fixed 2026-08-12–13.** Live browser-testing of item 33's Recordings section surfaced "uploaded a real mp3, player shows 0:00/0:00 and won't play." Diagnosed (not assumed) via a direct `curl` against the stored S3 object: the file itself was fine (1.2MB, correct size) — a plain `Storage::disk('s3')->url(...)` returned HTTP 403, because this bucket's newer objects default to private; a signed `->temporaryUrl(...)` (verified 200 against the same object) fixed it. **Same bug, same fix, applied to two more call sites the user asked to check after the first fix landed:** Version Pitch Files' audio/PDF-read links (§5.5) — using the identical plain-`->url()` pattern the Recordings code had been copied from — and `CandidateApplicationData::resolveLogoUrl()` (§5.7), flagged proactively before it could bite (all seeded logo columns are blank, so it had never been observed broken). **Also fixed, same pass:** `preload="none"` on the Recordings/Pitch-Files `<audio>`/`<video>` tags meant the displayed duration stayed stuck at 0:00/0:00 until the user pressed play even after the URL fix — changed to `preload="metadata"` (fetches just the header, not the whole file) on a direct follow-up request. **Also fixed in `CandidateApplicationData::fromCandidate()`, caught by a new test:** `teacherCellPhone`/`studentCellPhone`/`emergencyContactPhone` were passed through as raw normalized digits instead of being run through the existing `PhoneNormalizer::format()` helper (already the Reports module's own convention) — fixed to render `(201) 555-1234`-style; a genuinely separate bug surfaced in the same pass, `$emergencyContact->name`/`->preferred_phone` accessed without a null check (would fatal-error for any Candidate with no emergency contact on file), fixed with an explicit ternary rather than `?->` per this project's cataloged Larastan `?-> `nullsafe.neverNull` false-positive quirk. 12 new tests in `CandidateApplicationDataTest.php`; 1079/1079 app-wide, PHPStan clean throughout.
35. **Recordings → `recordings` (Adjudication) sync bridge; amber "in progress" checklist status; `RecordingReviewService` mis-filed-recording assist — designed and built 2026-08-13.** Three follow-on requests in one continuous thread. **Sync bridge:** a user report — "I approved a recording but don't see it in the `recordings` table" — surfaced that item 33's Recordings section (`candidate_upload_files`) and the pre-existing judge-facing `recordings` table (§9 item 23) were two independent, disconnected models of the same real-world thing, with no path between them; an approved upload was genuinely invisible to a judge. Presented three reconciliation options (sync bridge / full rebuild onto `recordings` / leave disconnected) via `AskUserQuestion`, including an honest tradeoff discussion (two tables to keep in step vs. touching already-tested Adjudication code); **sync bridge chosen** — see the `recordings` table note in §5.2 for the full approve/reject/replace sync contract and its one known limitation (slot names vs. score category names aren't enforced to match). **Amber checklist status:** the Upload/Approval checklist items were previously strict green/red; per direct request, `HasCandidateChecklist`'s item shape gained an optional `partial` closure (consulted only when `check` is false) so "some but not all" now renders amber with a `minus-circle` icon, across all three places checklist pills render (`CandidateDetail`, and both the mobile-card and desktop-table views on `VersionDashboard`'s roster) — every other checklist item is unaffected, since none of them define `partial`. Checklist labels also became upload-type-aware ("audio files"/"video files" instead of the generic "audio/video files") in the same pass. **`RecordingReviewService` — the mis-filed-recording assist:** per direct request, addressing "teachers uploading a scales file into the solo slot, ~90% caught by manual review, not all" — two cheap, non-blocking heuristics run synchronously at upload time, discussed via a genuine back-and-forth (the product owner asked whether the filename check depends on correct naming — clarified it depends on the filename carrying *any* descriptive signal, matching a wrong slot rather than requiring a match to the right one) before being greenlit as "belt-and-suspenders": (1) a filename check flags when the upload's original filename matches a *different* known slot in the Version but not the one it's being uploaded to, silent on generic device filenames (no signal either way); (2) a duration check flags when the file's length falls outside ±1 standard deviation of every *approved* prior upload for that exact slot, silent until at least 3 approved priors exist (the "hurdle" the product owner explicitly expected and accepted). New dependency `james-heinrich/getid3` (pure PHP, no ffmpeg binary — confirmed none installed) for real duration extraction; new `candidate_upload_files` columns `original_filename`/`duration_seconds`/`flagged_at`/`flag_reason`; a red "Review Suggested" badge + reason text on the Recordings card, never blocking approve/reject. Both thresholds (`FLAG_STD_DEVS = 1.0`, `MIN_SAMPLE_SIZE = 3`) are named constants for easy tuning. 58 CandidateDetail tests, 10 dedicated `RecordingReviewServiceTest.php` tests (including a genuine WAV-file duration-extraction test, not a mocked one), 119 passing across Registrations + `RecordingReviewServiceTest`, PHPStan clean app-wide.
